"""Native Media Ingestion Adapter for Cresmo.

Directly orchestrates yt-dlp and openai-whisper via official Python library APIs.
Provides robust native subtitle extraction (mitigating HTTP 429), ephemeral
scratch isolation for audio transcription, and zero legacy isb.ai coupling (ADR-004 & SPEC-004).
"""

from __future__ import annotations

import json
import re
import shutil
import tempfile
import threading
import urllib.error
import urllib.request
from datetime import UTC, datetime
from pathlib import Path
from typing import Any

import yt_dlp

from cresmo.application.ports import MediaIngestionPort
from cresmo.domain.entities import RawTranscript
from cresmo.domain.exceptions import (
    CresmoInfrastructureError,
    DomainValidationError,
    IngestionNetworkError,
    RateLimitExceededError,
)
from cresmo.domain.taxonomy import classify_channel
from cresmo.domain.value_objects import (
    ChannelFeedQuery,
    ContentId,
    DiscoveredMediaItem,
)
from cresmo.infrastructure.adapters.header_generator import RandomHeaderGenerator

_VIDEO_ID_REGEX = re.compile(r"(?:v=|\/)([a-zA-Z0-9_-]{8,64})(?:[&?]|\Z)")
_ILLEGAL_FS_CHARS = re.compile(r'[\\/*?:"<>|%]')


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut: MutantDict = {}  # type: ignore
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut: MutantDict = {}  # type: ignore


class NativeMediaIngestionAdapter(MediaIngestionPort):
    """Production Hexagonal adapter for media ingestion using pure Python libraries."""

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut)
    def __init__(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_orig(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_1(
        self,
        request_timeout: float = 31.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_2(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 3,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_3(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = None
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_4(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = None
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_5(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator and RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_6(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = None

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_7(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(None)

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_8(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(None, whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_9(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, None))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_10(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(whisper_concurrency_limit))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_11(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(1, ))

    def xǁNativeMediaIngestionAdapterǁ__init____mutmut_12(
        self,
        request_timeout: float = 30.0,
        header_generator: RandomHeaderGenerator | None = None,
        whisper_concurrency_limit: int = 2,
    ) -> None:
        """Initialize NativeMediaIngestionAdapter.

        Args:
            request_timeout: Socket timeout in seconds for subtitle HTTP requests.
            header_generator: Optional dynamic browser request header generator.
            whisper_concurrency_limit: Maximum concurrent Whisper model executions.
        """
        self.request_timeout = request_timeout
        self._header_generator = header_generator or RandomHeaderGenerator()
        self._whisper_semaphore = threading.BoundedSemaphore(max(2, whisper_concurrency_limit))

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut)
    def _extract_video_id(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(url)
        if match:
            return match.group(1)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_orig(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(url)
        if match:
            return match.group(1)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_1(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = None
        if match:
            return match.group(1)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_2(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(None)
        if match:
            return match.group(1)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_3(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(url)
        if match:
            return match.group(None)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_4(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(url)
        if match:
            return match.group(2)
        return Path(url).stem

    def xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_5(self, url: str) -> str:
        """Extract YouTube video identifier from URL string."""
        match = _VIDEO_ID_REGEX.search(url)
        if match:
            return match.group(1)
        return Path(None).stem

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut)
    def _sanitize_fs_name(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_orig(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_1(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = None
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_2(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub(None, name).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_3(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", None).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_4(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub(name).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_5(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", ).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_6(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("XX_XX", name).strip()
        return cleaned or "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_7(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned and "Unknown_Channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_8(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned or "XXUnknown_ChannelXX"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_9(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned or "unknown_channel"

    def xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_10(self, name: str) -> str:
        """Sanitize channel or video title for filesystem safety."""
        cleaned = _ILLEGAL_FS_CHARS.sub("_", name).strip()
        return cleaned or "UNKNOWN_CHANNEL"

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut)
    def _is_native_subtitle_url(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "tlang=" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_orig(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "tlang=" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_1(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "tlang=" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_2(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return True
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "tlang=" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_3(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "XXtlang=XX" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_4(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "TLANG=" not in url

    def xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_5(self, url: str | None) -> bool:
        """Validate that subtitle URL is native and not an on-the-fly machine translation."""
        if not url:
            return False
        # Strictly reject on-the-fly machine-translated subtitles that cause YouTube HTTP 429
        return "tlang=" in url

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut)
    def _find_native_subtitle_url(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_orig(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_1(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = None
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_2(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") and {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_3(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get(None) or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_4(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("XXsubtitlesXX") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_5(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("SUBTITLES") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_6(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = None

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_7(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") and {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_8(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get(None) or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_9(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("XXautomatic_captionsXX") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_10(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("AUTOMATIC_CAPTIONS") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_11(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = None
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_12(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").upper()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_13(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(None).lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_14(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") and "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_15(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get(None) or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_16(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("XXlanguageXX") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_17(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("LANGUAGE") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_18(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "XXXX").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_19(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = None

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_20(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith(None)

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_21(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("XXptXX")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_22(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("PT")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_23(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = None
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_24(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["XXpt-origXX", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_25(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["PT-ORIG", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_26(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "XXpt-BRXX", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_27(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-br", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_28(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "PT-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_29(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "XXptXX", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_30(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "PT", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_31(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "XXpt-PTXX", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_32(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-pt", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_33(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "PT-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_34(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "XXen-origXX", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_35(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "EN-ORIG", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_36(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "XXenXX", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_37(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "EN", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_38(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "XXen-USXX"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_39(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-us"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_40(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "EN-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_41(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = None

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_42(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["XXen-origXX", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_43(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["EN-ORIG", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_44(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "XXenXX", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_45(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "EN", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_46(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "XXen-USXX", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_47(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-us", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_48(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "EN-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_49(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "XXpt-origXX", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_50(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "PT-ORIG", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_51(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "XXpt-BRXX", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_52(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-br", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_53(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "PT-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_54(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "XXptXX", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_55(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "PT", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_56(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "XXpt-PTXX"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_57(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-pt"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_58(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "PT-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_59(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang not in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_60(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = None
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_61(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get(None)
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_62(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("XXurlXX")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_63(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("URL")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_64(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" or self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_65(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get(None) == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_66(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("XXextXX") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_67(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("EXT") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_68(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") != "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_69(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "XXjson3XX" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_70(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "JSON3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_71(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(None):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_72(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(None)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_73(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = None
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_74(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get(None)
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_75(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("XXurlXX")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_76(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("URL")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_77(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" or self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_78(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get(None) == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_79(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("XXextXX") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_80(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("EXT") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_81(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") != "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_82(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "XXjson3XX" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_83(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "JSON3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_84(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(None):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_85(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(None)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_86(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = None
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_87(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") and k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_88(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith(None) or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_89(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("XX-origXX") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_90(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-ORIG") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_91(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k != "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_92(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "XXorigXX"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_93(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "ORIG"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_94(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=None
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_95(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: None
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_96(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 1 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_97(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) and (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_98(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video or "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_99(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "XXptXX" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_100(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "PT" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_101(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" not in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_102(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video or "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_103(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_104(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "XXenXX" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_105(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "EN" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_106(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" not in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_107(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 2
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_108(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = None
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_109(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get(None)
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_110(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("XXurlXX")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_111(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("URL")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_112(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" or self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_113(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get(None) == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_114(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("XXextXX") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_115(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("EXT") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_116(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") != "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_117(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "XXjson3XX" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_118(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "JSON3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_119(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(None):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_120(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(None)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_121(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang not in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_122(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = None
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_123(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get(None)
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_124(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("XXurlXX")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_125(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("URL")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_126(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" or self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_127(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get(None) == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_128(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("XXextXX") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_129(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("EXT") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_130(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") != "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_131(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "XXjson3XX" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_132(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "JSON3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_133(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(None):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_134(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(None)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_135(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = None
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_136(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get(None)
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_137(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("XXurlXX")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_138(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("URL")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_139(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" or self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_140(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get(None) == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_141(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("XXextXX") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_142(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("EXT") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_143(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") != "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_144(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "XXjson3XX" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_145(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "JSON3" and self._is_native_subtitle_url(url):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_146(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(None):
                    return str(url)

        return None

    def xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_147(self, info: dict[str, Any]) -> str | None:
        """Search for native spoken subtitle URL, strictly rejecting tlang= translations."""
        subtitles: dict[str, list[dict[str, Any]]] = info.get("subtitles") or {}
        auto_captions: dict[str, list[dict[str, Any]]] = info.get("automatic_captions") or {}

        video_lang = str(info.get("language") or "").lower()
        is_pt_video = video_lang.startswith("pt")

        if is_pt_video:
            ordered_langs = ["pt-orig", "pt-BR", "pt", "pt-PT", "en-orig", "en", "en-US"]
        else:
            ordered_langs = ["en-orig", "en", "en-US", "pt-orig", "pt-BR", "pt", "pt-PT"]

        # 1. Search in manual subtitles in preferred language order (ext: json3)
        for lang in ordered_langs:
            if lang in subtitles:
                for fmt in subtitles[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 2. Search manual subtitles in any language
        for formats in subtitles.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 3. Automatic captions: Prioritize explicit original tracks (*-orig)
        orig_keys = [k for k in auto_captions if k.endswith("-orig") or k == "orig"]
        orig_keys.sort(
            key=lambda k: 0 if (is_pt_video and "pt" in k) or (not is_pt_video and "en" in k) else 1
        )
        for lang in orig_keys:
            for fmt in auto_captions[lang]:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(url)

        # 4. Automatic captions matching preferred language order without tlang
        for lang in ordered_langs:
            if lang in auto_captions:
                for fmt in auto_captions[lang]:
                    url = fmt.get("url")
                    if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                        return str(url)

        # 5. Any automatic caption track without tlang
        for formats in auto_captions.values():
            for fmt in formats:
                url = fmt.get("url")
                if fmt.get("ext") == "json3" and self._is_native_subtitle_url(url):
                    return str(None)

        return None

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut)
    def _fetch_url_content(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_orig(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_1(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = None
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_2(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            None,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_3(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=None,
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_4(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_5(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_6(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(None, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_7(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=None) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_8(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_9(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, ) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_10(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = None
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_11(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode(None)
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_12(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("XXutf-8XX")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_13(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("UTF-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_14(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code != 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_15(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 430:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_16(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    None
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_17(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(None) from exc
        except Exception as exc:
            raise IngestionNetworkError(f"Network error fetching subtitles: {exc}") from exc

    def xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_18(self, url: str) -> str:
        """Fetch remote subtitle payload with dynamic browser headers."""
        req = urllib.request.Request(
            url,
            headers=self._header_generator.get_random_headers(),
        )
        try:
            with urllib.request.urlopen(req, timeout=self.request_timeout) as resp:
                raw_bytes: bytes = resp.read()
                return raw_bytes.decode("utf-8")
        except urllib.error.HTTPError as exc:
            if exc.code == 429:
                raise RateLimitExceededError(
                    f"HTTP 429 Too Many Requests while fetching subtitles: {exc}"
                ) from exc
            raise IngestionNetworkError(f"HTTP error fetching subtitles: {exc}") from exc
        except Exception as exc:
            raise IngestionNetworkError(None) from exc

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut)
    def _reconstruct_json3_paragraphs(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_orig(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_1(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = None
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_2(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get(None, [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_3(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", None)
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_4(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get([])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_5(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", )
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_6(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("XXeventsXX", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_7(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("EVENTS", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_8(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = None
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_9(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = None

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_10(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event or not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_11(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "XXsegsXX" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_12(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "SEGS" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_13(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" not in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_14(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_15(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get(None):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_16(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("XXaAppendXX"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_17(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aappend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_18(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("AAPPEND"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_19(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = None
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_20(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(None).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_21(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "XXXX".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_22(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(None) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_23(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get(None, "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_24(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", None)) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_25(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_26(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", )) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_27(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("XXutf8XX", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_28(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("UTF8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_29(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "XXXX")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_30(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["XXsegsXX"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_31(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["SEGS"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_32(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text or seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_33(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text == "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_34(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "XX\nXX":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_35(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(None)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_36(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 and seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_37(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) > 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_38(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 5 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_39(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith(None):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_40(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith(("XX.XX", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_41(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "XX!XX", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_42(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "XX?XX")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_43(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(None)
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_44(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(None))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_45(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append("XX XX".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_46(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = None

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_47(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(None)

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_48(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(None))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_49(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append("XX XX".join(current_sentences))

        return "\n\n".join(paragraphs).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_50(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "\n\n".join(None).strip()

    def xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_51(self, data: dict[str, Any]) -> str:
        """Reconstruct JSON3 subtitle event segments into continuous prose paragraphs."""
        events: list[dict[str, Any]] = data.get("events", [])
        paragraphs: list[str] = []
        current_sentences: list[str] = []

        for event in events:
            if "segs" in event and not event.get("aAppend"):
                seg_text = "".join(str(s.get("utf8", "")) for s in event["segs"]).strip()
                if seg_text and seg_text != "\n":
                    current_sentences.append(seg_text)
                    # Group into paragraphs roughly every 4 sentences or punctuation pauses
                    if len(current_sentences) >= 4 or seg_text.endswith((".", "!", "?")):
                        paragraphs.append(" ".join(current_sentences))
                        current_sentences = []

        if current_sentences:
            paragraphs.append(" ".join(current_sentences))

        return "XX\n\nXX".join(paragraphs).strip()

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut)
    def _handle_yt_dlp_error(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_orig(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_1(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = None
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_2(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(None)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_3(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg and "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_4(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "XX429XX" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_5(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" not in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_6(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "XXToo Many RequestsXX" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_7(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "too many requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_8(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "TOO MANY REQUESTS" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_9(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" not in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_10(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(None) from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_11(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            None
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_12(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term not in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_13(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("XXConnection resetXX", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_14(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_15(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("CONNECTION RESET", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_16(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "XXtimed outXX", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_17(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "TIMED OUT", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_18(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "XXName or service not knownXX", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_19(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_20(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "NAME OR SERVICE NOT KNOWN", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_21(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "XXNetworkXX")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_22(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_23(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "NETWORK")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_24(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(None) from exc
        raise CresmoInfrastructureError(f"Media extraction infrastructure error: {msg}") from exc

    def xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_25(self, exc: Exception) -> None:
        """Translate yt-dlp internal exceptions to canonical domain exception hierarchy."""
        msg = str(exc)
        if "429" in msg or "Too Many Requests" in msg:
            raise RateLimitExceededError(f"Upstream rate limit exceeded (HTTP 429): {msg}") from exc
        if any(
            term in msg
            for term in ("Connection reset", "timed out", "Name or service not known", "Network")
        ):
            raise IngestionNetworkError(f"Ingestion network connectivity error: {msg}") from exc
        raise CresmoInfrastructureError(None) from exc

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut)
    def _transcribe_audio_fallback(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_orig(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_1(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = None
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_2(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "XXUnknown_ChannelXX"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_3(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "unknown_channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_4(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "UNKNOWN_CHANNEL"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_5(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = None

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_6(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                None
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_7(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(None)
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_8(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") and "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_9(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") and info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_10(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get(None) or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_11(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("XXchannelXX") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_12(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("CHANNEL") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_13(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get(None) or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_14(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("XXuploaderXX") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_15(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("UPLOADER") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_16(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "XXUnknown_ChannelXX")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_17(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "unknown_channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_18(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "UNKNOWN_CHANNEL")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_19(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix=None) as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_20(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="XXcresmo_scratch_XX") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_21(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="CRESMO_SCRATCH_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_22(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = None
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_23(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(None)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_24(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = None

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_25(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "XXformatXX": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_26(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "FORMAT": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_27(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "XXbestaudio/bestXX",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_28(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "BESTAUDIO/BEST",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_29(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "XXouttmplXX": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_30(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "OUTTMPL": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_31(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(None),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_32(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path * "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_33(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "XX%(id)s.%(ext)sXX"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_34(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(ID)S.%(EXT)S"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_35(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "XXquietXX": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_36(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "QUIET": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_37(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": False,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_38(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "XXno_warningsXX": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_39(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "NO_WARNINGS": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_40(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": False,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_41(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "XXhttp_headersXX": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_42(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "HTTP_HEADERS": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_43(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(None) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_44(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download(None)
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_45(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(None)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_46(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = None
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_47(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(None)
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_48(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob(None))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_49(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("XX*.*XX"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_50(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_51(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    None
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_52(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = None

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_53(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[1]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_54(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = None
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_55(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir * channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_56(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=None, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_57(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=None)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_58(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_59(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, )
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_60(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=False, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_61(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=False)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_62(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = None
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_63(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir * audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_64(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(None, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_65(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, None)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_66(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_67(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, )

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_68(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = None
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_69(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(None)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_70(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = None
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_71(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(None)
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_72(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(None))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_73(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = None
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_74(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(None).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_75(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get(None, "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_76(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", None)).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_77(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_78(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", )).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_79(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("XXtextXX", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_80(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("TEXT", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_81(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "XXXX")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(f"Whisper transcription failed: {exc}") from exc

        return body, channel_name

    def xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_82(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str,
        keep_audio: bool,
        info: dict[str, Any] | None = None,
    ) -> tuple[str, str]:
        """Download audio to ephemeral scratch dir, transcribe via Whisper, and clean up."""
        import whisper

        channel_name = "Unknown_Channel"
        if info:
            channel_name = self._sanitize_fs_name(
                str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
            )

        with tempfile.TemporaryDirectory(prefix="cresmo_scratch_") as scratch_str:
            scratch_path = Path(scratch_str)
            ydl_opts: dict[str, Any] = {
                "format": "bestaudio/best",
                "outtmpl": str(scratch_path / "%(id)s.%(ext)s"),
                "quiet": True,
                "no_warnings": True,
                "http_headers": self._header_generator.get_random_headers(),
            }

            try:
                with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                    ydl.download([video_url])
            except Exception as exc:  # noqa: BLE001
                self._handle_yt_dlp_error(exc)

            downloaded_files = list(scratch_path.glob("*.*"))
            if not downloaded_files:
                raise IngestionNetworkError(
                    f"yt-dlp completed audio download but no file was found in {scratch_path}"
                )

            audio_file = downloaded_files[0]

            if keep_audio:
                dest_dir = output_dir / channel_name
                dest_dir.mkdir(parents=True, exist_ok=True)
                dest_audio = dest_dir / audio_file.name
                shutil.copy2(audio_file, dest_audio)

            try:
                with self._whisper_semaphore:
                    model = whisper.load_model(whisper_model)
                    transcription_result = model.transcribe(str(audio_file))
                    body = str(transcription_result.get("text", "")).strip()
            except Exception as exc:
                raise CresmoInfrastructureError(None) from exc

        return body, channel_name

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut)
    def ingest_single_video(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_orig(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_1(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "XXbaseXX",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_2(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "BASE",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_3(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = True,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_4(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = None

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_5(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(None)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_6(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = None

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_7(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "XXskip_downloadXX": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_8(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "SKIP_DOWNLOAD": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_9(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": False,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_10(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "XXquietXX": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_11(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "QUIET": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_12(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": False,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_13(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "XXno_warningsXX": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_14(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "NO_WARNINGS": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_15(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": False,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_16(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "XXhttp_headersXX": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_17(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "HTTP_HEADERS": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_18(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(None) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_19(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = None
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_20(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(None, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_21(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=None)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_22(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_23(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, )
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_24(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=True)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_25(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(None)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_26(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_27(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(None)

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_28(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = None
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_29(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(None)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_30(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") and video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_31(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get(None) or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_32(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("XXidXX") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_33(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("ID") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_34(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = None

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_35(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            None
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_36(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(None)
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_37(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") and "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_38(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") and info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_39(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get(None) or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_40(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("XXchannelXX") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_41(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("CHANNEL") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_42(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get(None) or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_43(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("XXuploaderXX") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_44(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("UPLOADER") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_45(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "XXUnknown_ChannelXX")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_46(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "unknown_channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_47(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "UNKNOWN_CHANNEL")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_48(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = None
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_49(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(None)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_50(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = None

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_51(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = "XXXX"

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_52(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = None
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_53(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(None)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_54(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = None
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_55(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(None)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_56(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = None
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_57(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(None)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_58(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = None

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_59(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = "XXXX"

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_60(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_61(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = None

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_62(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=None,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_63(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=None,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_64(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=None,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_65(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=None,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_66(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=None,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_67(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_68(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_69(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_70(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_71(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_72(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_73(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                None
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_74(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = None
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_75(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(None)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_76(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = None
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_77(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(None)
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_78(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") and "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_79(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get(None) or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_80(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("XXupload_dateXX") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_81(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("UPLOAD_DATE") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_82(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "XXXX")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_83(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = ""
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_84(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 or date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_85(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) != 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_86(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 9 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_87(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = None
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_88(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=None).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_89(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(None, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_90(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, None).replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_91(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime("%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_92(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, ).replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_93(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "XX%Y%m%dXX").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_94(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_95(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%M%D").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_96(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=None,
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_97(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=None,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_98(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=None,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_99(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=None,
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_100(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=None,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_101(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=None,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_102(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=None,
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_103(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=None,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_104(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=None,
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_105(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_106(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_107(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_108(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_109(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_110(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_111(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_112(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_113(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_114(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(None),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_115(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(None),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_116(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") and ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_117(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get(None) or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_118(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("XXtitleXX") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_119(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("TITLE") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_120(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or "XXXX"),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_121(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(None),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_122(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") and "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_123(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get(None) or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_124(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("XXchannel_idXX") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_125(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("CHANNEL_ID") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_126(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "XXunknown_channelXX"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_127(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "UNKNOWN_CHANNEL"),
            channel_category=category,
            video_description=str(info.get("description") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_128(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(None),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_129(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") and ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_130(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get(None) or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_131(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("XXdescriptionXX") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_132(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("DESCRIPTION") or ""),
        )

    def xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_133(
        self,
        video_url: str,
        output_dir: Path,
        whisper_model: str = "base",
        keep_audio: bool = False,
    ) -> RawTranscript | None:
        """Ingest single video into RawTranscript domain aggregate.

        Prioritizes native spoken subtitles. If unavailable, falls back to local
        Whisper transcription with RAII temporary scratch storage.
        """
        video_id = self._extract_video_id(video_url)

        ydl_opts: dict[str, Any] = {
            "skip_download": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return None

        if not info:
            raise IngestionNetworkError(f"yt-dlp returned no metadata for URL '{video_url}'")

        actual_video_id = str(info.get("id") or video_id)
        channel_name = self._sanitize_fs_name(
            str(info.get("channel") or info.get("uploader") or "Unknown_Channel")
        )

        # Check for native subtitles (rejecting tlang=)
        sub_url = self._find_native_subtitle_url(info)
        body = ""

        if sub_url:
            try:
                raw_sub = self._fetch_url_content(sub_url)
                sub_data = json.loads(raw_sub)
                body = self._reconstruct_json3_paragraphs(sub_data)
            except RateLimitExceededError:
                raise
            except Exception:  # noqa: BLE001
                body = ""

        # If subtitles were absent, malformed, or empty, trigger Whisper fallback
        if not body:
            body, channel_name = self._transcribe_audio_fallback(
                video_url=video_url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
                info=info,
            )

        if not body:
            raise DomainValidationError(
                f"Extracted transcript body is empty for video ID '{actual_video_id}'."
            )

        category, _ = classify_channel(channel_name)
        date_str = str(info.get("upload_date") or "")
        upload_date = None
        if len(date_str) == 8 and date_str.isdigit():
            try:
                upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
            except ValueError:
                pass

        return RawTranscript(
            content_id=ContentId(actual_video_id),
            channel_name=channel_name,
            body=body,
            title=str(info.get("title") or ""),
            source_url=video_url,
            upload_date=upload_date,
            channel_id=str(info.get("channel_id") or "unknown_channel"),
            channel_category=category,
            video_description=str(info.get("description") or "XXXX"),
        )

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut)
    def discover_channel_feed(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_orig(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_1(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = None

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_2(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "XXextract_flatXX": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_3(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "EXTRACT_FLAT": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_4(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": False,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_5(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "XXplaylistendXX": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_6(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "PLAYLISTEND": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_7(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "XXquietXX": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_8(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "QUIET": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_9(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": False,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_10(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "XXno_warningsXX": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_11(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "NO_WARNINGS": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_12(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": False,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_13(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "XXhttp_headersXX": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_14(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "HTTP_HEADERS": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_15(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(None) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_16(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = None
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_17(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(None, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_18(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=None)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_19(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_20(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, )
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_21(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=True)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_22(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(None)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_23(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_24(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = None
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_25(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") and []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_26(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get(None) or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_27(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("XXentriesXX") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_28(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("ENTRIES") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_29(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = None
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_30(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(None)
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_31(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") and "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_32(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") and info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_33(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get(None) or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_34(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("XXtitleXX") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_35(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("TITLE") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_36(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get(None) or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_37(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("XXuploaderXX") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_38(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("UPLOADER") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_39(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "XXUnknown_ChannelXX")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_40(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "unknown_channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_41(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "UNKNOWN_CHANNEL")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_42(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = None

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_43(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry and not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_44(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_45(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_46(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                break

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_47(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = None
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_48(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(None).strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_49(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") and "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_50(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get(None) or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_51(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("XXidXX") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_52(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("ID") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_53(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "XXXX").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_54(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = None
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_55(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(None).strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_56(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") and "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_57(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get(None) or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_58(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("XXtitleXX") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_59(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("TITLE") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_60(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "XXXX").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_61(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid and not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_62(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_63(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_64(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                break

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_65(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = None
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_66(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(None).strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_67(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") and f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_68(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get(None) or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_69(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("XXurlXX") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_70(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("URL") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_71(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = None

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_72(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(None).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_73(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") and channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_74(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") and entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_75(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get(None) or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_76(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("XXchannelXX") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_77(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("CHANNEL") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_78(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get(None) or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_79(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("XXuploaderXX") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_80(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("UPLOADER") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_81(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = None
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_82(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get(None)
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_83(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("XXupload_dateXX")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_84(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("UPLOAD_DATE")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_85(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = None
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_86(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(None)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_87(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) or len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_88(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date or isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_89(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) != 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_90(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 9:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_91(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = None
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_92(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=None)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_93(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(None, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_94(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, None).replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_95(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime("%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_96(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, ).replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_97(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "XX%Y%m%dXX").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_98(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_99(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%M%D").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_100(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                None
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_101(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=None,
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_102(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=None,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_103(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=None,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_104(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=None,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_105(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=None,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_106(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_107(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_108(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_109(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    channel_name=channel,
                )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_110(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(vid),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    )
            )

        return items

    def xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_111(
        self,
        query: ChannelFeedQuery,
    ) -> list[DiscoveredMediaItem]:
        """Discover media items from a YouTube channel or playlist feed."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "playlistend": query.max_videos,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(query.channel_url, download=False)
        except Exception as exc:  # noqa: BLE001
            self._handle_yt_dlp_error(exc)
            return []

        if not info:
            return []

        entries = info.get("entries") or []
        channel_title = str(info.get("title") or info.get("uploader") or "Unknown_Channel")
        items: list[DiscoveredMediaItem] = []

        for entry in entries:
            if not entry or not isinstance(entry, dict):
                continue

            vid = str(entry.get("id") or "").strip()
            title = str(entry.get("title") or "").strip()
            if not vid or not title:
                continue

            url = str(entry.get("url") or f"https://www.youtube.com/watch?v={vid}").strip()
            channel = str(entry.get("channel") or entry.get("uploader") or channel_title).strip()

            raw_date = entry.get("upload_date")
            pub_date = datetime.now(UTC)
            if raw_date and isinstance(raw_date, str) and len(raw_date) == 8:
                try:
                    pub_date = datetime.strptime(raw_date, "%Y%m%d").replace(tzinfo=UTC)
                except ValueError:
                    pass

            items.append(
                DiscoveredMediaItem(
                    content_id=ContentId(None),
                    title=title,
                    published_at=pub_date,
                    media_url=url,
                    channel_name=channel,
                )
            )

        return items

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut)
    def ingest_channels_and_playlists(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_orig(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_1(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 731,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_2(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "XXbaseXX",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_3(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "BASE",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_4(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = True,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_5(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 5,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_6(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = None
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_7(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = None
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_8(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=None,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_9(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=None,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_10(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=None,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_11(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=None,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_12(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_13(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_14(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_15(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                )
            if raw:
                transcripts.append(raw)
        return transcripts

    def xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_16(
        self,
        playlist_urls: list[str],
        output_dir: Path,
        days_lookback: int = 730,
        whisper_model: str = "base",
        keep_audio: bool = False,
        max_workers: int = 4,
    ) -> list[RawTranscript]:
        """Ingest videos from playlists sequentially or in parallel batches."""
        transcripts: list[RawTranscript] = []
        for url in playlist_urls:
            raw = self.ingest_single_video(
                video_url=url,
                output_dir=output_dir,
                whisper_model=whisper_model,
                keep_audio=keep_audio,
            )
            if raw:
                transcripts.append(None)
        return transcripts

    @_mutmut_mutated(mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut)
    def extract_channel_url_from_video(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_orig(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_1(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = None

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_2(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "XXextract_flatXX": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_3(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "EXTRACT_FLAT": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_4(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": False,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_5(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "XXquietXX": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_6(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "QUIET": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_7(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": False,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_8(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "XXno_warningsXX": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_9(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "NO_WARNINGS": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_10(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": False,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_11(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "XXhttp_headersXX": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_12(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "HTTP_HEADERS": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_13(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(None) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_14(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = None
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_15(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(None, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_16(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=None)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_17(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_18(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, )
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_19(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=True)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_20(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info and not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_21(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_22(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_23(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = None
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_24(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") and info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_25(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get(None) or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_26(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("XXchannel_urlXX") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_27(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("CHANNEL_URL") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_28(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get(None)
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_29(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("XXuploader_urlXX")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_30(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("UPLOADER_URL")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_31(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(None).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_32(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = None
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_33(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") and info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_34(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get(None) or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_35(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("XXchannel_idXX") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_36(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("CHANNEL_ID") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_37(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get(None)
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_38(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("XXuploader_idXX")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_39(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("UPLOADER_ID")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(channel_id).strip()}"

        return None

    def xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_40(
        self,
        video_url: str,
    ) -> str | None:
        """Resolve YouTube channel URL or feed identifier from a video URL."""
        ydl_opts: dict[str, Any] = {
            "extract_flat": True,
            "quiet": True,
            "no_warnings": True,
            "http_headers": self._header_generator.get_random_headers(),
        }

        try:
            with yt_dlp.YoutubeDL(ydl_opts) as ydl:
                info = ydl.extract_info(video_url, download=False)
        except Exception:  # noqa: BLE001
            return None

        if not info or not isinstance(info, dict):
            return None

        channel_url = info.get("channel_url") or info.get("uploader_url")
        if channel_url:
            return str(channel_url).strip()

        channel_id = info.get("channel_id") or info.get("uploader_id")
        if channel_id:
            return f"https://www.youtube.com/channel/{str(None).strip()}"

        return None

mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ__init____mutmut['xǁNativeMediaIngestionAdapterǁ__init____mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ__init____mutmut_12 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut['xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_extract_video_id__mutmut_5 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut['xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_sanitize_fs_name__mutmut_10 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_is_native_subtitle_url__mutmut_5 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_40 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_41'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_41 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_42'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_42 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_43'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_43 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_44'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_44 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_45'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_45 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_46'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_46 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_47'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_47 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_48'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_48 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_49'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_49 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_50'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_50 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_51'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_51 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_52'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_52 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_53'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_53 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_54'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_54 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_55'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_55 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_56'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_56 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_57'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_57 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_58'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_58 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_59'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_59 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_60'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_60 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_61'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_61 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_62'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_62 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_63'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_63 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_64'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_64 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_65'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_65 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_66'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_66 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_67'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_67 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_68'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_68 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_69'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_69 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_70'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_70 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_71'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_71 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_72'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_72 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_73'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_73 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_74'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_74 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_75'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_75 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_76'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_76 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_77'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_77 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_78'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_78 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_79'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_79 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_80'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_80 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_81'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_81 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_82'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_82 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_83'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_83 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_84'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_84 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_85'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_85 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_86'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_86 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_87'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_87 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_88'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_88 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_89'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_89 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_90'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_90 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_91'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_91 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_92'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_92 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_93'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_93 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_94'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_94 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_95'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_95 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_96'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_96 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_97'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_97 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_98'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_98 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_99'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_99 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_100'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_100 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_101'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_101 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_102'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_102 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_103'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_103 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_104'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_104 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_105'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_105 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_106'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_106 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_107'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_107 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_108'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_108 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_109'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_109 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_110'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_110 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_111'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_111 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_112'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_112 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_113'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_113 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_114'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_114 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_115'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_115 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_116'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_116 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_117'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_117 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_118'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_118 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_119'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_119 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_120'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_120 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_121'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_121 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_122'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_122 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_123'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_123 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_124'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_124 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_125'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_125 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_126'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_126 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_127'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_127 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_128'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_128 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_129'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_129 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_130'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_130 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_131'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_131 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_132'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_132 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_133'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_133 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_134'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_134 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_135'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_135 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_136'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_136 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_137'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_137 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_138'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_138 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_139'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_139 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_140'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_140 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_141'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_141 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_142'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_142 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_143'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_143 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_144'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_144 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_145'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_145 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_146'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_146 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut['xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_147'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_find_native_subtitle_url__mutmut_147 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut['xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_fetch_url_content__mutmut_18 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_40 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_41'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_41 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_42'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_42 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_43'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_43 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_44'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_44 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_45'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_45 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_46'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_46 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_47'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_47 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_48'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_48 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_49'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_49 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_50'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_50 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut['xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_51'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_reconstruct_json3_paragraphs__mutmut_51 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut['xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_handle_yt_dlp_error__mutmut_25 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_40 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_41'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_41 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_42'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_42 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_43'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_43 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_44'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_44 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_45'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_45 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_46'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_46 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_47'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_47 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_48'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_48 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_49'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_49 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_50'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_50 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_51'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_51 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_52'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_52 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_53'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_53 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_54'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_54 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_55'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_55 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_56'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_56 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_57'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_57 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_58'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_58 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_59'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_59 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_60'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_60 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_61'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_61 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_62'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_62 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_63'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_63 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_64'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_64 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_65'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_65 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_66'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_66 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_67'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_67 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_68'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_68 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_69'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_69 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_70'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_70 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_71'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_71 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_72'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_72 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_73'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_73 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_74'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_74 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_75'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_75 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_76'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_76 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_77'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_77 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_78'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_78 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_79'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_79 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_80'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_80 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_81'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_81 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut['xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_82'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁ_transcribe_audio_fallback__mutmut_82 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_40 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_41'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_41 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_42'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_42 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_43'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_43 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_44'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_44 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_45'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_45 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_46'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_46 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_47'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_47 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_48'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_48 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_49'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_49 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_50'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_50 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_51'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_51 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_52'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_52 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_53'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_53 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_54'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_54 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_55'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_55 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_56'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_56 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_57'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_57 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_58'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_58 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_59'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_59 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_60'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_60 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_61'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_61 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_62'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_62 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_63'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_63 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_64'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_64 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_65'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_65 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_66'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_66 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_67'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_67 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_68'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_68 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_69'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_69 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_70'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_70 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_71'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_71 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_72'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_72 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_73'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_73 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_74'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_74 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_75'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_75 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_76'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_76 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_77'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_77 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_78'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_78 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_79'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_79 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_80'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_80 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_81'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_81 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_82'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_82 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_83'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_83 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_84'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_84 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_85'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_85 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_86'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_86 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_87'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_87 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_88'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_88 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_89'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_89 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_90'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_90 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_91'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_91 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_92'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_92 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_93'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_93 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_94'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_94 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_95'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_95 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_96'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_96 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_97'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_97 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_98'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_98 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_99'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_99 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_100'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_100 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_101'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_101 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_102'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_102 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_103'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_103 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_104'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_104 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_105'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_105 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_106'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_106 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_107'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_107 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_108'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_108 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_109'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_109 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_110'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_110 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_111'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_111 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_112'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_112 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_113'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_113 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_114'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_114 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_115'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_115 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_116'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_116 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_117'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_117 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_118'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_118 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_119'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_119 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_120'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_120 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_121'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_121 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_122'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_122 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_123'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_123 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_124'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_124 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_125'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_125 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_126'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_126 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_127'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_127 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_128'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_128 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_129'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_129 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_130'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_130 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_131'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_131 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_132'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_132 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut['xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_133'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_single_video__mutmut_133 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_40 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_41'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_41 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_42'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_42 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_43'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_43 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_44'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_44 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_45'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_45 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_46'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_46 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_47'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_47 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_48'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_48 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_49'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_49 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_50'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_50 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_51'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_51 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_52'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_52 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_53'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_53 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_54'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_54 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_55'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_55 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_56'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_56 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_57'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_57 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_58'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_58 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_59'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_59 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_60'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_60 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_61'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_61 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_62'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_62 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_63'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_63 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_64'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_64 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_65'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_65 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_66'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_66 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_67'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_67 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_68'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_68 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_69'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_69 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_70'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_70 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_71'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_71 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_72'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_72 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_73'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_73 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_74'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_74 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_75'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_75 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_76'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_76 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_77'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_77 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_78'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_78 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_79'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_79 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_80'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_80 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_81'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_81 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_82'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_82 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_83'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_83 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_84'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_84 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_85'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_85 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_86'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_86 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_87'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_87 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_88'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_88 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_89'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_89 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_90'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_90 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_91'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_91 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_92'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_92 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_93'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_93 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_94'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_94 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_95'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_95 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_96'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_96 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_97'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_97 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_98'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_98 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_99'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_99 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_100'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_100 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_101'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_101 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_102'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_102 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_103'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_103 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_104'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_104 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_105'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_105 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_106'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_106 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_107'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_107 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_108'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_108 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_109'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_109 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_110'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_110 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut['xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_111'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁdiscover_channel_feed__mutmut_111 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut['xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁingest_channels_and_playlists__mutmut_16 # type: ignore # mutmut generated

mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['_mutmut_orig'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_orig # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_1'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_1 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_2'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_2 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_3'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_3 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_4'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_4 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_5'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_5 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_6'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_6 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_7'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_7 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_8'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_8 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_9'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_9 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_10'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_10 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_11'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_11 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_12'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_12 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_13'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_13 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_14'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_14 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_15'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_15 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_16'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_16 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_17'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_17 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_18'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_18 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_19'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_19 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_20'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_20 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_21'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_21 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_22'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_22 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_23'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_23 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_24'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_24 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_25'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_25 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_26'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_26 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_27'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_27 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_28'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_28 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_29'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_29 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_30'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_30 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_31'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_31 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_32'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_32 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_33'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_33 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_34'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_34 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_35'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_35 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_36'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_36 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_37'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_37 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_38'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_38 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_39'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_39 # type: ignore # mutmut generated
mutants_xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut['xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_40'] = NativeMediaIngestionAdapter.xǁNativeMediaIngestionAdapterǁextract_channel_url_from_video__mutmut_40 # type: ignore # mutmut generated
