"""Obsidian Second Brain Vault Repository Adapter.

Implements VaultRepositoryPort using atomic filesystem writes, YAML frontmatter serialization,
and tiered index management.
"""

from __future__ import annotations

import json
import os
import re
from datetime import UTC, datetime
from pathlib import Path
from typing import Any
from uuid import uuid4

import yaml

from cresmo.application.ports import VaultRepositoryPort
from cresmo.domain.entities import (
    AtomicNote,
    EnrichedCompendium,
    MapOfContent,
    RawTranscript,
)
from cresmo.domain.exceptions import NoteTypologyError
from cresmo.domain.value_objects import (
    CausalMatrix,
    ContentId,
    CrossContextRelations,
    NoteTitle,
    NoteType,
)

_ILLEGAL_FILENAME_CHARS = re.compile(r'[\\/*?:"<>|%]')
_FRONTMATTER_PATTERN = re.compile(r"^---\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$")


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_sanitize_filename__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_sanitize_filename__mutmut)
def sanitize_filename(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub("_", name.strip())


def x_sanitize_filename__mutmut_orig(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub("_", name.strip())


def x_sanitize_filename__mutmut_1(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub(None, name.strip())


def x_sanitize_filename__mutmut_2(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub("_", None)


def x_sanitize_filename__mutmut_3(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub(name.strip())


def x_sanitize_filename__mutmut_4(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub("_", )


def x_sanitize_filename__mutmut_5(name: str) -> str:
    """Sanitize note title for safe filesystem path."""
    return _ILLEGAL_FILENAME_CHARS.sub("XX_XX", name.strip())

mutants_x_sanitize_filename__mutmut['_mutmut_orig'] = x_sanitize_filename__mutmut_orig # type: ignore # mutmut generated
mutants_x_sanitize_filename__mutmut['x_sanitize_filename__mutmut_1'] = x_sanitize_filename__mutmut_1 # type: ignore # mutmut generated
mutants_x_sanitize_filename__mutmut['x_sanitize_filename__mutmut_2'] = x_sanitize_filename__mutmut_2 # type: ignore # mutmut generated
mutants_x_sanitize_filename__mutmut['x_sanitize_filename__mutmut_3'] = x_sanitize_filename__mutmut_3 # type: ignore # mutmut generated
mutants_x_sanitize_filename__mutmut['x_sanitize_filename__mutmut_4'] = x_sanitize_filename__mutmut_4 # type: ignore # mutmut generated
mutants_x_sanitize_filename__mutmut['x_sanitize_filename__mutmut_5'] = x_sanitize_filename__mutmut_5 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁ_atomic_write__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁget_raw_transcript__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁ_get_note_path__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁsave_atomic_note__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁupdate_index_entry__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁsave_map_of_content__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁremove_index_entry__mutmut: MutantDict = {}  # type: ignore
mutants_xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut: MutantDict = {}  # type: ignore


class ObsidianVaultAdapter(VaultRepositoryPort):
    """Filesystem-backed Obsidian Second Brain vault adapter."""

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁ__init____mutmut)
    def __init__(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_orig(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_1(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = None
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_2(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(None).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_3(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = None
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_4(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(None).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_5(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = None

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_6(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(None).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_7(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = None
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_8(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir * "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_9(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "XXMOCsXX"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_10(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "mocs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_11(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCS"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_12(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = None

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_13(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir * "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_14(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "XX_index.jsonXX"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_15(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_INDEX.JSON"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_16(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=None, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_17(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=None)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_18(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_19(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, )
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_20(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=False, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_21(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=False)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_22(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=None, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_23(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=None)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_24(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_25(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, )
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_26(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=False, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_27(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=False)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_28(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=None, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_29(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=None)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_30(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_31(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, )
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_32(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=False, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_33(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=False)
        self.mocs_dir.mkdir(parents=True, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_34(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=None, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_35(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=None)

    def xǁObsidianVaultAdapterǁ__init____mutmut_36(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_37(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, )

    def xǁObsidianVaultAdapterǁ__init____mutmut_38(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=False, exist_ok=True)

    def xǁObsidianVaultAdapterǁ__init____mutmut_39(
        self,
        vault_dir: Path,
        raw_dir: Path,
        enriched_dir: Path,
    ) -> None:
        self.vault_dir = Path(vault_dir).resolve()
        self.raw_dir = Path(raw_dir).resolve()
        self.enriched_dir = Path(enriched_dir).resolve()

        self.mocs_dir = self.vault_dir / "MOCs"
        self.index_path = self.vault_dir / "_index.json"

        # Ensure base directory tree exists
        self.raw_dir.mkdir(parents=True, exist_ok=True)
        self.enriched_dir.mkdir(parents=True, exist_ok=True)
        self.vault_dir.mkdir(parents=True, exist_ok=True)
        self.mocs_dir.mkdir(parents=True, exist_ok=False)

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁ_atomic_write__mutmut)
    def _atomic_write(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_orig(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_1(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=None, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_2(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=None)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_3(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_4(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, )
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_5(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=False, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_6(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=False)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_7(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = None
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_8(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent * f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_9(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(None, encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_10(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding=None)
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_11(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(encoding="utf-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_12(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, )
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_13(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="XXutf-8XX")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_14(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="UTF-8")
        os.replace(temp_file, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_15(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(None, target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_16(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, None)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_17(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(target_path)

    def xǁObsidianVaultAdapterǁ_atomic_write__mutmut_18(self, target_path: Path, content: str) -> None:
        """Atomically write text content using temp-file replace pattern."""
        target_path.parent.mkdir(parents=True, exist_ok=True)
        temp_file = target_path.parent / f"{target_path.name}.tmp.{uuid4().hex}"
        temp_file.write_text(content, encoding="utf-8")
        os.replace(temp_file, )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut)
    def save_raw_transcript(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_orig(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_1(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = None
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_2(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir * sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_3(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(None)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_4(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = None

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_5(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir * f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_6(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = None
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_7(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description and ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_8(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or "XXXX"
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_9(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = None
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_10(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join(None)
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_11(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "XX\nXX".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_12(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " - l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_13(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("XX  XX" + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_14(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = None
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_15(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime(None) if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_16(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("XX%Y%m%dXX") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_17(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_18(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%M%D") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_19(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else "XXXX"
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_20(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = None
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_21(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace(None, '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_22(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', None)
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_23(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_24(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', )
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_25(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title and transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_26(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('XX"XX', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_27(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', 'XX\\"XX')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_28(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = None
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_29(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace(None, '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_30(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', None)
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_31(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_32(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', )
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_33(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('XX"XX', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_34(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', 'XX\\"XX')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_35(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = None
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_36(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace(None, '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_37(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', None)
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_38(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_39(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', )
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_40(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category and "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_41(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "XXuncategorizedXX").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_42(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "UNCATEGORIZED").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_43(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('XX"XX', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_44(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', 'XX\\"XX')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_45(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = None

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_46(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id and "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_47(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "XXunknown_channelXX"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_48(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "UNKNOWN_CHANNEL"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_49(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = None
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_50(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = None
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_51(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(None, content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_52(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, None)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_53(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(content)

    def xǁObsidianVaultAdapterǁsave_raw_transcript__mutmut_54(self, transcript: RawTranscript) -> None:
        """Persist raw transcript with playground-compatible YAML frontmatter."""
        channel_dir = self.raw_dir / sanitize_filename(transcript.channel_name)
        file_path = channel_dir / f"{transcript.content_id.value}.md"

        desc = transcript.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        date_str = transcript.upload_date.strftime("%Y%m%d") if transcript.upload_date else ""
        escaped_title = (transcript.title or transcript.content_id.value).replace('"', '\\"')
        escaped_channel = transcript.channel_name.replace('"', '\\"')
        escaped_category = (transcript.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = transcript.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {transcript.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {transcript.source_url}\n"
            f"video_date: {date_str}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        content = f"{yaml_header}\n\n{transcript.body}"
        self._atomic_write(file_path, )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁget_raw_transcript__mutmut)
    def get_raw_transcript(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_orig(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_1(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = None
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_2(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = None
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_3(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(None)
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_4(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(None))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_5(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_6(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = None
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_7(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[1]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_8(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = None
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_9(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding=None)
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_10(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="XXutf-8XX")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_11(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="UTF-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_12(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = None
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_13(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(None)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_14(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_15(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=None,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_16(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=None,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_17(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=None,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_18(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_19(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_20(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_21(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = None
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_22(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = None
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_23(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) and {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_24(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(None) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_25(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = None
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_26(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(None)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_27(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") and file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_28(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") and meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_29(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get(None) or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_30(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("XXchannel_nameXX") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_31(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("CHANNEL_NAME") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_32(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get(None) or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_33(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("XXchannelXX") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_34(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("CHANNEL") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_35(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = None
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_36(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(None)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_37(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") and "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_38(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") and meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_39(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get(None) or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_40(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("XXvideo_titleXX") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_41(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("VIDEO_TITLE") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_42(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get(None) or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_43(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("XXtitleXX") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_44(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("TITLE") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_45(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "XXXX")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_46(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = None
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_47(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(None)
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_48(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") and "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_49(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get(None) or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_50(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("XXchannel_idXX") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_51(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("CHANNEL_ID") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_52(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "XXXX")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_53(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = None
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_54(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(None)
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_55(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") and "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_56(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") and meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_57(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get(None) or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_58(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("XXchannel_categoryXX") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_59(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("CHANNEL_CATEGORY") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_60(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get(None) or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_61(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("XXdomainXX") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_62(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("DOMAIN") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_63(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "XXXX")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_64(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = None
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_65(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(None)
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_66(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") and "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_67(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") and meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_68(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get(None) or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_69(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("XXurlXX") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_70(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("URL") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_71(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get(None) or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_72(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("XXsource_urlXX") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_73(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("SOURCE_URL") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_74(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "XXXX")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_75(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = None
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_76(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") and meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_77(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get(None) or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_78(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("XXvideo_dateXX") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_79(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("VIDEO_DATE") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_80(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get(None)
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_81(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("XXupload_dateXX")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_82(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("UPLOAD_DATE")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_83(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = ""
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_84(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = None
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_85(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(None).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_86(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 or date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_87(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) != 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_88(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 9 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_89(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = None
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_90(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=None).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_91(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(None, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_92(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, None).replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_93(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime("%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_94(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, ).replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_95(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "XX%Y%m%dXX").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_96(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_97(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%M%D").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_98(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = None

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_99(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(None)

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_100(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") and "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_101(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get(None) or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_102(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("XXvideo_descriptionXX") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_103(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("VIDEO_DESCRIPTION") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_104(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "XXXX")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_105(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=None,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_106(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=None,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_107(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=None,
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_108(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=None,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_109(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=None,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_110(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=None,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_111(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=None,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_112(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=None,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_113(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=None,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_114(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_115(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_116(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_117(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_118(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_119(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            channel_id=ch_id,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_120(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_category=ch_cat,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_121(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_raw_transcript__mutmut_122(self, content_id: ContentId) -> RawTranscript | None:
        """Retrieve raw transcript by searching raw/ directory."""
        target_name = f"{content_id.value}.md"
        matched = list(self.raw_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return RawTranscript(
                content_id=content_id,
                channel_name=file_path.parent.name,
                body=text,
            )

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or "")
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        raw_date = meta.get("video_date") or meta.get("upload_date")
        upload_date = None
        if raw_date:
            date_str = str(raw_date).strip()
            if len(date_str) == 8 and date_str.isdigit():
                try:
                    upload_date = datetime.strptime(date_str, "%Y%m%d").replace(tzinfo=UTC).date()
                except ValueError:
                    pass
        desc = str(meta.get("video_description") or "")

        return RawTranscript(
            content_id=content_id,
            channel_name=ch_name,
            body=body.strip(),
            title=title,
            source_url=url,
            upload_date=upload_date,
            channel_id=ch_id,
            channel_category=ch_cat,
            )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut)
    def save_enriched_compendium(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_orig(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_1(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = None
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_2(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir * sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_3(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(None)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_4(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = None

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_5(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir * f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_6(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = None
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_7(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description and ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_8(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or "XXXX"
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_9(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = None
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_10(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join(None)
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_11(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "XX\nXX".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_12(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " - l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_13(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("XX  XX" + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_14(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = None
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_15(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace(None, '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_16(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', None)
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_17(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_18(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', )
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_19(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('XX"XX', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_20(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', 'XX\\"XX')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_21(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = None
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_22(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace(None, '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_23(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', None)
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_24(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_25(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', )
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_26(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('XX"XX', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_27(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', 'XX\\"XX')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_28(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = None
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_29(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace(None, '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_30(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', None)
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_31(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_32(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', )
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_33(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category and "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_34(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "XXuncategorizedXX").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_35(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "UNCATEGORIZED").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_36(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('XX"XX', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_37(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', 'XX\\"XX')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_38(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = None

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_39(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id and "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_40(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "XXunknown_channelXX"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_41(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "UNKNOWN_CHANNEL"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_42(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = None
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_43(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = None
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_44(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = None
        self._atomic_write(file_path, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_45(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(None, content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_46(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, None)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_47(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(content)

    def xǁObsidianVaultAdapterǁsave_enriched_compendium__mutmut_48(self, compendium: EnrichedCompendium) -> None:
        """Persist enriched compendium directly into enriched/ directory with playground YAML."""
        channel_dir = self.enriched_dir / sanitize_filename(compendium.channel_name)
        file_path = channel_dir / f"{compendium.content_id.value}.md"

        desc = compendium.video_description or ""
        desc_indented = "\n".join("  " + l for l in desc.splitlines())
        escaped_title = compendium.title.value.replace('"', '\\"')
        escaped_channel = compendium.channel_name.replace('"', '\\"')
        escaped_category = (compendium.channel_category or "uncategorized").replace('"', '\\"')
        channel_id_val = compendium.channel_id or "unknown_channel"

        yaml_header = (
            f"---\n"
            f'video_title: "{escaped_title}"\n'
            f"video_id: {compendium.content_id.value}\n"
            f'channel_name: "{escaped_channel}"\n'
            f"channel_id: {channel_id_val}\n"
            f'channel_category: "{escaped_category}"\n'
            f"url: {compendium.source_url}\n"
            f"video_date: {compendium.video_date}\n"
            f"pass_count: {compendium.pass_count}\n"
            f"video_description: |\n"
            f"{desc_indented}\n"
            f"---"
        )
        body_text = (
            f"# {compendium.title.value}\n\n"
            f"{compendium.body}\n\n"
            f"## Informações Complementares\n\n"
            f"{compendium.complementary_info}"
        )
        content = f"{yaml_header}\n\n{body_text}"
        self._atomic_write(file_path, )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut)
    def get_enriched_compendium(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_orig(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_1(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = None
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_2(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = None
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_3(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(None)
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_4(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(None))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_5(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_6(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = None
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_7(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[1]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_8(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = None
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_9(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding=None)
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_10(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="XXutf-8XX")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_11(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="UTF-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_12(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = None
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_13(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(None)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_14(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_15(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = None
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_16(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = None

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_17(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) and {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_18(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(None) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_19(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = None
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_20(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split(None)
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_21(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("XX## Informações ComplementaresXX")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_22(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## informações complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_23(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## INFORMAÇÕES COMPLEMENTARES")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_24(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = None
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_25(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[1].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_26(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = None

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_27(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[2].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_28(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) >= 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_29(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 2 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_30(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else "XXXX"

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_31(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith(None):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_32(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("XX# XX"):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_33(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = None
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_34(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = None

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_35(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(None).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_36(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "XX\nXX".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_37(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[2:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_38(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = None
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_39(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(None)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_40(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") and file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_41(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") and meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_42(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get(None) or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_43(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("XXchannel_nameXX") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_44(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("CHANNEL_NAME") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_45(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get(None) or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_46(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("XXchannelXX") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_47(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("CHANNEL") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_48(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = None
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_49(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(None)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_50(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") and file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_51(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") and meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_52(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get(None) or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_53(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("XXvideo_titleXX") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_54(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("VIDEO_TITLE") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_55(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get(None) or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_56(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("XXtitleXX") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_57(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("TITLE") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_58(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = None
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_59(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(None)
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_60(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") and "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_61(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get(None) or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_62(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("XXchannel_idXX") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_63(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("CHANNEL_ID") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_64(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "XXXX")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_65(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = None
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_66(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(None)
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_67(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") and "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_68(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") and meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_69(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get(None) or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_70(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("XXchannel_categoryXX") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_71(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("CHANNEL_CATEGORY") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_72(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get(None) or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_73(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("XXdomainXX") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_74(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("DOMAIN") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_75(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "XXXX")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_76(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = None
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_77(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(None)
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_78(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") and "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_79(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") and meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_80(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get(None) or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_81(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("XXurlXX") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_82(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("URL") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_83(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get(None) or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_84(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("XXsource_urlXX") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_85(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("SOURCE_URL") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_86(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "XXXX")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_87(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = None
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_88(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(None)
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_89(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") and "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_90(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get(None) or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_91(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("XXvideo_dateXX") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_92(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("VIDEO_DATE") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_93(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "XXXX")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_94(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = None

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_95(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(None)

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_96(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") and "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_97(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get(None) or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_98(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("XXvideo_descriptionXX") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_99(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("VIDEO_DESCRIPTION") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_100(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "XXXX")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_101(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=None,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_102(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=None,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_103(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=None,
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_104(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=None,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_105(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=None,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_106(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=None,
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_107(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=None,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_108(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=None,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_109(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=None,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_110(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=None,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_111(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=None,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_112(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_113(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_114(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_115(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_116(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_117(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_118(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_119(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_120(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_121(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_122(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_123(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(None),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_124(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(None),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_125(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get(None, 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_126(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", None)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_127(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get(1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_128(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", )),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_129(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("XXpass_countXX", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_130(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("PASS_COUNT", 1)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    def xǁObsidianVaultAdapterǁget_enriched_compendium__mutmut_131(self, content_id: ContentId) -> EnrichedCompendium | None:
        """Retrieve enriched compendium by content ID."""
        target_name = f"{content_id.value}.md"
        matched = list(self.enriched_dir.glob(f"**/{target_name}"))
        if not matched:
            return None

        file_path = matched[0]
        text = file_path.read_text(encoding="utf-8")
        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, raw_body = match.groups()
        meta = yaml.safe_load(fm_text) or {}

        # Split into main body and complementary info
        parts = raw_body.split("## Informações Complementares")
        body_part = parts[0].strip()
        comp_part = parts[1].strip() if len(parts) > 1 else ""

        # Strip initial # title if present
        if body_part.startswith("# "):
            body_lines = body_part.splitlines()
            body_part = "\n".join(body_lines[1:]).strip()

        ch_name = str(meta.get("channel_name") or meta.get("channel") or file_path.parent.name)
        title = str(meta.get("video_title") or meta.get("title") or file_path.stem)
        ch_id = str(meta.get("channel_id") or "")
        ch_cat = str(meta.get("channel_category") or meta.get("domain") or "")
        url = str(meta.get("url") or meta.get("source_url") or "")
        v_date = str(meta.get("video_date") or "")
        desc = str(meta.get("video_description") or "")

        return EnrichedCompendium(
            content_id=content_id,
            channel_name=ch_name,
            title=NoteTitle(title),
            body=body_part,
            complementary_info=comp_part,
            pass_count=int(meta.get("pass_count", 2)),
            channel_id=ch_id,
            channel_category=ch_cat,
            source_url=url,
            video_date=v_date,
            video_description=desc,
        )

    @staticmethod
    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut)
    def _get_note_subfolder(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "entities"
        if note_type == NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_orig(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "entities"
        if note_type == NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_1(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type != NoteType.ENTITY:
            return "entities"
        if note_type == NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_2(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "XXentitiesXX"
        if note_type == NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_3(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "ENTITIES"
        if note_type == NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_4(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "entities"
        if note_type != NoteType.PROCESS:
            return "processes"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_5(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "entities"
        if note_type == NoteType.PROCESS:
            return "XXprocessesXX"
        return f"{note_type.value}s"

    @staticmethod
    def xǁObsidianVaultAdapterǁ_get_note_subfolder__mutmut_6(note_type: NoteType) -> str:
        """Return standardized plural folder name for a note type."""
        if note_type == NoteType.ENTITY:
            return "entities"
        if note_type == NoteType.PROCESS:
            return "PROCESSES"
        return f"{note_type.value}s"

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁ_get_note_path__mutmut)
    def _get_note_path(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(note.note_type)
        return self.vault_dir / sub_folder / f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_orig(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(note.note_type)
        return self.vault_dir / sub_folder / f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_1(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = None
        return self.vault_dir / sub_folder / f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_2(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(None)
        return self.vault_dir / sub_folder / f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_3(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(note.note_type)
        return self.vault_dir / sub_folder * f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_4(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(note.note_type)
        return self.vault_dir * sub_folder / f"{sanitize_filename(note.title.value)}.md"

    def xǁObsidianVaultAdapterǁ_get_note_path__mutmut_5(self, note: AtomicNote) -> Path:
        """Derive target file path in vault/ for an atomic note."""
        sub_folder = self._get_note_subfolder(note.note_type)
        return self.vault_dir / sub_folder / f"{sanitize_filename(None)}.md"

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁsave_atomic_note__mutmut)
    def save_atomic_note(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_orig(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_1(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = None

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_2(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(None)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_3(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = None
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_4(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "XXtitleXX": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_5(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "TITLE": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_6(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "XXtypeXX": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_7(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "TYPE": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_8(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "XXdomainXX": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_9(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "DOMAIN": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_10(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "XXclusterXX": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_11(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "CLUSTER": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_12(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "XXsourceXX": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_13(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "SOURCE": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_14(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "XXaliasesXX": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_15(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "ALIASES": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_16(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(None),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_17(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "XXtagsXX": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_18(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "TAGS": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_19(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(None),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_20(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = None

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_21(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(None, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_22(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=None, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_23(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=None)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_24(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_25(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_26(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, )

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_27(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=False, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_28(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=True)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_29(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = None

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_30(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "XX---XX",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_31(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "XX---XX",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_32(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "XXXX",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_33(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "XXXX",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_34(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "XX## Definição & Análise ContextualXX",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_35(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## definição & análise contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_36(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## DEFINIÇÃO & ANÁLISE CONTEXTUAL",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_37(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "XXXX",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_38(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append(None)
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_39(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("XX## Conexões & Relações DiretasXX")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_40(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## conexões & relações diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_41(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## CONEXÕES & RELAÇÕES DIRETAS")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_42(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(None)
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_43(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append(None)

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_44(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("XXXX")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_45(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append(None)
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_46(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("XX## Matriz CausalXX")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_47(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## matriz causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_48(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## MATRIZ CAUSAL")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_49(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(None)
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_50(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(None)
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_51(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(None)
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_52(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append(None)

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_53(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("XXXX")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_54(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append(None)
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_55(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("XX## Redes de Conexão (Cross-Context)XX")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_56(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## redes de conexão (cross-context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_57(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## REDES DE CONEXÃO (CROSS-CONTEXT)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_58(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(None)
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_59(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(None)
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_60(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(None)
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_61(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append(None)

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_62(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("XXXX")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_63(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(None, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_64(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, None)

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_65(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write("\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_66(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, )

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_67(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() - "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_68(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(None).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_69(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "XX\nXX".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_atomic_note__mutmut_70(self, note: AtomicNote) -> None:
        """Persist individual atomic note with standardized YAML frontmatter."""
        target_path = self._get_note_path(note)

        frontmatter_dict: dict[str, Any] = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "source": note.source,
            "aliases": list(note.aliases),
            "tags": list(note.content_tags),
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{note.title.value}]]",
            "",
            "## Definição & Análise Contextual",
            note.definition,
            "",
        ]

        if note.direct_relations:
            lines.append("## Conexões & Relações Diretas")
            for rel in note.direct_relations:
                lines.append(f"- [[{rel.value}]]")
            lines.append("")

        if note.causal_matrix:
            lines.append("## Matriz Causal")
            lines.append(f"- Causa: {note.causal_matrix.cause}")
            lines.append(f"- Efeito: {note.causal_matrix.effect}")
            if note.causal_matrix.epistemic_attribution:
                lines.append(f"- Atribuição Epistêmica: {note.causal_matrix.epistemic_attribution}")
            lines.append("")

        if note.cross_context:
            lines.append("## Redes de Conexão (Cross-Context)")
            if note.cross_context.precursors:
                lines.append(f"- Precursores: {note.cross_context.precursors}")
            if note.cross_context.lateral_events:
                lines.append(f"- Eventos Laterais: {note.cross_context.lateral_events}")
            if note.cross_context.aftermath:
                lines.append(f"- Desdobramentos: {note.cross_context.aftermath}")
            lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "XX\nXX")

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut)
    def _parse_atomic_note_file(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_orig(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_1(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = None
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_2(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding=None)
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_3(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="XXutf-8XX")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_4(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="UTF-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_5(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = None
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_6(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(None)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_7(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_8(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = None
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_9(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = None
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_10(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) and {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_11(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(None) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_12(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = None
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_13(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get(None, "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_14(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", None)
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_15(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_16(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", )
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_17(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("XXtypeXX", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_18(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("TYPE", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_19(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "XXconceptXX")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_20(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "CONCEPT")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_21(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = None
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_22(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(None)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_23(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = None

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_24(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = None
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_25(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get(None, file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_26(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", None)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_27(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get(file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_28(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", )
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_29(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("XXtitleXX", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_30(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("TITLE", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_31(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = None

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_32(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(None)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_33(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = None
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_34(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            None,
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_35(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            None,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_36(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_37(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_38(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"XX## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)XX",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_39(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## definição & análise contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_40(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## DEFINIÇÃO & ANÁLISE CONTEXTUAL\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_41(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = None
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_42(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(None).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_43(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(2).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_44(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) <= 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_45(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 21:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_46(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = None

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_47(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = None
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_48(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = None
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_49(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            None,
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_50(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            None,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_51(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_52(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_53(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"XX## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)XX",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_54(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## conexões & relações diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_55(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## CONEXÕES & RELAÇÕES DIRETAS\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_56(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(None, rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_57(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", None):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_58(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_59(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", ):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_60(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"XX\[\[(.*?)\]\]XX", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_61(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(None)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_62(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(2)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_63(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().upper() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_64(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() == title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_65(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.upper():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_66(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(None)

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_67(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(None))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_68(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = ""
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_69(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = None
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_70(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(None, body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_71(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", None)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_72(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_73(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", )
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_74(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"XX## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)XX", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_75(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## matriz causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_76(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## MATRIZ CAUSAL\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_77(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = None
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_78(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(None)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_79(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(2)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_80(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = None
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_81(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(None, cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_82(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", None)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_83(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_84(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", )
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_85(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"XX- Causa:\s*(.*)XX", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_86(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_87(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- CAUSA:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_88(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = None
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_89(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(None, cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_90(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", None)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_91(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_92(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", )
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_93(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"XX- Efeito:\s*(.*)XX", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_94(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_95(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- EFEITO:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_96(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = None
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_97(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(None, cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_98(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", None)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_99(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_100(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", )
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_101(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"XX- Atribuição Epistêmica:\s*(.*)XX", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_102(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- atribuição epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_103(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- ATRIBUIÇÃO EPISTÊMICA:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_104(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = None

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_105(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=None,
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_106(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=None,
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_107(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=None,
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_108(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_109(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_110(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_111(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(None).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_112(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(2).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_113(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "XXXX",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_114(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(None).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_115(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(2).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_116(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "XXXX",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_117(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(None).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_118(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(2).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_119(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "XXXX",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_120(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = ""
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_121(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = None
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_122(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            None, body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_123(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", None
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_124(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_125(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_126(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"XX## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)XX", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_127(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## redes de conexão \(cross-context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_128(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## REDES DE CONEXÃO \(CROSS-CONTEXT\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_129(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = None
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_130(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(None)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_131(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(2)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_132(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = None
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_133(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(None, cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_134(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", None)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_135(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_136(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", )
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_137(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"XX- Precursores:\s*(.*)XX", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_138(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_139(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- PRECURSORES:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_140(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = None
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_141(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(None, cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_142(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", None)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_143(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_144(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", )
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_145(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"XX- Eventos Laterais:\s*(.*)XX", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_146(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- eventos laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_147(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- EVENTOS LATERAIS:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_148(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = None
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_149(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(None, cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_150(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", None)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_151(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_152(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", )
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_153(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"XX- Desdobramentos:\s*(.*)XX", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_154(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_155(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- DESDOBRAMENTOS:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_156(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = None

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_157(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=None,
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_158(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=None,
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_159(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=None,
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_160(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_161(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_162(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_163(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(None).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_164(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(2).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_165(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "XXXX",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_166(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(None).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_167(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(2).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_168(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "XXXX",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_169(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(None).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_170(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(2).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_171(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "XXXX",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_172(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=None,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_173(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=None,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_174(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=None,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_175(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=None,
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_176(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=None,
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_177(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=None,
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_178(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=None,
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_179(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=None,
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_180(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=None,
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_181(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=None,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_182(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=None,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_183(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_184(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_185(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_186(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_187(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_188(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_189(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_190(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_191(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_192(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_193(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_194(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(None),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_195(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get(None, ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_196(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", None)),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_197(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get(())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_198(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", )),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_199(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("XXtagsXX", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_200(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("TAGS", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_201(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get(None, ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_202(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", None),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_203(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get(""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_204(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_205(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("XXdomainXX", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_206(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("DOMAIN", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_207(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", "XXXX"),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_208(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get(None, ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_209(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", None),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_210(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get(""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_211(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_212(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("XXclusterXX", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_213(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("CLUSTER", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_214(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", "XXXX"),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_215(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get(None, ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_216(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", None),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_217(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get(""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_218(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_219(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("XXsourceXX", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_220(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("SOURCE", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_221(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", "XXXX"),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_222(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(None),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_223(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get(None, ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_224(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", None)),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_225(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get(())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_226(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", )),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_227(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("XXaliasesXX", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_228(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("ALIASES", ())),
            direct_relations=tuple(relations),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    def xǁObsidianVaultAdapterǁ_parse_atomic_note_file__mutmut_229(self, file_path: Path) -> AtomicNote | None:
        """Parse markdown file into AtomicNote domain entity."""
        try:
            text = file_path.read_text(encoding="utf-8")
        except OSError:
            return None

        match = _FRONTMATTER_PATTERN.match(text)
        if not match:
            return None

        fm_text, body = match.groups()
        meta = yaml.safe_load(fm_text) or {}
        raw_type = meta.get("type", "concept")
        try:
            note_type = NoteType.from_string(raw_type)
        except (NoteTypologyError, ValueError):
            note_type = NoteType.CONCEPT

        title_str = meta.get("title", file_path.stem)
        title = NoteTitle(title_str)

        # Parse definition
        def_match = re.search(
            r"## Definição & Análise Contextual\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        definition = def_match.group(1).strip() if def_match else body.strip()
        if len(definition) < 20:
            definition = f"Definição contextual de {title.value} com análise teórica substancial."

        # Parse direct relations
        relations: list[NoteTitle] = []
        rel_match = re.search(
            r"## Conexões & Relações Diretas\s*\n([\s\S]*?)(?=\n## |\Z)",
            body,
        )
        if rel_match:
            for rel_str in re.findall(r"\[\[(.*?)\]\]", rel_match.group(1)):
                if rel_str.strip().lower() != title.value.lower():
                    relations.append(NoteTitle(rel_str.strip()))

        # Parse causal matrix
        causal_matrix: CausalMatrix | None = None
        cm_match = re.search(r"## Matriz Causal\s*\n([\s\S]*?)(?=\n## |\Z)", body)
        if cm_match:
            cm_text = cm_match.group(1)
            cause = re.search(r"- Causa:\s*(.*)", cm_text)
            effect = re.search(r"- Efeito:\s*(.*)", cm_text)
            attrib = re.search(r"- Atribuição Epistêmica:\s*(.*)", cm_text)
            causal_matrix = CausalMatrix(
                cause=cause.group(1).strip() if cause else "",
                effect=effect.group(1).strip() if effect else "",
                epistemic_attribution=attrib.group(1).strip() if attrib else "",
            )

        # Parse cross context
        cross_context: CrossContextRelations | None = None
        cc_match = re.search(
            r"## Redes de Conexão \(Cross-Context\)\s*\n([\s\S]*?)(?=\n## |\Z)", body
        )
        if cc_match:
            cc_text = cc_match.group(1)
            prec = re.search(r"- Precursores:\s*(.*)", cc_text)
            lat = re.search(r"- Eventos Laterais:\s*(.*)", cc_text)
            aft = re.search(r"- Desdobramentos:\s*(.*)", cc_text)
            cross_context = CrossContextRelations(
                precursors=prec.group(1).strip() if prec else "",
                lateral_events=lat.group(1).strip() if lat else "",
                aftermath=aft.group(1).strip() if aft else "",
            )

        return AtomicNote(
            title=title,
            note_type=note_type,
            definition=definition,
            content_tags=tuple(meta.get("tags", ())),
            domain=meta.get("domain", ""),
            cluster=meta.get("cluster", ""),
            source=meta.get("source", ""),
            aliases=tuple(meta.get("aliases", ())),
            direct_relations=tuple(None),
            causal_matrix=causal_matrix,
            cross_context=cross_context,
        )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut)
    def get_atomic_note_by_title(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_orig(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_1(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = None
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_2(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(None)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_3(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = None
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_4(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(None)
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_5(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(None))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_6(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "XXMOCsXX" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_7(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "mocs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_8(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCS" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_9(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_10(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                break
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_11(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = None
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_12(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(None)
            if note and note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_13(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note or note.title.value.lower() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_14(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.upper() == title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_15(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() != title.value.lower():
                return note
        return None

    def xǁObsidianVaultAdapterǁget_atomic_note_by_title__mutmut_16(self, title: NoteTitle) -> AtomicNote | None:
        """Retrieve atomic note by searching index or vault directory."""
        sanitized = sanitize_filename(title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts:
                continue
            note = self._parse_atomic_note_file(p)
            if note and note.title.value.lower() == title.value.upper():
                return note
        return None

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut)
    def get_all_atomic_notes(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_orig(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_1(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = None
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_2(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob(None):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_3(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("XX**/*.mdXX"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_4(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.MD"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_5(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "XXMOCsXX" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_6(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "mocs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_7(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCS" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_8(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" not in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_9(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                break
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_10(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = None
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_11(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(None)
            if note is not None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_12(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is None:
                notes.append(note)
        return notes

    def xǁObsidianVaultAdapterǁget_all_atomic_notes__mutmut_13(self) -> list[AtomicNote]:
        """Retrieve all atomic notes in vault/ excluding MOCs."""
        notes: list[AtomicNote] = []
        for file_path in self.vault_dir.glob("**/*.md"):
            if "MOCs" in file_path.parts:
                continue
            note = self._parse_atomic_note_file(file_path)
            if note is not None:
                notes.append(None)
        return notes

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁupdate_index_entry__mutmut)
    def update_index_entry(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_orig(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_1(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = None
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_2(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = None
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_3(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(None)
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_4(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding=None))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_5(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="XXutf-8XX"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_6(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="UTF-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_7(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = None

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_8(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = None
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_9(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(None)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_10(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = None

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_11(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(None)

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_12(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(None))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_13(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = None

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_14(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "XXtitleXX": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_15(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "TITLE": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_16(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "XXtypeXX": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_17(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "TYPE": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_18(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "XXdomainXX": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_19(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "DOMAIN": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_20(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "XXclusterXX": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_21(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "CLUSTER": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_22(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "XXaliasesXX": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_23(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "ALIASES": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_24(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(None),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_25(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "XXpathXX": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_26(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "PATH": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_27(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = None
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_28(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.upper()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_29(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = None

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_30(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.upper()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_31(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            None,
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_32(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            None,
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_33(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            json.dumps(index, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_34(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_35(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(None, ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_36(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=None, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_37(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=None),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_38(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(ensure_ascii=False, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_39(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_40(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, ),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_41(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=True, indent=2),
        )

    def xǁObsidianVaultAdapterǁupdate_index_entry__mutmut_42(self, note: AtomicNote) -> None:
        """Update master _index.json lookup index atomically."""
        index: dict[str, Any] = {}
        if self.index_path.exists():
            try:
                index = json.loads(self.index_path.read_text(encoding="utf-8"))
            except (json.JSONDecodeError, OSError):
                index = {}

        target_path = self._get_note_path(note)
        relative_path = str(target_path.relative_to(self.vault_dir))

        entry_payload = {
            "title": note.title.value,
            "type": note.note_type.value,
            "domain": note.domain,
            "cluster": note.cluster,
            "aliases": list(note.aliases),
            "path": relative_path,
        }

        # Index primary title
        index[note.title.value.lower()] = entry_payload
        # Index aliases
        for alias in note.aliases:
            index[alias.lower()] = entry_payload

        self._atomic_write(
            self.index_path,
            json.dumps(index, ensure_ascii=False, indent=3),
        )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁsave_map_of_content__mutmut)
    def save_map_of_content(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_orig(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_1(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = None

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_2(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir * f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_3(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(None)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_4(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = None
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_5(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "XXtitleXX": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_6(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "TITLE": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_7(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "XXtypeXX": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_8(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "TYPE": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_9(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "XXmocXX",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_10(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "MOC",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_11(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "XXthemeXX": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_12(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "THEME": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_13(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = None

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_14(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(None, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_15(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=None, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_16(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=None)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_17(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_18(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_19(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, )

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_20(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=False, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_21(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=True)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_22(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = None
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_23(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "XX---XX",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_24(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "XX---XX",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_25(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "XXXX",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_26(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "XXXX",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_27(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "XXXX",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_28(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "XX## Notas AssociadasXX",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_29(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## notas associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_30(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## NOTAS ASSOCIADAS",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_31(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(None)
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_32(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append(None)

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_33(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("XXXX")

        self._atomic_write(target_path, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_34(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(None, "\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_35(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, None)

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_36(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write("\n".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_37(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, )

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_38(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() - "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_39(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(None).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_40(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "XX\nXX".join(lines).strip() + "\n")

    def xǁObsidianVaultAdapterǁsave_map_of_content__mutmut_41(self, moc: MapOfContent) -> None:
        """Persist Map of Content in vault/MOCs/ atomically."""
        target_path = self.mocs_dir / f"{sanitize_filename(moc.title.value)}.md"

        frontmatter_dict = {
            "title": moc.title.value,
            "type": "moc",
            "theme": moc.theme,
        }
        frontmatter_yaml = yaml.dump(frontmatter_dict, allow_unicode=True, sort_keys=False)

        lines: list[str] = [
            "---",
            frontmatter_yaml.strip(),
            "---",
            "",
            f"# [[{moc.title.value}]]",
            "",
            moc.overview,
            "",
            "## Notas Associadas",
        ]
        for note_title in moc.associated_notes:
            lines.append(f"- [[{note_title.value}]]")
        lines.append("")

        self._atomic_write(target_path, "\n".join(lines).strip() + "XX\nXX")

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut)
    def delete_atomic_note(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_orig(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_1(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = None
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_2(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(None)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_3(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = None
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_4(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(None)
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_5(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(None))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_6(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts or p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_7(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "XXMOCsXX" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_8(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "mocs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_9(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCS" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_10(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_11(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=None)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_12(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=False)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_13(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(None)
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_14(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.upper())
        for alias in note.aliases:
            self.remove_index_entry(alias.lower())

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_15(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(None)

    def xǁObsidianVaultAdapterǁdelete_atomic_note__mutmut_16(self, note: AtomicNote) -> None:
        """Remove atomic note file from vault and clean up index entries."""
        sanitized = sanitize_filename(note.title.value)
        matched = list(self.vault_dir.glob(f"**/{sanitized}.md"))
        for p in matched:
            if "MOCs" not in p.parts and p.is_file():
                p.unlink(missing_ok=True)

        self.remove_index_entry(note.title.value.lower())
        for alias in note.aliases:
            self.remove_index_entry(alias.upper())

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁremove_index_entry__mutmut)
    def remove_index_entry(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_orig(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_1(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_2(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = None
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_3(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(None)
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_4(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding=None))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_5(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="XXutf-8XX"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_6(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="UTF-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_7(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = None
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_8(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.upper().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_9(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k not in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_10(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                None,
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_11(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                None,
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_12(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                json.dumps(index, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_13(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_14(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(None, ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_15(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=None, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_16(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=None),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_17(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(ensure_ascii=False, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_18(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_19(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, ),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_20(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=True, indent=2),
            )

    def xǁObsidianVaultAdapterǁremove_index_entry__mutmut_21(self, key: str) -> None:
        """Remove specific canonical title or alias key from master _index.json."""
        if not self.index_path.exists():
            return
        try:
            index = json.loads(self.index_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, OSError):
            return

        k = key.lower().strip()
        if k in index:
            del index[k]
            self._atomic_write(
                self.index_path,
                json.dumps(index, ensure_ascii=False, indent=3),
            )

    @_mutmut_mutated(mutants_xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut)
    def rewrite_wiki_links(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_orig(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_1(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = None
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_2(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = None
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_3(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.upper() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_4(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() != new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_5(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.upper():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_6(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 1

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_7(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = None
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_8(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(None, re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_9(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", None)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_10(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_11(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", )
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_12(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(None)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_13(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = None

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_14(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 1

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_15(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob(None):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_16(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("XX**/*.mdXX"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_17(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.MD"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_18(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = None
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_19(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding=None)
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_20(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="XXutf-8XX")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_21(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="UTF-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_22(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                break

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_23(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = None
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_24(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) and ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_25(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(None) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_26(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(2) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_27(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or "XXXX"
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_28(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = None
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_29(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(None, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_30(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, None)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_31(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_32(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, )
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_33(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count >= 0:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_34(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 1:
                self._atomic_write(file_path, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_35(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(None, new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_36(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, None)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_37(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(new_content)
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_38(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, )
                updated_count += 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_39(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count = 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_40(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count -= 1

        return updated_count

    def xǁObsidianVaultAdapterǁrewrite_wiki_links__mutmut_41(self, old_title: NoteTitle, new_title: NoteTitle) -> int:
        """Rewrite all inbound [[old_title]] links to [[new_title]] across all markdown files in vault/.

        Returns:
            Count of files updated.
        """
        old_val = old_title.value.strip()
        new_val = new_title.value.strip()
        if old_val.lower() == new_val.lower():
            return 0

        pattern = re.compile(rf"\[\[{re.escape(old_val)}(\|.*?)?\]\]", re.IGNORECASE)
        updated_count = 0

        for file_path in self.vault_dir.glob("**/*.md"):
            try:
                content = file_path.read_text(encoding="utf-8")
            except OSError:
                continue

            def _repl(m: re.Match[str]) -> str:
                pipe_part = m.group(1) or ""
                return f"[[{new_val}{pipe_part}]]"

            new_content, count = pattern.subn(_repl, content)
            if count > 0:
                self._atomic_write(file_path, new_content)
                updated_count += 2

        return updated_count

mutants_xǁObsidianVaultAdapterǁ__init____mutmut['_mutmut_orig'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_1'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_2'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_3'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_4'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_5'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_6'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_7'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_8'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_9'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_10'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_11'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_12'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_13'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_14'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_15'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_16'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_17'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_18'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_19'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_20'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_21'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_22'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_23'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_24'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_24 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_25'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut_25 # type: ignore # mutmut generated
mutants_xǁObsidianVaultAdapterǁ__init____mutmut['xǁObsidianVaultAdapterǁ__init____mutmut_26'] = ObsidianVaultAdapter.xǁObsidianVaultAdapterǁ__init____mutmut