"""Stage 5 Use Case: Synthesize Atomic Note Batches.

Generates rich AtomicNote entities from an AtomicEntityInventory in small batches (<= 5).
"""

from __future__ import annotations

import json
import re

from cresmo.application.json_parser import extract_json_data
from cresmo.application.ports import (
    LLMTransformationPort,
    PromptProviderPort,
    VaultRepositoryPort,
)
from cresmo.domain.entities import AtomicNote, EnrichedCompendium
from cresmo.domain.exceptions import DomainValidationError, NoteTypologyError
from cresmo.domain.value_objects import (
    AtomicEntityInventory,
    CausalMatrix,
    CrossContextRelations,
    NoteTitle,
    NoteType,
)

_PREFIX_RE = re.compile(r"^(?:o|a|os|as|um|uma|uns|umas|dr|dra|prof|dom|frei)\s+", re.IGNORECASE)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__norm_honorific__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__norm_honorific__mutmut)
def _norm_honorific(text: str) -> str:
    return _PREFIX_RE.sub("", text.strip().lower())


def x__norm_honorific__mutmut_orig(text: str) -> str:
    return _PREFIX_RE.sub("", text.strip().lower())


def x__norm_honorific__mutmut_1(text: str) -> str:
    return _PREFIX_RE.sub(None, text.strip().lower())


def x__norm_honorific__mutmut_2(text: str) -> str:
    return _PREFIX_RE.sub("", None)


def x__norm_honorific__mutmut_3(text: str) -> str:
    return _PREFIX_RE.sub(text.strip().lower())


def x__norm_honorific__mutmut_4(text: str) -> str:
    return _PREFIX_RE.sub("", )


def x__norm_honorific__mutmut_5(text: str) -> str:
    return _PREFIX_RE.sub("XXXX", text.strip().lower())


def x__norm_honorific__mutmut_6(text: str) -> str:
    return _PREFIX_RE.sub("", text.strip().upper())

mutants_x__norm_honorific__mutmut['_mutmut_orig'] = x__norm_honorific__mutmut_orig # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_1'] = x__norm_honorific__mutmut_1 # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_2'] = x__norm_honorific__mutmut_2 # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_3'] = x__norm_honorific__mutmut_3 # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_4'] = x__norm_honorific__mutmut_4 # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_5'] = x__norm_honorific__mutmut_5 # type: ignore # mutmut generated
mutants_x__norm_honorific__mutmut['x__norm_honorific__mutmut_6'] = x__norm_honorific__mutmut_6 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut: MutantDict = {}  # type: ignore


class SynthesizeAtomicBatchUseCase:
    """Stage 5: Batched Atomic Note synthesis and incremental reconciliation."""

    @_mutmut_mutated(mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut)
    def __init__(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_orig(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_1(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 6,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_2(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = None
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_3(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = None
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_4(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = None
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_5(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(None, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_6(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, None)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_7(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_8(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, )
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_9(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(2, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_10(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is not None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_11(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = None
        else:
            self.prompt_provider = prompt_provider

    def xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_12(
        self,
        llm_port: LLMTransformationPort,
        vault_port: VaultRepositoryPort,
        batch_size: int = 5,
        prompt_provider: PromptProviderPort | None = None,
    ) -> None:
        self.llm_port = llm_port
        self.vault_port = vault_port
        self.batch_size = max(1, batch_size)
        if prompt_provider is None:
            from cresmo.infrastructure.adapters.prompt_provider import JsonPromptProvider

            self.prompt_provider: PromptProviderPort = JsonPromptProvider()
        else:
            self.prompt_provider = None

    @_mutmut_mutated(mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut)
    def execute(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_orig(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_1(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = None
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_2(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = None
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_3(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = None
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_4(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.upper()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_5(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = None

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_6(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.upper()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_7(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = None
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_8(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.upper().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_9(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("XXdom XX", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_10(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("DOM ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_11(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "XXdona XX", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_12(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "DONA ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_13(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "XXd. XX", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_14(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "D. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_15(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "XXd XX"):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_16(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "D "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_17(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(None):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_18(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = None
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_19(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    return
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_20(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(None, " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_21(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", None, cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_22(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", None).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_23(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(" ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_24(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_25(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", ).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_26(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"XX[\s\-_.,()]+XX", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_27(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", "XX XX", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_28(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = None

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_29(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(None): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_30(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) > 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_31(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 5
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_32(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = None
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_33(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = None

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_34(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = None
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_35(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.upper()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_36(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = None
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_37(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(None)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_38(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key not in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_39(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(None)
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_40(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key not in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_41(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(None)
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_42(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append(None)

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_43(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(None, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_44(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, None, self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_45(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), None):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_46(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_47(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_48(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), ):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_49(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(1, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_50(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = None
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_51(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i - self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_52(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = None

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_53(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"XXtitleXX": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_54(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"TITLE": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_55(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "XXtypeXX": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_56(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "TYPE": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_57(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = None
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_58(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=None,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_59(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=None,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_60(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=None,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_61(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=None,
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_62(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_63(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_64(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_65(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_66(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(None, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_67(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=None),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_68(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_69(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_70(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=True),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_71(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = None
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_72(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=None)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_73(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = None
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_74(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(None)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_75(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_76(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    None
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_77(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(None).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_78(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_79(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    break
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_80(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = None
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_81(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get(None)
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_82(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("XXtitleXX")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_83(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("TITLE")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_84(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str and not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_85(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_86(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_87(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    break
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_88(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = None

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_89(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(None)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_90(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = None
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_91(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get(None, "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_92(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", None)
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_93(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_94(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", )
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_95(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("XXtypeXX", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_96(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("TYPE", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_97(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "XXconceptXX")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_98(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "CONCEPT")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_99(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = None
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_100(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(None)
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_101(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(None))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_102(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = None

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_103(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = None

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_104(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(None).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_105(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get(None, "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_106(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", None)).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_107(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_108(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", )).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_109(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("XXdefinitionXX", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_110(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("DEFINITION", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_111(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "XXXX")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_112(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = None
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_113(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get(None, [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_114(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", None)
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_115(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get([])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_116(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", )
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_117(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("XXdirect_relationsXX", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_118(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("DIRECT_RELATIONS", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_119(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = None

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_120(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    None
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_121(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(None) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_122(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) or r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_123(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = None
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_124(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get(None)
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_125(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("XXcausal_matrixXX")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_126(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("CAUSAL_MATRIX")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_127(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = ""
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_128(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = None

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_129(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=None,
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_130(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=None,
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_131(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=None,
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_132(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_133(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_134(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_135(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(None),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_136(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get(None, "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_137(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", None)),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_138(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_139(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", )),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_140(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("XXcauseXX", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_141(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("CAUSE", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_142(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "XXXX")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_143(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(None),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_144(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get(None, "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_145(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", None)),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_146(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_147(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", )),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_148(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("XXeffectXX", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_149(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("EFFECT", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_150(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "XXXX")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_151(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(None),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_152(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get(None, "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_153(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", None)),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_154(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_155(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", )),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_156(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("XXepistemic_attributionXX", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_157(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("EPISTEMIC_ATTRIBUTION", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_158(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "XXXX")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_159(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = None
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_160(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get(None)
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_161(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("XXcross_contextXX")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_162(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("CROSS_CONTEXT")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_163(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = ""
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_164(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = None

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_165(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=None,
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_166(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=None,
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_167(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=None,
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_168(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_169(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_170(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_171(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(None),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_172(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get(None, "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_173(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", None)),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_174(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_175(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", )),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_176(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("XXprecursorsXX", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_177(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("PRECURSORS", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_178(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "XXXX")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_179(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(None),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_180(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get(None, "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_181(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", None)),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_182(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_183(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", )),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_184(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("XXlateral_eventsXX", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_185(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("LATERAL_EVENTS", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_186(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "XXXX")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_187(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(None),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_188(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get(None, "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_189(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", None)),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_190(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_191(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", )),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_192(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("XXaftermathXX", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_193(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("AFTERMATH", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_194(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "XXXX")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_195(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = None
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_196(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get(None, [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_197(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", None)
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_198(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get([])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_199(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", )
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_200(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("XXaliasesXX", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_201(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("ALIASES", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_202(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = None

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_203(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(None)

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_204(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) or a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_205(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = None
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_206(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get(None, [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_207(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", None)
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_208(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get([])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_209(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", )
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_210(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("XXcontent_tagsXX", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_211(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("CONTENT_TAGS", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_212(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = None

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_213(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    None
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_214(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) or t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_215(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = None

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_216(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=None,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_217(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=None,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_218(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=None,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_219(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=None,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_220(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=None,
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_221(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=None,
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_222(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=None,
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_223(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=None,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_224(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=None,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_225(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=None,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_226(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=None,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_227(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_228(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_229(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_230(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_231(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_232(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_233(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_234(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_235(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_236(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_237(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_238(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(None),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_239(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get(None, compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_240(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", None)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_241(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get(compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_242(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", )),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_243(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("XXdomainXX", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_244(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("DOMAIN", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_245(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(None),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_246(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get(None, "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_247(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", None)),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_248(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_249(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", )),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_250(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("XXclusterXX", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_251(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("CLUSTER", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_252(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "XXXX")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_253(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(None),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_254(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get(None, compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_255(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", None)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_256(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get(compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_257(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", )),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_258(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("XXsourceXX", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_259(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("SOURCE", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_260(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(None)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_261(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(None)
                synthesized_notes.append(note)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

    def xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_262(
        self,
        inventory: AtomicEntityInventory,
        compendium: EnrichedCompendium,
    ) -> list[AtomicNote]:
        """Execute Stage 5 batched synthesis.

        Args:
            inventory: Discovered unique entity inventory from Stage 4.
            compendium: Enriched compendium from Stage 3.

        Returns:
            List of all synthesized and persisted AtomicNote domain aggregates.

        Raises:
            DomainValidationError: If synthesis payload violates domain rules.
        """
        # Check existing notes in vault for incremental idempotency (Tier 1 lookup)
        existing_notes = self.vault_port.get_all_atomic_notes()
        existing_lookup: dict[str, AtomicNote] = {}
        for n in existing_notes:
            existing_lookup[n.title.value.lower()] = n
            for a in n.aliases:
                existing_lookup[a.lower()] = n

        def _norm_honorific(s: str) -> str:
            cleaned = s.lower().strip()
            for pfx in ("dom ", "dona ", "d. ", "d "):
                if cleaned.startswith(pfx):
                    cleaned = cleaned[len(pfx) :].strip()
                    break
            return re.sub(r"[\s\-_.,()]+", " ", cleaned).strip()

        norm_lookup: dict[str, AtomicNote] = {
            _norm_honorific(n.title.value): n
            for n in existing_notes
            if len(_norm_honorific(n.title.value)) >= 4
        }

        synthesized_notes: list[AtomicNote] = []
        pending_items: list[tuple[NoteTitle, NoteType]] = []

        for title, note_type in inventory.items:
            key = title.value.lower()
            norm_key = _norm_honorific(key)
            if key in existing_lookup:
                synthesized_notes.append(existing_lookup[key])
            elif norm_key in norm_lookup:
                synthesized_notes.append(norm_lookup[norm_key])
            else:
                pending_items.append((title, note_type))

        # Partition pending entities into chunks of size <= batch_size
        for i in range(0, len(pending_items), self.batch_size):
            chunk = pending_items[i : i + self.batch_size]
            targets_summary = [
                {"title": title.value, "type": note_type.value} for title, note_type in chunk
            ]

            # Step 2a: Prompt LLM for structured JSON definitions, causal matrices, and relations.
            prompt = self.prompt_provider.get_batch_notes_prompt(
                compendium_title=compendium.title.value,
                channel_name=compendium.channel_name,
                compendium_body=compendium.body,
                targets_json=json.dumps(targets_summary, ensure_ascii=False),
            )
            response = self.llm_port.transform(prompt=prompt)
            data = extract_json_data(response)
            if not isinstance(data, list):
                raise DomainValidationError(
                    f"Batch synthesis expected JSON array, got: {type(data).__name__}"
                )

            # Step 2b: Parse response into AtomicNote domain aggregates.
            for entry in data:
                if not isinstance(entry, dict):
                    continue
                title_str = entry.get("title")
                if not title_str or not isinstance(title_str, str):
                    continue
                title = NoteTitle(title_str)

                type_raw = entry.get("type", "concept")
                try:
                    note_type = NoteType.from_string(str(type_raw))
                except NoteTypologyError:
                    note_type = NoteType.CONCEPT

                definition = str(entry.get("definition", "")).strip()

                relations_raw = entry.get("direct_relations", [])
                relations = tuple(
                    NoteTitle(r) for r in relations_raw if isinstance(r, str) and r.strip()
                )

                cm_raw = entry.get("causal_matrix")
                causal_matrix: CausalMatrix | None = None
                if isinstance(cm_raw, dict):
                    causal_matrix = CausalMatrix(
                        cause=str(cm_raw.get("cause", "")),
                        effect=str(cm_raw.get("effect", "")),
                        epistemic_attribution=str(cm_raw.get("epistemic_attribution", "")),
                    )

                cc_raw = entry.get("cross_context")
                cross_context: CrossContextRelations | None = None
                if isinstance(cc_raw, dict):
                    cross_context = CrossContextRelations(
                        precursors=str(cc_raw.get("precursors", "")),
                        lateral_events=str(cc_raw.get("lateral_events", "")),
                        aftermath=str(cc_raw.get("aftermath", "")),
                    )

                aliases_raw = entry.get("aliases", [])
                aliases = tuple(a.strip() for a in aliases_raw if isinstance(a, str) and a.strip())

                tags_raw = entry.get("content_tags", [])
                content_tags = tuple(
                    t.strip() for t in tags_raw if isinstance(t, str) and t.strip()
                )

                note = AtomicNote(
                    title=title,
                    note_type=note_type,
                    definition=definition,
                    content_tags=content_tags,
                    domain=str(entry.get("domain", compendium.channel_name)),
                    cluster=str(entry.get("cluster", "")),
                    source=str(entry.get("source", compendium.title.value)),
                    aliases=aliases,
                    direct_relations=relations,
                    causal_matrix=causal_matrix,
                    cross_context=cross_context,
                )

                # Step 2c & 2d: Save note and update index entry incrementally.
                self.vault_port.save_atomic_note(note)
                self.vault_port.update_index_entry(note)
                synthesized_notes.append(None)

        # Step 3: Return list of all synthesized AtomicNote entities.
        return synthesized_notes

mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['_mutmut_orig'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_1'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_2'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_3'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_4'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_5'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_6'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_7'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_8'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_9'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_10'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_11'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut['xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_12'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁ__init____mutmut_12 # type: ignore # mutmut generated

mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['_mutmut_orig'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_1'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_2'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_3'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_4'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_5'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_6'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_7'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_8'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_9'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_10'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_11'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_12'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_13'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_14'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_15'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_16'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_17'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_18'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_19'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_20'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_21'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_22'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_22 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_23'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_23 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_24'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_24 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_25'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_25 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_26'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_26 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_27'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_27 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_28'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_28 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_29'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_29 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_30'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_30 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_31'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_31 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_32'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_32 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_33'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_33 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_34'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_34 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_35'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_35 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_36'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_36 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_37'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_37 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_38'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_38 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_39'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_39 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_40'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_40 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_41'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_41 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_42'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_42 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_43'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_43 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_44'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_44 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_45'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_45 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_46'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_46 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_47'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_47 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_48'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_48 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_49'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_49 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_50'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_50 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_51'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_51 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_52'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_52 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_53'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_53 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_54'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_54 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_55'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_55 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_56'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_56 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_57'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_57 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_58'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_58 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_59'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_59 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_60'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_60 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_61'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_61 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_62'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_62 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_63'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_63 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_64'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_64 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_65'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_65 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_66'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_66 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_67'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_67 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_68'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_68 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_69'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_69 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_70'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_70 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_71'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_71 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_72'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_72 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_73'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_73 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_74'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_74 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_75'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_75 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_76'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_76 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_77'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_77 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_78'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_78 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_79'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_79 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_80'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_80 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_81'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_81 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_82'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_82 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_83'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_83 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_84'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_84 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_85'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_85 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_86'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_86 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_87'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_87 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_88'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_88 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_89'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_89 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_90'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_90 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_91'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_91 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_92'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_92 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_93'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_93 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_94'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_94 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_95'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_95 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_96'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_96 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_97'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_97 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_98'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_98 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_99'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_99 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_100'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_100 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_101'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_101 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_102'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_102 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_103'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_103 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_104'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_104 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_105'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_105 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_106'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_106 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_107'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_107 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_108'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_108 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_109'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_109 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_110'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_110 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_111'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_111 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_112'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_112 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_113'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_113 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_114'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_114 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_115'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_115 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_116'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_116 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_117'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_117 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_118'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_118 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_119'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_119 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_120'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_120 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_121'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_121 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_122'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_122 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_123'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_123 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_124'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_124 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_125'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_125 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_126'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_126 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_127'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_127 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_128'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_128 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_129'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_129 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_130'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_130 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_131'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_131 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_132'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_132 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_133'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_133 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_134'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_134 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_135'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_135 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_136'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_136 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_137'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_137 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_138'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_138 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_139'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_139 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_140'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_140 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_141'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_141 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_142'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_142 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_143'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_143 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_144'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_144 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_145'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_145 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_146'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_146 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_147'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_147 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_148'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_148 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_149'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_149 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_150'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_150 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_151'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_151 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_152'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_152 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_153'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_153 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_154'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_154 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_155'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_155 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_156'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_156 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_157'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_157 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_158'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_158 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_159'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_159 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_160'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_160 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_161'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_161 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_162'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_162 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_163'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_163 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_164'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_164 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_165'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_165 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_166'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_166 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_167'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_167 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_168'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_168 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_169'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_169 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_170'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_170 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_171'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_171 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_172'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_172 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_173'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_173 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_174'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_174 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_175'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_175 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_176'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_176 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_177'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_177 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_178'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_178 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_179'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_179 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_180'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_180 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_181'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_181 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_182'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_182 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_183'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_183 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_184'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_184 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_185'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_185 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_186'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_186 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_187'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_187 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_188'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_188 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_189'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_189 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_190'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_190 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_191'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_191 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_192'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_192 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_193'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_193 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_194'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_194 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_195'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_195 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_196'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_196 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_197'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_197 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_198'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_198 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_199'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_199 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_200'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_200 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_201'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_201 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_202'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_202 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_203'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_203 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_204'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_204 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_205'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_205 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_206'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_206 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_207'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_207 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_208'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_208 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_209'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_209 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_210'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_210 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_211'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_211 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_212'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_212 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_213'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_213 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_214'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_214 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_215'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_215 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_216'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_216 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_217'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_217 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_218'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_218 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_219'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_219 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_220'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_220 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_221'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_221 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_222'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_222 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_223'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_223 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_224'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_224 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_225'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_225 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_226'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_226 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_227'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_227 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_228'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_228 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_229'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_229 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_230'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_230 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_231'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_231 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_232'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_232 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_233'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_233 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_234'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_234 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_235'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_235 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_236'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_236 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_237'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_237 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_238'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_238 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_239'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_239 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_240'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_240 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_241'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_241 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_242'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_242 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_243'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_243 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_244'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_244 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_245'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_245 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_246'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_246 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_247'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_247 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_248'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_248 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_249'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_249 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_250'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_250 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_251'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_251 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_252'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_252 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_253'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_253 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_254'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_254 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_255'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_255 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_256'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_256 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_257'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_257 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_258'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_258 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_259'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_259 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_260'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_260 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_261'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_261 # type: ignore # mutmut generated
mutants_xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut['xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_262'] = SynthesizeAtomicBatchUseCase.xǁSynthesizeAtomicBatchUseCaseǁexecute__mutmut_262 # type: ignore # mutmut generated
