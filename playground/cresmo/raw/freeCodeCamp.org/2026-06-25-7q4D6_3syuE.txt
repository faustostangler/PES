---
video_title: "Scaling Your AI Models with Micro-DDP – Tutorial"
video_id: 7q4D6_3syuE
channel_name: "freeCodeCamp.org"
channel_id: UC8butISFwT-Wl7EV0hUK0BQ
channel_category: "Education"
url: https://www.youtube.com/watch?v=7q4D6_3syuE
video_date: 2026-06-25T10:00:35+00:00
video_description: |
  Learn about Distributed Data Parallelism (DDP), an important technique for training large-scale AI models. This course provides a hands-on guide for implementing DDP, ensuring your training processes are efficient and scalable. Learn how to overcome memory limitations and train models effectively across multiple GPUs.
  
  Course created by ‪@neuralkian 
  
  ❤️ Support for this channel comes from our friends at Scrimba – the coding platform that's reinvented interactive learning: https://scrimba.com/freecodecamp
  
  ⭐️ Contents ⭐️
  - 0:00:00 Introduction
  - 0:02:11 Setup and Prerequisites
  - 0:05:07 What is Distributed Training?
  - 0:07:25 Data Parallelism vs. Model Parallelism
  - 0:08:51 Data Parallelism (DP) vs. Distributed Data Parallelism (DDP)
  - 0:12:07 Distributed Setup Configurations
  - 0:13:28 General DDP Workflow and Communication Primitives
  - 0:13:58 Broadcast Operation
  - 0:15:18 All Reduce Operation
  - 0:21:23 Overlapping Computation and Communication (Gradient Hooks/Bucketing)
  - 0:25:57 Step 1: Manual Batch Averaging Implementation
  - 0:49:15 Step 2: All Reduce Sandbox
  - 1:07:05 Step 3: Implementing DDP Hooks
  - 1:17:10 Performance Analysis and Trade-offs
  
  🎉 Thanks to our Champion and Sponsor supporters:
  👾 @omerhattapoglu1158
  👾 @goddardtan
  👾 @akihayashi6629
  👾 @kikilogsin
  👾 @anthonycampbell2148
  👾 @tobymiller7790
  👾 @rajibdassharma497
  👾 @CloudVirtualizationEnthusiast
  👾 @adilsoncarlosvianacarlos
  👾 @martinmacchia1564
  👾 @ulisesmoralez4160
  👾 @_Oscar_
  👾 @jedi-or-sith2728
  👾 @justinhual1290
  
  --
  
  Learn to code for free and get a developer job: https://www.freecodecamp.org
  
  Read hundreds of articles on programming: https://freecodecamp.org/news
---
learn the essential techniques of distributed data parallelism. Keane will help you master the architecture and implementation of efficient model training across multiple devices. Learning the skill is important because it allows you to train massive AI models significantly faster by breaking the workload across multiple GPUs, which is a foundational requirement for a modern large-scale machine learning. So let's get started. Hello, and welcome to this video. My name is Keane, and I'm really excited to present this free code camp course to you today, titled MicroDDP in the style of Andre Carpothi's naming convention, and this stands precisely for micro-distributed data parallelism. So I've already made a course in fact on MicroPp, which is micro-pipeline parallelism, and in the spirit of the remaining parallelism techniques in distributed computing and deep learning, we're next going to tackle today distributed data parallelism. Now, when it comes to the purexes in this course, I assume that you've had some exposure to deep learning already. So for example, you will know how to do at basic PyTorch training loop where you have a forward pass, a backward pass, an authorizer step, and you just repeat this process to learn the weights of AMLP. And also, I'm assuming that you've had some experience with the terminal and more specifically using UV. But even if you haven't used UV before, then honestly, this is a great opportunity to learn how to use it. And furthermore, even if you're a window user, this tutorial should be completely viable for you, because everything I discussed here is either going to be identical in terms of window commands or slightly different. So the first thing that we're going to do is install UV if you haven't already. And the curl command, I believe, works the same for window. If it doesn't, then just head to the UV page and find the correct command. Otherwise, the rest of the commands that we're going to be running is all going to be within this virtual environment in the micro-dDP repository. So I didn't mention, but we'll need to clone the repo itself. So the URL for this repo is the following. This is for the course page. But also, if I come into a new browser and then just type in the repo name, we'll be able to find if it loads. Well, that's strange. Let's just change the link here to get to the original page. Boom. So this is the repository. And if I close my terminal, you'll be able to see it larger. So you can just simply come here. And choose either HTTPS or they could have CLI for cloning, whichever way you wish. And then copy this link and then clone the repository into your computer. So this is quite straightforward, in my opinion, in terms of the setup. And then once you have the repo cloned in your workspace, then you can open it up in your favorite IDE, integrated development environment. In this case, I'm using cursor, but VS code or other environments are completely suitable. And then once you're inside of the environment, you just need to run a simple UVC command. So you can see if I do print working directory, then we'll see that I'm in the micro DDP directory. And once you're in the directory, so you can do CD to get into the directory. If you're not in already, that means change the directory, then you'll just run UVSync. And I have it already set up. But actually, if I remove my virtual environment, which is already installed, recursive room. So be careful when you're doing this, because you don't want to remove the wrong thing incorrectly. That's a very dangerous command if you do improperly. But if now I do UVSync, you'll see that it's downloading all the pie torch, all the everything that we need in order to run this tutorial properly. So torch is the largest package of all. It takes the longest time, but even so, it's still really fast to seconds, I believe. So there you go. And now that that is done, we can come back to the course page and get right away started. So this is going to be our reference of the entire course. If you ever have issues that you're going through while you're going through this course, then this is the standard reference. In terms of principles, this is really quick. I just want to say that I'm doing this with you together to learn through problem solving. I write pseudocode for each function before implementation. If I don't then tell me in the comments. I demonstrate concepts through small examples. This is how I love to learn. And I think that it is pretty universal, pretty university helpful. And then I'm going to debug with you so we learn through my mistakes together. In previous tutorials, I've skipped through the errors because I didn't want to show them to waste your time. But then I realized from commenters that seeing how I go through the errors or just seeing the error process in general is helpful to understand. And also, if you don't want to see it, then you can just skip ahead. So that's all for the introduction. And now let's go to the real introduction, which is what is distributed training. So we're going to talk about this for the next 10, 15 minutes. And let me then close this so that we have a bigger view and move this up here. OK. So scrolling down, why does she retrain? Let's talk about some problems that occur when you train large models. So when you train a large model on a single GPU, there are three challenges. One is that if the model is too large, it may not fit in the GPU memory. Another one is that if you're batch size, which is to say the size of each batch, which goes through the model at a single time, is too large, then you'll exceed the memory capacity of your GPU or whatever device you're using in order to train your model, which will cause out of memory errors. And then also time, if you just do the single GPU, then if you're doing like a training run, which is anything similar to a transformer style model and on the order of maybe GP2, GP3, then it will literally take years on huge data sets. If you look at the models that are trained by front to your labs, at least open source ones where they talk about the entire training stack, you can see that in terms of GPU hours, which is to say how many hours, if you sum up all the GPUs working out concurrently, did it take? It's like in the multiples of years, because they just have so many GPUs. So given these errors, there are two things you can do. Can scale horizontally, which is the right or scale vertically, which is the left. So what does vertical scaling mean? It just means getting a server with larger memory. So in this case, we have a 8GB RAM and 4GB of GPU memory, and we just increase the size of the GPU. So this is possible. You can just go from a consumer maybe 4080 to a B200 chip if you have the capacity. And then in that case, you can just overcome your memory errors in that regard. But then the other side of the story is called horizontal scaling. And what this means is that you have the same hardware, but you just multiply it. So if you have this setup and you quadriple it, then this is called horizontal scaling. And this is what we'll be focusing on in our course today. And just on this same vein, there's another two set of terms, which we should now discuss. One being data parallelism and the other being model parallelism. So we're doing data parallelism in this course, which is to say that the model does already fit in a single GPU. So we have one model on one GPU. But the data that we train on is split along multiple GPUs, whereas in the previous course I did micro-PP, which is model parallelism, the model is too large for single GPU. So in fact, in a simple example, if the model is twice as large as the GPU that we have, then we can split it in half, and then put one half on one GPU and the second half on another. In this simplified example, due to the activation memory and the other overhead that you do get when you're training a GPU, even if it fit perfectly on the GPU, then when you actually start to train it, you'll get out of memory GPU errors. So you'd actually want some type of buffer space, but in any case, I'm just illustrating the example where you can have in some instances a model split across multiple GPUs, which, if you're talking about the one trillion parameter model sizes in large transform based models, then this is a prerequisite. But once again, we're only focusing now on data parallelism. These can be combined, in fact, but we're just going to do this specifically so we can focus all of our attention on it, which is to say that the model fits into single GPU. So instead, we'll split the data across the GPUs. And now, we already disalbaged that data parallel means that we're splitting the data across the GPUs. So why is it called micro-dvp instead of just micro-dp? And in fact, these are both valid terms. However, nobody uses this term anymore, data parallel. And they all just discussed distributed data parallel because it's an essence, like a super set of data parallel. And I'll just go through all of the different classifications on what these two terms mean, really quickly. So in terms of the process model, a process, if we come down here, is an independent program with its own memory. And a thread is a lightweight unit of work within a process that shares the same memory space with other threads in that same process. Processes are isolated while threads are not. So you can think of as process as some overarching process or state, and then it can spawn threads, which are all shared, and they are temporary. OK? So when you have data parallel, you only have a single process with multiple threads. So you can have one GPU that splits threads along itself. And then in distributed data parallel, you have multi-process, which means multiple GPUs. And typically, one process per GPU, which means that you run a separate independent Python process on each GPU, which manages every part of its memory state, et cetera. And then in terms of machine support, as I said, it only works on one single machine since it's a single process. But for distributed data parallel, by definition, it's distributed. So it's multiple machine support out of the box. And then when it comes to model replication, the model is replicated on all devices on every forward pass. And then for DDP, the model is replicated once that startup, and each process has its own replica. So what this means is that when you start your training run, you need to put the model on all of the different devices, which you're going to train on, because you have a separate copy on each device. And because we have separate processes in distributed data parallelism, then we can just only once. But since there's only one process, then we have to do this at every single time in the forward pass, once again, giving high overhead. And then with respect to communication, it's on via threads. And in Python, the master process is essentially governed by the global interpreter lock, which is the central orchestrator of the processes that occur in the program. And this means that there's a bottleneck, because everything needs to pass through this one interpreter process, whereas in communication for distributed data parallelism, you can use collective primitives, such as all reduce, that run asynchronous C outside of the global interpreter lock. And then as you can probably infer the performance is much better with DDP, as opposed to the single process data parallel term. But with all that being said, it's slightly ambiguous. Like people refer to DDP as DDP from time to time, but just know that the main term used nowadays is distributed data parallel. And now we can look at some examples to illustrate what I just talked about. So distributed data parallel is actually all three of these. So let's go a bit larger. What is this one? This is a multi-server single GPU setup, which means that you have, in this case, four different servers with one GPU each, and they communicate with some interconnect. And then I'm just going to skip ahead to multi-server, which is the superset of the previous two. So in this case, you have multiple servers with multiple GPUs. So two GPUs with two servers. And once again, you have them communicating throughout the servers and within the servers, since there's two GPUs per server. And then you can also have a single server multi-GPU setup with distributed distributed data parallelism, which in this case is one server and two GPUs. This would have much less communication overhead, since you don't need to communicate between the servers. But at the same time, you are limited to how many GPUs can you fit on a single server. And for example, if you have a set of NVIDIA A100 GPUs or A200 GPUs, a single server of those is eight if I'm not mistaken. So that is the max scaling factor that you can do before you need to do multi-server distributed data parallelism. OK, so now let's go through the general workflow involved in a DDP training run. We're going to be talking about a lot of these distributed communication primitive stuff to video. And I have another video on my YouTube channel, which explains these in more detail. So if you would like more context, then go ahead and watch that video. It's on my neural key and YouTube channel. However, I'll try to make this self-containing so that you don't need to watch it if you prefer not to. So what is a broadcast operation? It essentially means one device will send data to all other devices. And this is how we initialize model weights at the beginning. So one node sends to all nodes. Here you can see that on the first device here, we initialize the value. In this case, our device has only one value, 0.1. And then we broadcast it to the other four GPUs. So the initial weights are sent to all other nodes. And then now we have the same weights. Because if you initialize all your weights independently, then your models will have different weights at the beginning. And then they will diverge. If you start four different models with different weights and you train them on the same data, you're going to get different models at the end. So even though the loss will be going down, the final outcome will just be different. So you'll be training effectively four separate models. And you're not going to be taking advantage of your distributed data parallel setup. And you'll probably lead to some weird divergence. I haven't had experience with this type of situation, but that's my guess that you would have some divergence because the models all have different data, sorry, different initial conditions. And you're trying to synchronize them every single optimizer step, which would lead to strange behavior. OK, after the broadcast step, we have a forward backward. And in this step, each node trains on different data chunks and then computes local gradients, which are then synchronized. So we run the forward backward step on the batch of the data. And then this gives us our local gradient. So these are really small values. But one is 0.8. This one is negative 0.2. This one is negative 0.7. And this one is 0.4. And then you want to sum these together. And then you can update your weights on your model. So how do we sum these together? It's with an operation called all-reduce, which sums the gradients across all nodes. And then distributes back the results to all nodes. So in this case, we would, in one interpretation of all-reduce, because there are many ways to implement this, we would accumulate all of our gradients on the GPU 0. So the first GPU. This is kind of seen as the master device. And then it will calculate the sum, which turns out to be 0.3. So if you want to calculate the sum of these four values, then it should be equal to 0.3. And then after this has been done, it will be sent back to all the other nodes with a broadcast operation, which is the same one that we did at the beginning, when we were initializing the model weights. So then we will update our weights with these gradients. So each node updates its model using the average gradients. And then what this means is that at the end of the training run, the model weights should all be the same, because they were updated with the same value that was the sum. And then the average of the gradients from each different batch. And after that, you repeat the process. And if you have a specific convergence criteria, you'll stop at that point, or just after n steps, whatever, that's the point where you'll stop the training run. And you'll take those model weights and use them for whatever you desire. So this is the entire distributed data parallelism play look right here. And this is what we're implementing in this video. But just to stress a bit more on the communication derivatives, since I want to motivate this more than just a simple example, let's talk about different ways to do these primitives and how certain methods are more or less efficient. So when it comes to the reduced operation, which once again is to sum up all of a specific value at one device, there are many ways to do this. But the most efficient way is the following. And it's logarithmic. So let's say that we have these eight devices here. Instead of just sending everything to one device, which would be bottlenecks by communication, we can have every single device send to its neighbor and then sum at that neighbor. And then that neighbor, so we'll take rank six, that neighbor will sum its value and then send it to the two step away neighbor. And then that neighbor rank four will send it to GPU of rank zero. And at that point, GPU rank zero has the sum of all of the gradients. And that only takes three steps. So it's actually logarithmic in the number of devices that are in your network. And you might be thinking, well, we can actually just send all of the gradients at once to GPU zero. And then it can sum it in one step. But the issue is that, as I said, you're bottlenecked by the bandwidth of a connection between a set of GPUs on a network. And this means that if you send 7 GPUs sending to one, the bandwidth will be one seventh of the pure bandwidth because you're sending seven devices to one. Whereas in this example, the bandwidth is the full bandwidth. Since you're only sending device to device, not multi to one device. And in this way, you can achieve a much faster, all-reduce primitive routine. So this is just one example of how we can improve the efficiency. And then now, when we do the reduce, we need to do a broadcast to send it back to all the GPUs. This is the same exact story. You can try to do a point-to-point inefficient communication scheme where you either have this GPU send to all of the seven GPUs at once. But then because this GPUs outward data bandwidth is caps, it can only send at one seventh of the speed to all of the GPUs simultaneously. And then if you try to send it like one by one, then you'll have the full speed. I can send to this GPU, then this one, then this one, then this one. But then you'll have to wait. In this example, I'm assuming that each time step for sending the entire set of weights to a one GPU is five seconds. So if that was the case, it would take seven seconds, right? Sorry, 35 seconds, which would be five times seven. So we would send it seven times, and each time takes five seconds. So 35 seconds. And then also in the case where you split it up along all the GPUs, well, it would only take one step, but it's seven times slower than the pure speed, which is five seconds. So if it's seven times slower, then it's five times seven, thirty five seconds again. But then if you come into the inverted pattern that we did in the reduced scheme, what are we doing here? We're sending from rank zero to rank four, which is only five seconds. And then rank zero to rank two, rank four to rank sinks, which now has taken ten seconds in total from start time. And then finally, each neighbor sends to its adjacent neighbor, and this took 15 seconds, which is much faster than 35. So I hope this illustrates some intuition on how we can improve from a naive communication perspective or primitives. And now we can talk a bit more about some other aspects of improving pipe towards distributed data pralism. And the first one here is overlapping computation and communication. So what does this mean? Well, if we come down into this diagram below us, which skips ahead of it to bucketing, we'll still get the motivation for what gradient hooks are. So let's just read what it says. Gradient hooks trigger all reduced immediately when each gradient is ready. Communication overlaps with gradient computation, reducing idle time. So here we have the computational graph of a neural network, where this is the input to the neural network, which may be like an image. And then we pass it to the layers, we get an output and we get a loss. Normally, when you're doing distributed data parallel training, you'll go through the entire network, in this case, five layers. And then you'll calculate the output, then you calculate the loss, and then you'll do the all reduced here of the gradients that you calculated in the, oh, sorry. Then you'll do the backward pass. And once the backward pass is propagated to the inputs, you'll do the all reduced of those gradients and perform the off the step at this point. However, this does not overlap communication and computation at all because you do the entire forward backward. And then you communicate the gradients, whereas if we separate the layers into buckets, in this case, layer one is bucket three, layer two and three is bucket two, and layer four and five is bucket one, then we can have overlapping communication and computation. And you might be wondering, why are we naming the buckets in the reverse order? It's because in the backward pass, which starts at layer five, as soon as we've calculated all the gradients for layers five and four, in bucket one, we'll have a gradient hook, which will communicate all of the gradients along this first bucket on the different models. So if you think we have two different models, then we'll have a bucket on model one for layers four and five and another bucket on model two, for layers four and five. And then before we even compute the remaining buckets, we can already start to compute those gradients. And in this way, once again, overlap communication computation. So what's the difference between hooks and buckets? Buckets is just a generalization of hooks where you can group multiple layers together so that you can amortize the cost of splitting up the communication into small segments, because even though you're overlapping communication computation, since you're sending smaller buckets of gradients, the communication overhead is larger, because essentially you have to still send the data over multiple times, and instead of sending it once, which will have the lower send receive overhead. So all this means is that if you bucket into larger amounts of layers, then you can reduce the cost of the communication over it. And in the naive case, where you have a gradient hook for every single layer, then there isn't really bucketing, right? Because every single layer that's computed, you immediately trigger the all reduce. And then this would just be great hooks without bucketing, but most always gradient hooks are combined with bucketing to have an efficient communication computation overlap. There's one other interesting thing, which is just not even particular to just stupid data parallelism, but any form of parallelism. And this is if you have a failing node in your training run or something happens, which crosses the run. In a canonical, the serial data parallelism training set up, you'll just treat the weights on rank zero as the checkpoints and the source of truth. Because let's say that you have a run which crashes, then you probably have, in the case of four devices, right, four sets of weights, which are slightly different if you hadn't yet performed all reduce. So we just will define rank zero as the master node, even though, in this case, we never use this in the implementation. It's just still a useful thing to note is that you should set one of your devices as the master node such that if there's ever a failover, then that's the one that you check point based off of. OK, so there you have it, all of the introduction. And you should already know how a pretty good grasp of what the distributed data parallelism represents. So if we now come back and go to the next item, it's called manual, which means, or what says, manuals at the batch across two GPUs and average ratings. So this just links to the course next step, which is to open up. If I come and just open up the sidebar here, one second, there you go. So open up step one manual and let's increase the text. Of course, it's quite small, but coming to do that. Let me first explain what this is. And in fact, before we do that, I should probably run the manual example, which we have here. So manual.py essentially simulates two GPUs manually and performs distributed data parallelism on those GPUs. As if we're having pie towards implement back and for us, but actually everything is done manually. So let's just open up a terminal. You can see that we're activating the virtual environment that's in our microdDP directory. And because we have the virtual environment, we can just run Python 3 or whatever Python version you have. It's at the Python and Python 3 usually. Python 3, src, manual.py. And this just sets up a training run and I'll explain how we get these updates. Once we calculate it, for some reason it's taking some time here, but it should trigger pretty soon. Maybe it's some backend process that's occurring. There you go. So just as we can see here, there's 50 steps of a 16 layer neural network with a width of the individual layers being 128. And the batch size being 32. And you can see the loss starts at 0.7. This is cross-entry loss. Cross-entry loss and then ends around 2.1, taking 0.293 seconds. But what are we actually training on? It's kind of funny. We're training on random noise. So you can see here that the input data is torsht.randen of size batch size by hidden dimension. And what this means is that we're literally defining a set of 32 by 128 vectors, which are just randomly initialized. And then we're training our neural network to learn this random data. So we're learning random noise. And why am I doing this? It's just a toy example to show that the network is learning something, even if it's noise. And this is as opposed to like maybe we could run this on M-ness or something like this. But just because we want to have a faster iteration cycle, you can literally learn random noise in 50 steps on a small enough network. So it just shows us that it is working. But it's not really learning anything useful since it's just for educational purposes. With that being said, now let's just go through the code from the top to the bottom to explain what it means. And then we'll implement it ourselves. So we just set a manual seed for reproducibility. And then these are the two devices that we instantiate on our own. So device one, device two, it will be a coded device if you have it, else CPU. So this is just a standard instantiation of two devices. And then we want to initialize the amounts at the same seed. So I come ahead and re-instantiate manual seed to 42 here. And why am I doing this? If we've already done it up here, it's because after we set up model one with manual seed of 42, this now means that the random number generator has stepped to the next function or to the next number, right? Because the random number generator, depending on the number of times that you call it, it is deterministic for that seed. So because we want to later test this and compare it to our distributed data parallel method, which will have manual device, sorry, model one, instantiate on one process and model two instantiate on a separate process, when that happens in the distributed data parallel on case, those two processes, they will have just model equals full MLP, not model one and model two, because it's being instantiate on a completely separate process, which means that both of those models will get the manual seed at the very beginning, as opposed to here, if we got model two, without re-initializing your manual seed, then it would be a different set of weights for the random number generator. This will be clear when we compare this manual method to the actual data, distribute data parallelism method, but just keep that in mind for now. So as we say, it says reset seed to model two as the same initial weights as model one. And then very simply here, we'll initialize two sets of atom optimizers on the parameters of their respective models with a pretty standard learning rate of 0.001. Then we need to switch the batch across the GPUs. So in this case, we're going to discuss that we're generating these random sets of batch size by hidden dimension matrices. And then we also need to target. So in this case, we're just doing a binary classification of the random noise. And what this means is we have a set of random integers that is either 0 or 1, because this is the inclusive value on the left and exclusive on the right, so 0 to 1. And then we get the vector size being batch size. So if I just come here into the examples in the PyTorps documentation, in this case, we have 3, 5, of 3. So we'll get a set of random numbers between 3 and 4 of size 3, in this case, 4, 3, 4. Or here, if you have numbers from 0 to 10, sorry, 0 to 9. And it's a 2x2 vector, then a possibility of 0 to 5. So there you go. Now, because we are splitting up our batch across two devices, just as is the case with distributed data parallelism, right? We're assuming that let's say one batch is too large to host on one device. Then we will do the following slice, which is to say that input 1 is from the beginning of the input up to batch size divided by 2. In this case, the batch size is 32. So it'll just be from the batch size from 0 to 15. And then it will be from 15 to 31 on this end of the spectrum. And then it will be from 15 here on the input for 2 up to 31. And this will be put on device 2. And then if we come to the target, the target will, the same thing be the first 16 elements, which is from index 0 to 15 on this fixed target. And then for the second GPU, it's going to be from index 16 to 31. And then you have a pretty simple training loop here. So we have the timers. That starts, we put it into training mode, which if we look at the definition here, what does it do? It turns off, drop out, and batch norm, if I'm not mistaken, I could be wrong here though. Just to know that it makes the training more efficient. And then if we come back here, let me just reduce that. There you go. For step in range steps, which in this case is 50, we'll first make our gradients equal to 0, since we don't want to accumulate the gradients in the previous step. They will do a forward pass, which is just calling the model itself. I actually haven't shown this. So let me just open up the model, which is in our SRC folder, model.py. Let's have a quick look at this. So in our full MLP, we have a model of dimension 128. And the depth is 16. Since there are 16 layers, we'll mean that we'll initialize it in manual.py. So you can see the total layer 16 hidden in 128. And what is this network precisely? Well first, we initialize the parent class, which is the nN module, in order to have parameter and device management by default. And then we will generate the number of layers, which is 16 in this case, which is composed of a fully connected linear layer, followed by a rectified linear unit activation function. And then at the very end, we'll have one model head, which is just a binary class fire, which is to say mapping from 128 to 2 in the dimension space. And then we'll define this as an sequential of this layers argument. And then this star here is essentially an unpacking operator. So it'll take all these layers, which is in a list, and then it'll unpack it, so that it can be received by the sequential class. And then we define the loss as the cross entry loss, which is the standard loss we use for binary classification. And when it comes to our forward pass, it's pretty straightforward. In essence, since we've defined our net here as a nN.sequential, we can just call that the input on the net, since it has a forward by default implemented. So we call the forward pass on our input x. And then if target is not none, then we will return the loss function of those logics in the targets, which is one single cross entry loss. And actually, this target feature here is unnecessary. So I'm just going to simplify the code right here. I just realized that, yeah, we had no need for a specific target to be done. It's always the case that we do a full model pass in every single device. So in essence, we calculate the logics, and then we determine the loss over those logics. And this is the forward pass. It's very simple. Okay, you can even input the forward pass in your training loop, instead of within the class of the neural network. In that case, you would just do self.net and then self.loss. But since the loss function is a element of the class, it's better practices to do this within the forward method itself. And then we can call it simply on our network, which is done. If I come back down to where we were, let's see, which is done right here. So we calculate both of our losses on the models with the inputs and the targets. And then we perform the backward pass in order to calculate our gradients. And then we do the following. So it's manual gradient averaging across the two models. And right here, we have with torched.no grad. Why is this done? Because we want to avoid doing gradient tracking. And why would there be any gradient tracking? In this case, when we're adding these two gradients of P1 and P2, which I'll explain very shortly, adding this will create more notes in our computational graph. And this will lead to undesired behavior in our training run in terms of the Python back, PyTorch back end. So we just want to set PyTorch dot no grad, which disables gradient calculation. And instead, we're just calculating these gradients simply to get the average gradient. So let's see what's done here. So let's say for P1 and P2 in the zip of model 1 parameters and model 2 parameters. So first of all, this stands for parameter, parameter 1 and parameter 2. And then what zip does is it makes a iterator. It yields a tuple until an input is exhausted. So it essentially yields a tuple of parameters. So we can iterate over both parameters at the same time. So in this case, if you have model 1 and model 2 parameters as the two inputs, then we'll get tuples of the parameters. So let's just generalize that there's only two layers in the neural network here. Then we'll get two tuples. One tuple is layer 1 of model 1 and layer 1 of model 2. And the second tuple is layer 2 of model 1 and layer 2 of model 2. So every single time, we're going to add these two gradients since they're from the same layer. And then we'll divide them by 2 to get the average gradient across those two layers on the two models. In the case where you have more than one more than two models, then you would just do this for like P3 and then add the model 3 up parameters in your zip expression. And then what we're going to do after is call p1.grad.copy the average gradient. So essentially, we're copying the gradient that we calculated here, which is the average one between the two into the respective fields of parameter 1 and parameter 2. So we're saving back the gradients on the devices themselves for that layer specifically. And it's worth noting that this is the manual approach, right? We're wanting to do this with the communication primitives that we discussed in the intro. But since we are learning this first manually, then this is how it's done in code. And then you just do the off-carser step to update the model weights with those gradients. And after that, well, we just print our loss to see if the loss goes down and we can define a loss. So, without further ado, we can just run this right away. So it's just in SRC and we run manual and we should get a response. So you can just are there with UV run or just Python 3. And here you have it. We have our response or rather our final loss, which is 0.10686. So now we're going to actually implement this with the skeleton code in our Mywork directory. So I'll take the time now. I haven't mentioned it. But the Mywork directory includes all of the code that we're going to write together in this course. And we're going to start off with manual, which is essentially what we just talked about in the example. But this time we're going to be implementing the key components on our own. So let's start off here. The rest is just initialization, right? The hyperharmoners in the manual, which you've seen before. But the first step is going to be to set the specific device that we are training on. In case you have GPUs, and this will actually put it on each of the two GPUs. So coming here, we can simply do torch.device. And this is either going to be CUDA 0. This should be a string. So what does CUDA 0 do? It would initialize device 1 on the first GPU. And this is only possible if torch.cuda is available. So this is essentially a Boolean function, which tells us whether or not we actually have a device. That this does not essentially give a error if you try to force it on this CPU to GPU when you don't have one. And then when we don't have it, we'll just say LCPU because everybody has a CPU. This is the safe base case, which you can always assume. And then in this case, we're going to do device on CUDA 1 if torch.cuda is available. And if you really want to be safe here, because this expression may be true, but you may only have one GPU, then you also need to do and torch.cudaDeviceCount. So I get a little confused here by this popup, CUDA.deviceCount. This is a function which returns the number of GPUs available. This should be greater than 1. So if these two conditions are true, then we'll have, oh, I just realized we need to call this device 2. Then we'll have both devices on the GPUs, if not, they'll be on the CPU. And it will just be represented on those devices as if they're on two different GPUs or whatever accelerators that you're using. But in this case, it's just going to be on the CPU. I'm running on a Mac that does not have any Nvidia GPUs on it. So in that case, we're going to see this work on the CPU, which is fine because the model size that we're dealing with is nothing extraordinary. I guess the total size would just be 128 by 16. So this is, yeah, not a big number, to the power of 10, I guess. So now that we've set up the devices to correspond to the correct machine on our computer, we can come down and see what's next. So that was the stuff I've left that, the simple, I've left that the same. But now we need to split the batch across GPUs. So you can see that we have our fixed input and our fixed target, but we need to split it now such that each device gets the first or the second half, depending on which one we're on. So in the case of GPU 1, we're going to take the first half. So we're going to get the fixed input here and we will slice it. And what we want is to come here and take the first half of the batch size. So it's just going to be batch size divided by two. And it's 32, but we should still do integer division because you can't have a death, you can have a floating point batch size. And then when we come back to the target, it's the same process. So we'll just go fixed target and then assign this to the first half of the target, which is once again a vector of 32 values, either 0 or 1 for this binary classification task. And one other thing that I am failing to recognize is that we should move these onto the proper devices because we just initialized our devices here. So we're going to do that too. And then device one in this case. Okay. And then this one will also be on device one. So why not just copy it like that? Okay. So we put these on the first two devices and then coming here, it's the same procedure except this time, we will put the second half of both the input and the target if I can spell it properly. There you go. Fixed. Oh, I can't spell it properly. Fixed input and then batch size. Come down here, divided by two with integer division and then slice it such that we take the value which in this case would correspond to 16 and then go to the very end of the vector which would be 32. Sorry, 31 in this case since we're doing zero indexing. And then we're going to move this to device zero to device two rather than why I'm saying device zero. Boom. And for sake of simplicity, let's just copy this and instead put fixed target for the second example. Boom. So that we've mapped our inputs and our targets to the proper device and have had the proper batch procedure. We can move on into the trading loop and the thing which we are implanted here is the manual gradient averaging itself. So if you remember, we had to first start off with the with torch dot no grad so we don't do any gradient calculations that are saved within the computational graph of PyTorch. And then we will iterate over the two parameters in our model. So once again, our model is called another one model two. What we're going to do is to say for P1 comma P2 in we're going to use a zip expression once again which yields and length tuples where n is the number of interbrose past as position arguments to zip. In this case, we're going to take model one dot parameters and what this does is it returns to the iterative over model parameters over module parameters. And in fact, when we do this, if it loads, yes, this is returning in iterative which is exactly the type of input that the zip function takes. So just coming down, we've now put model one and model two in the parameters and then we will simply average the gradients. So we'll say average, how we called it in the example, but that's an important average grad equals P1 dot grad plus P2 dot grad that we've trained. And then we're going to divide this by two boom. And then we now need to copy this down into the two respective values using the grad dot copy method of a PyTorch parameter. So we're going to do a grad and then since it's in place, we need to do a underscore before the method, before the name of the function itself. So P1 dot grad dot copy, oh, I forgot, sorry, the underscore comes afterwards. So copies the element from SRC into self tensor and return self. So essentially what it means is that we're going to copy this value, which is average gradients into P1 into the gradient field of P1. And then it'll also return average gradient, which we actually don't care too much about. We don't need to accept that as a return, but it doesn't matter since all we care about is placing that back into the function for the specific parameter so that the parameters are updated and updated the same so that they're both average gradient and the average gradients. And I think that's everything for this implementation. So if we come here now and I go Python 3 into my work directory and then run step 1, which is manual.py, hopefully we'll get 0.106. Oh, okay. So we got 0.231414 and this is a cool learning opportunity. In fact, if you pay attention, 0.106806 is double or rather half of this value here, okay? And why is that so? It's because when we are printing the average loss here in step 1 manual, we're doing it as the average and then printing the average loss. Whereas if we come into the manual.py, actually I haven't just so that it prints the loss of the first model, which is different because it has a different set of training data. So just to make things really simple, I'm going to copy paste this new way which we're printing out the loss and then paste it here. So it doesn't change anything in particular. We're just printing loss 1 instead of the total average loss. And now if I run this once again, it should hopefully give us 0.106806, okay? There you have it. And just as like an interesting thought experiment, let's see what happens if we don't set back the average gradients to the two models. Then we should get something else. Maybe the model won't learn. I'm not sure. Okay, so the model still learns because in fact, it turns out it takes a lot to make a neural network not learn, but it doesn't get the same loss as suddenly higher. But otherwise, yeah, we can see that it still learns in certain respects. So if you don't reset the gradients to the average gradients after every single forward backward pass, then maybe it's going to be a silent bug that ends up killing your program over time. So just make sure that you watch out for that. But if everything is that properly, then you should be getting this value instead. So that's really everything that we have for the manual approach where we literally did to shoot data parallelism without any of the communication parameters that we just learned. And now let's actually start to implement those and see what that looks like in practice. And the way that we're going to start off to see how we can play with these distributed communication primitives is by coming to the sandbox. So in that instance, we're just going to be playing with all of our operations. And this is just step two in the My Work directory. So if I open up our sidebar and come to step two, click on that, then this is what we're going to be working on for the next few minutes. So step two sandbox for the all of our operations. This follows the experimenting with the pie-throughs distributed primitives. And we can run it with this command. So if we run it now, then it probably will lead to an error because there's going to be unimplemented work. Oh, okay. I guess I have it set up so that it doesn't error out even if you haven't implemented things yet. So let's just go through and read what it says. Okay. And before we jump into the exercises, I realized that we haven't discussed the import here, which is quite significant from SRC.com's import this library. And if we just open up our oops file, which is comms.py, I'll demonstrate what this actually is doing. So there you go. Now we're in comms.py, which has a in distributed method, which if you read it, initializes the as we have the docs from here, the decision post group from tors run environment variables. And it returns the rank, world size and device. So rank means which GPU are you in the network? So if there's two GPUs, one would be rank zero, and one would be rank one. Then the world size in that case would be two. Since there's two GPUs, and then the device would be like, are you on a CPU, a GPU, the device type? Okay. And whenever we run with torch run, so just any command where you have torch run is going to automatically export in your environment, the following variables that it's visible in the shell that you're running it in. So it will export a rank and a world underscore size variable. And you can just retrieve this with the OS package in Python. So import OS and then just OS dot environment, which will just grab any environment variable which has this name. And this is our rank. And this is our rank. And then we'll also grab the world size, which is just from here. Then what else are we doing in this in this method? Well, we're determining which device we're on. So this device, current accelerator just returns whatever pie trips device you have. One thing we're noting is that if we have the device equal to MPS, which is the case if you have a silicon M1 plus chip on a Mac, then we'll set the device to CPU because it turns out that the distributed communication primitives like reduce all reduce, et cetera are not supported by MPS yet. So we can't take advantage of it if we have a Mac silicon backend. And then we have a really long method here, which is called torch.sup.get. underscore default underscore backend for device, which returns the default backend for the given device. And in this case, does it give us examples? No, it doesn't. So for the types of backends that you can have, there is nickel or NCCL, which stands for NVIDIA Collective Communication Library. And there's also glue GLOO, which is the standard backend for CPUs. So actually don't know what glue stands for. So let me just ask cursor, what does glue stand for? So that's the really nifty feature in cursor where you can just ask in line anything that it means. Who does not stand for anything? It's just the name of Facebook's collective communication library for CPU and GPU tensor operations in PyTorch for distributed training. OK, so it doesn't have anything but the CPU backend is glue. So there you go. Otherwise, we then call this method, which is init underscore process underscore group. And it initializes default distributed process group. So in a sense, init distributed is a wrap around this function, which determines the backend, the rank, the world size. And then we'll initialize that so that the devices can communicate amongst each other in our distributed data parallelism training setup. And then we're going to return these devices simply back to whoever called it. And then the cleanup method is just distra, distra, distra, distra, process group, which very simply destroys a given process group and de-initializes distributed package. So if you have some communication network that's established between multiple CPUs, we'll after you're done using them, it's best practice to destroy it so that the proper procedure takes place. Like, I would compare it to like, if you have a USB, you should eject it before you take it out, before just ripping it out. Probably nothing bad will happen if you just rip it out, but this is the best practice right here. OK? So with that being said, we can now return to step two. This is not step two. This is step one. So I'm not sure what happened here. There you go. And then right now that we know what the communication stand for and now that we have the rank, the world says in device, we can come in and implement all reduced with both the sum and the average. OK? So as I was mentioning, we have these two tensors, if we have two, and how is this initialized? Well, we have the rank that we got above here. And initial value will just be rank times two plus one, which is to say that if we have rank zero, then it'll be zero times two plus one. If we have rank one, then it'll be one times two plus one, which is four, and so on, that we can have a set of tensors, essentially, that are just the consecutive numbers in the whole number line. And then we just initialize the tensor with the three values, float initial value. This is not really elegant. Now look at this. I could have done this with a for loop or didn't arrange. Actually, let's do this right now. So it's a tensor of four. So let's say float. We're going to have to do a list compression here. So it's going to be float of I for I in range three, and it shouldn't be I, it should be initial value. So let's just say initial value. And then I'm just going to remove this so that we get it all the way. All of this craft. I don't want to do everything. I think if I just go like this, that should be it. Okay, and I need to fix something because this isn't look right. So it's going to be float initial value plus I. And now what is wrong? Why is it mad at me? I don't know, it's not mad at me. So the tensor is now going to be float of the initial value, which in this case would be zero for rank zero plus I. Sorry, the initial value will be one because we add one. And then we do this three times for each device. This may break, but we're going to find out together. And then if it does, we'll just reset it to what it was initially. And then we just have a bunch of print statements here, which just prints the initial tensors. And then the rank and then we have to do all reduce and then afterwards the rank of the tensor. And one thing you'll note is we only print after all reduce if rank equals zero. And why we do this is because if not, then we'll print everything twice because as I mentioned earlier in the tutorial, in essence, when you're using torquetron, you're spawning multiple processes, which are, which are completely independent. So if we have like four processes, they will print the same thing four times. And I'm just going to demonstrate this right now for you. So I have this script, right? And if I just comment out everything because I don't know, it should be fine. Let's just actually, I think I need to comment out everything because there are now some things that will break if it's not commented out. And if I don't put this in indentation and I run this script, we'll get a interesting phenomenon where it will be printed multiple times if we check. So you can see here, right? Since I have this actually set up for four devices, right? World size equals four. It's printing initial tensors four time. One, two, three, four. But then if I come back and I uncomment this and then I end up these, then it should only print it once. And there you go. It only printed it once. So this is important just for logistical reasons. We don't want to always see the same thing however many devices that we have in our network. So just keep that in mind. And now we actually have to perform the all-reduce. So if I click this line, first of all, the all-reduce is implemented in the Python disk package, the PyTorch disk package, sorry for the name over there. And it's just called all-reduce. So what it does as a reminder is reduces the tensor data across all machines in a way that all get the final result. And we actually need to define the operation here. So if we do a sum operation, then it will sum all the tensors across all the devices. And then it will return back the sum to every single device. And in this case, what the inputs are is first the tensor. So I think we have this initialized here as just tensor. And then the operation is slightly more sophisticated how we write it. So we have to come here into the example and it's op equals reduce op dot sum if we want to do a sum. So let's just call that. So op, what I do op equals reduce. I said, I think I got it wrong. What was it again? I apologize. Oh no, reduce op dot sum. Okay. Because it's a reduced operation and then we do the sum of that. And this will take the sum over the tensors in our setup with four devices. But it's still giving me an error. And I'm not entirely certain why. Oh, I believe it's because this is in fact from the distributed library as well. So we need to do this dot reduce op dot sum. And now let's just test this out and see what happens. So coming here and commenting these out. What are we going to get? Make the terminal slightly larger. Oh, okay. We need to do some all capital. So thank you for the tip there. And let's run it again. Okay, cool. So right, we have four devices. And we have some jumbled up stuff here. So I need to check out why we're printing that. Here you have all three ranks and it's one, two, three, four, five, six, seven, eight, nine, and 10, 11, 12. You'll notice that the three and the two here are inverted in the order because in fact, what happens is that since these are all running asynchronously in individual processes, the device, which is on rank three, arrived at the print statement sooner than rank two. So it printed it before. It doesn't have any notion of like, oh, rank zero is the first rank. So we should print it first, whichever one terminates first is what is printed. And then I think we're just printing rank three and rank two. Is this correct? Let's see. So we print each tensor in the initial stage. And then we do the after all reduce. So let's see. I see what happened. So the rank three and rank two are actually so fast that they printed, they computed the sum before the after all reduce line was even printed. And what we can do here is implant the following method. And it's called this dot barrier. And then this will make it such that these two print statements have to happen after the all reduced print statement. And what this, what this introduces is it synchronizes all process. This collective blocks processes into the whole group emphasis function. And then once it enters that function, then we can continue on in our evaluation. So if I rerun this now, all of the three of the four print statements should happen after we have the all reduce print. So let's see what happened here. No, that is not the case still. So we're going to debug this together because that's what I said at the beginning of the video. And let's figure out why that's happening. I think it is because we should, let's try to put this before the all reduce. I think this may be the, the crux here. Right. So why was that being the case before? I believe, yeah, since, since some of them still computed all reduce, then it seems that for whatever reason, they were skipping along here. In theory, that shouldn't be, that shouldn't be possible. But I am also not an expert in the pie torch collective communications backend. So just to be on the safe side, we can see that this is the better way to put it after the, I'll reduce, I say, before the all reduce computation. And now we have the, the final result. And if we just do a, a sanity check rate, one plus four is five plus seven is 12 plus 10 is 22. And then this one is 26 and 30, which makes sense. It's four larger than this one. And this one is also four larger because we are essentially incrementing by one on each tensor. And there's four tensors. Okay. Now, if we want to do the average, it's quite simple. We just changed us to a, a, vg. And then we should have the average. So let's see what happens now. I'm not sure what the averages of these numbers are, but here you go. So it turns out to be. And if you'd like to verify this, then please be my guest. And now we can see once again that we actually have the after all reduced not being respected along the different devices. So the distout very seems not to be working. And I can't really put a reason why that's happening. But this is just goes, this goes to show you that in these distributed communication libraries, when things are happening acrliciously, you can't guarantee that a specific operation will happen when you want it. So that just needs to be considered when you are dealing at this level of our abstraction. Of course, in this tutorial, right, we're building these things from essentially the ground up. When you're using this in production or in practice, you're using the pie towards optimized code. This is not to worry, but it's cool to realize that these are actually the factors which are coming into play when you're implementing these routines and these computations at this level of abstraction. So if you're curious, right, you can just come into the reduced operation and then see all of the other options that we have. There's max min bxor, I guess, binary xor is my guess of what that would be. I'm not actually sure. But I mean, let's try to run it and see if it gives a scenario or not. I'm not sure what that completes to. Okay, so I maybe this one is only valid on zero than one. So I'm not entirely sure on this one, but let's just continue on to exercise. Oh, it should be called exercise two. Okay, so this is a broadcast. And if we remind ourselves broadcast sends the value from one rank to all of the ranks. And here, I just realized we have everything commented out. So here we're saying that we have the value 42 on the rank zero. And then if you're not on the rank zero, then you are the, I don't know why it's called 10 to three. I think it's because this is supposed to be exercise two. So it's called to be called 10 to two. And then this one, let's see, I don't know what's called 10 to four. Let's change this to 10 to three for consistency. And then what happens here, once again, is that every single device, other than device zero has the value zero and then device zero has a value 42. So we're going to print the broadcast exercise only for rank zero. So that is not duplicated and then print each rank. And then we get to do the broadcast and after that print the final result. Okay, so this is also pretty straightforward. This dot broadcast, if I'm not mistaken. And then this takes the following parameters. It takes tensor as the first parameter. And then it also takes the source. So the source in this case is the GPU zero, right? Because we are broadcasting from GPU zero to all of the GPUs. And let's see, just if this runs, I am not sure if we have a bug or not. So okay, it's the fact that down here, we have an initialized code. So just to make sure this is working properly. Boom. Hopefully this works now. All right, cool. So this is actually exercise three right here broadcast. And at the beginning, we have an interesting phenomenon. It's the fact that the other ranks, they still have the mean from the previous exercise. And I see what's going on here. In fact, we have the devices printing their outputs for exercise one during exercise two. So maybe for the best interest of everybody. And let me just rename this. We should comment out exercise one so we don't have this superimposing on our exercise two. And now if we put this back again, we can see that we get an error, which is the following. Let's see. Did I uncomments something that I shouldn't have? You can see that these, okay, tensors are defined. I didn't mean tensors two. Thank you so much, PyTorch. So coming here, tensors two is undefined. Oh, and now it's actually then inputting the tensor from exercise one and exercise two. So that's not great. But now, cool. So coming up here, right, we have rank zero, having the tensor 42. And then all other ones are zero before. And then after everything is 42. So there you go. And just as a sanity checker, if we don't do the broadcast, then they should still remain zero afterwards. Let's try that right now. Okay. And you can see that after the broadcast, rank three is zero. Hold on. Yeah. We have rank three zero. Once again, we have some weird mixing between the different exercises. But yeah, rank one, rank two, and rank three are all zero. And then rank zero is 42. So there you have it. The broadcast is necessary to allow the synchronization and sending the weights if we are talking about the GPU to the different devices. And now let's just uncomment this. And then go ahead, looks like somebody is pretty mad on the road now. And then do the final exercise, which is reduced. So in this case, we're sending all ranks to one. And this would be the case if you have all of the devices wanting to send their weights to a central device, which is going to compute the average gradients and then send it back to all the other devices. So in this case, what are we initializing? Each tensor is float of the rank itself plus one. So tensor, I'm sorry, GPU zero should have a tensor of just the value one and so on. And we'll see this when we print it. But for the to do, it's not going to be terribly difficult. We're just going to do this dot reduce. I'm not mistaken. And then we need to specify our source. So first of all, it's tensor three. If I'm not mistaken, let me stop saying that tensor three. And then the source or rather the destination is going to be zero, right? Because we want to send something. We don't need a destination. We don't need a source. We all need this destination. Okay. And then we have the cleanup here. As I mentioned, this will destroy the process group. Otherwise, this should give us all of the values onto the tensor three, which is on to device zero and it's tensor is tensor three. So let's just try this out and see what we get at the bottom of the screen. Okay, exercise four. So once again, you see that rank zero is one. This one is two. This one is three and this one is four. And then after reduced to rank zero, we have the following, which doesn't not seem correct. So we have rank zero being ten, which is true because we have one plus two is three, three plus three is six plus four is ten. And the thing is we have rank one, two and three also changing their values. So this is very strange. Let's see why this could be the case. Are we doing some computations up here, which could be affecting that? No. Okay. So this reduces the tensor data across all machines, only the process with rank this is going to receive the final result. Okay. I can only imagine that during some internal operations, the values will be saved and instead not cleared back to the original value when we're doing the computation, I think that's the truth. So what must be happening here is, for example, right, rank three is equal to four and all right, rank three actually doesn't change, rank three is equal to four, but rank two does change. So this must mean that we are first summing rank two and rank three into rank two and then summing that into rank one and then summing that into rank zero, which is ten. Otherwise I wouldn't have any clues on to why this is the case, but if somebody wants to actually come into the reduce operations and see why we have this modification of other values, which is not the destination, then I'd be happy to see what the response is from that, but we just learned something new about pie-tourch collective communications and it's the fact that even if you're doing a reduce on the destination, note of zero, the other notes will also be affected in their value. So this is why, for example, if you're doing a training run and you don't zero your gradients in between your runs, then you're going to get undefined behavior if you're not accumulating gradients, that is the case. So there you have it. We've done the three exercises here. That was a bit rockier than I expected, but I hope we still learned something I definitely did. So now if we come back into right into our course directory, we have all reduced next and let's quickly go over this. In essence, we're going to talk about a few concepts before we rock the code itself. So let's first discuss the communication overhead of a few different all-reduce algorithms. And in this example, we are assuming n-ranks in this case three and we want the sum or average depending on what you're computing for the all-reduce in this operation. So if we do all-reduce sum on this sample, then this is the result. And for the communication overhead that we're going to discuss below, we want n as the number of ranks and s as the size of the tensor. Okay, so if there's in this case, right, it would be three, in fact, the size of the tenser is three s and also there's three ranks. So let's talk about 9 all-reduce first, which is when you send all ranks to rank zero as the master rank, it sums it and then it broadcasts it back to all of the ranks. In this case, the communication overhead per rank is the size of our data, which is s times n minus 1, because in both the send phase and the, which is called the reduce. And then in the broadcast phase, we are either sending all to rank zero or rank zero sending all to the other node. So in this case, there's n minus 1 operations for size of s at a given moment. And then this would actually be two times n minus 1 times s if you consider the entire operation, but we're just doing per rank in this case. So it's order of n times s. And then there's an interesting algorithm called tree all-reduce, which is a binary tree topology. And in this orchestration of all-reduce, instead of having your nodes linearly arranged and then they're sending to each other, for example, all at once and then returning all from the same node, you have them organized as leafs. And in this way, there's one note at the very top, which is going to receive the sum from all the leaf nodes. And then at the very top of the tree, that will be the sum. And then you will distribute the sum back to the leaf nodes in order to broadcast the result back. And the interesting thing here is that it's log of the size of the number of nodes in your cluster in terms of the complexity of the overall communication. But then if you talk about per rank, well, if you're a leaf node, then at one given moment, you either send, which is size s or you receive. So it's 2s for a single leaf node. And then if you are the root node, which is to say at the very top, well, you receive from 2 in the bottom and then you send back down to 2 better below you. And then if you're an internal node, well, you receive also from 2 children below you. And that is equal to 4s, right? And then you will also send up to the parent above you, which is 2s. So these are just a few examples of the complexity of the communication. And then the last one is ring all reduced. So how this works is instead of having our nodes linearly, it's in a ring. And in this way, if each node passes to its neighbor, then it takes n minus 1 steps in order to circulate the sum across the entire ring. So just to explain this in our text here, it says that the tensors put into chunks and on each of the ranks accumulate a partial sum for one chunk. So if you have three nodes, in this case, right, then rank 0 will accumulate on the first column. Rank 1 would accumulate on the second column and rank 3 would accumulate on the third column. And then if you pass this around the ring in two steps, then every single node will have the partial sum. And then you would gather that partial sum across the three GPUs to get the full sum. And then this is essentially what's happening in the all gathered space. We're using the same ring. We stick to the final chunks. And after n minus 1 step, every rank has all chunks. In other words, the full all reduced result. And in this case, the communication overhead per rank is 2 times n minus 1 times s divided by n. So let's break this down. Why is it the case? Let's first look at s divided by n. And this is just saying that if our vector is of size s, then we divide this by n. As I mentioned, it's a partial chunk. So rank 0 is only going to be summing the first rank, which is only one column as opposed to all three. So that's where the s divided by n comes. And then the n minus 1 is just the fact that you need to send this around the ring. And that takes n minus 1 times. But then afterwards, you need to do it again to distribute the result. So the first ring step is called the scatter reduce, which accumulates the partial sums, and the second phase is the all gathered. Which then circulates the final chunks to all of the neighbors. OK? And then we're going to look at the implementation right away. One thing that's worth noting is double buffering. So we're actually going to talk about that when we get the code. I feel like it'll be easier to demonstrate that through an example. So with that being said, we can come ahead and open up the all reduced Python file and go through a few algorithms. And now look at the code since we've seen the example illustrated in text. So I have four different all reduced algorithms. So you can see that there's many ways that you can implement this all reduced scheme, which is once again how you can make gradients across many GPUs in distributed data parallelism. And the first one is really trivial here. It is just a reduced and a broadcast. And this is equivalent to the strategy one which we saw here, where we had the naive all reduced where we rank zero sums of tensors, then rank zero broadcast of tensors. So if I come back here, that's exactly happening. We're reducing at the destination zero and then we're broadcasting from zero to all other notes. Okay. So really just two lines of code. And then this one is the same exact style as the above code except it's just spelled out in a more deliberate way. So it's a manual all reduced ones again. But in this case, we're doing the operations ourselves, which is to say we're cloning a tensor to give us the result as like a buffer and then we're doing the following. So in phase one we reduce, we're just say that if you're at rank zero, then for all the other ranks from one to the world size, you'll receive a tensor and we're just initializing a tensor to be a tensor of zero. So we have a buffer in essence to receive the value from the other notes. And then we're going to receive the tensor with the source being the source rank from this for loop. And then we're going to add the receive tensor to the result in order to accumulate. And then if we're not the master node zero, then we're going to send to that node. So we turned the reduce operation into a receive and a send, which are the base primitive operations. You can see this just says send a tensor singularly and receive a tensor singularity. And then phase two, it's quite similar. So now if we're ranked zero, we're going to have a for loop where we send to all the nodes with the destination, I mean the destination rank. And then if we are at not node zero, then we're going to receive from node zero. So this is the same code as above except it's done with receive and send operations. Then we have the all reduce using reduce scatter and all gather. So this is more bandwidth efficient, as I said here, because it reduces the data movement since each node is only doing s divided by n, sends at a specific time, which is to say it's only sending a chunk of the data that it contains. So let's look at how this works. We have the size being equal to just our world size at the beginning. We want to instantiate this so we can divide the tensor by that size. And so we're doing here. So the input list is kind of like a buffer once again. And we're first of all, converting this into a list because the method here, the reduce scatter must, what must essentially receive input list as a Python list. So that's the only reason why we have this list here. And then it's a torch dot chunk, which is a function that's supposed to tensor into the specified number of chunks. That's it. So we have the tensor here, which is what we're trying to all reduce. And then we split that into size chunks. So if we have four tensors, it will be split up into four chunks. And then we have a value between the size to scatter chunk, which is a tensor of zeros. That is the size of one of the chunks. So not the size of the entire input list, rather the size of the chunk itself. And why are we doing this? It's because when we do the reduce scatter, we are summing up into a single chunk only, as opposed to across the entire tensor. So the output is for every single GPU, it's going to be all reduce, sorry, reduce scattering a specific chunk. And that is dependent on the world, on the world size. So if we have size four, then each of the four GPUs will be scattered, reduce scattering a fourth of the size of our parameter vector. And then the input list is just the following list. So in this case, it's the chunk list. And we take the specific chunk that we have for each GPU when we reduce scatter. And then after reduce scatter, we have what's called an all-gather. And this is the step where every single GPU passes its chunk to the other GPUs. So how this works is that we make a list called scattered chunks, which is initialized as a bunch of zeros in terms of Python tensors. Each one being the size of the scattered chunks. And we do this for the number of GPUs that we have. So we are essentially rebuilding the input list, okay, where we had the chunk tensors. But this time we want to input, we want to build it as a buffer so that we can essentially add data to these lists, okay. And then in this all-gather phase, this is our tensor list here, which is the scattered chunks. And then the tensor that we are sending so that each tensor list of scattered chunks receives these across our ring is this one, this value scattered chunk, which we calculate here. So this is what we're sending across the network. And then what we return for every tensor is the torch.cat of scattered chunks. In fact, we don't need to return this tensor because this is all done through the primitives here, which do the operations on their own. So I don't know why we had that return statement there. You can see that here and here we don't have any returns. So we actually can remove that. And yeah, I just said ring all reduce is not technically ring all reduce because we are just implementing reduced scatter and all-gather. And these could be implanted in a variety of ways. All we're doing is an all-reduce where we do it on a chunk basis so that it's of order s to add by n in terms of the number of data sent, sent instead of s, okay. So now that we've done all-reduce three, which once again is a reduced scatter paired with an all-gather, all-reduce four is the similar thing where we do a deliberate ring all-reduce in the sense that if you come here, we are checking which is which of the devices to our left, which is the missed to our right by this simple calculation, rank minus one, module size and rank plus one, module size. And it's similar to the above examples where we had all reduce one being just the pure Python, PyTorch primitives and then all reduce two being the base communications which are received and sent. So if you'd like to go through the code for all reduce four, it's the above code here except in a lower level of abstraction. So please do that if you wish. But instead right now we're going to skip ahead and jump straight into our lab, which is the last lab, step three. So coming here into the work directory, we can just open up this file and see what we want to do. So the intent here is to implement the hooks that we mentioned in the introduction of the course. And in order to do that, let's also open up a secondary file which is called optimizations and bring that into our editor. So what this file does is it registers hooks so that each grade is computed as it is propagated through our network in the backward pass. So I just want to quickly give an overview of the file here and then I'll go into more detail. So we have a function called register hooks, which is the main one that we're going to be using. And then for the buckets in gradient bucketing, we have essentially a class called gradient bucket which allows us to sort gradients into separate buckets. And this is permitted through this all reduced method which is for a specific bucket performing to all reduce, then we have register bucket hooks which combines it to. So this is a longer method. And what I'd like to note here is we have a bucket size as 25 megabytes. So as I mentioned, right, you need to amortize the cost of having multiple buckets by essentially reaching an equilibrium where the bucket size is too small where you have too much communication overhead but it's too big where you're not overlapping enough communication and computation. And this ranges between 25 and 100 megabytes. So the default value I have set is 25 and I believe that in PyTorch it's also set to 25. However, just for now, let's look at the code for register hooks. This is the most intuitive. So the first thing that we want to do is calculate how many devices that we have in our network. And then the hook has the input parameter of our gradient which we all reduce. And then we divide the gradient by the world size since we're kind of in the mean. And we return the gradient. Then for the parameter in modeled out parameters, if the parameter where car is grad, then we'll register hook on this function here, which means that as soon as the parameters backward pass occurs, we will immediate the perform the all reduce and how this works is that the register hook function as soon as we see that the gradient field for a parameter has been modified will then trigger this hook. So that's all that happens is that this register hook as soon as we populate the gradient of expensive parameter. And we're doing this for all parameters will perform the all reduce. And one thing worth noting here is that since we do it for all parameters, we can just register the hooks one time at the beginning of the training run and then not have to worry about it later, but this does not implement bucking at all. So the one thing is that there would be quite a lot of communication overhead by just calling the all reduce for every single parameter instead of doing it all at once then. So just keep that in mind, but this is essentially what we're going to be putting into our final project, which is step three. So you've already actually imported imported it here. And it's not it's not rocket science to actually get this working in essence, what we just need to do is before we train our model, we will take our register hooks and simply invoke it. And then it takes the parameter of model, which we already have implanted, which I'll initialize up here above. And then in terms of splitting the batch size across the ranks, well, we know our rank is here. So the starting index for our rank is simply rank, just rank purely times the chunk size. And then this one is just rank plus one times the chunk size. So it's not a not a crazy computation that we have to complete here. So let's just yet rank plus one times the chunk size. And this gives us the starting and ending index for our training across the ranks when we want to split up our batch. And then I think that was all we have to implement. Otherwise this is the same structure as in manual, where we are just completely lost. But the gradients are all reduced via hooks. In this case, in the backward pass, we don't need to manually do it within the training loop. So that's really the key aspect of this as opposed to the initial implementation, which was manual. Knowing this and that we've done step three, let's just run it and see what happens. So torch run. I actually forget the exact demand. So I'm going to have to cheat here. All right. It's torch run and dash dash NPR OC dash PER. So number of process per node equals four. So what this means is that we have four processes in a single node. You can also specify the number of nodes you have if you have multiple servers. And in that case, if you have like four nodes and four process per node, then you have 16 GPUs. The one thing we're noting is that we're torch run. It's less optimized for running multi node setups. In that case, you're better off using slurm as your essentially runtime manager. But since we're only essentially emulating in this case, having four processes in our single node, then this is valid. But we want to instead run this in the step three folder. I said the my work folder was step three. And let's see if we get a bug or not. Okay. So it looks like it trained. And there you go. So we have not arrived at the same loss as our initial manual approach. Let me just check something because we have a function here called main.py, which should implement the same functionality. So as long as we get the same output as that one, then we should be in clear water. The one thing is that I don't recall that we got the same value here 0.249. That seems slightly off to me. So we may have to do a bit of debugging here, but let's first calculate the main.py value and see what we get. Okay. Yeah. So it's definitely different. So I don't believe at this moment, there's anything wrong with our with our file. So let's see what is going on in main.py. So let's open that up right now and compare it with the code that we just wrote. So come up here. We can notice that we have somewhere the register hooks, which is the same. And then otherwise, the optimizer looks, opt-ins, loop looks pretty similar. The one thing I just noticed here is that the end index seems to be incorrect. So we have it as start index plus chunk size. There's no multiplication at all. So I'm slightly sized out by that. I think it needs to be ranked times chunk size and add one to this. Like we can always ask her to see if this is valid. Is this correct and see what it says? Usually yes, it is. Okay. So there you go. Nice little sanity check. And maybe let's run main.py once again and see if we get the same result. Okay. We do get the same result. That's quite odd. Otherwise, let's see if we're, yeah, we initialize in the same manual C as well. So the only thing that I can think of now is to run a compare. So you can click here, press select for compare on DDP and compare with select.on main.py. Let's close this window here and see what's up. So I don't like the view that we have right now. I want to do this side by side view instead and see if we can find the error. So right, this is just a lot of documentation imports that look to be the same. Just a bit of spacing that's different. And then we set the manual seed. That's exactly the same. Some trivial spacing differences to be expected. Oh, sorry about that. Then let's continue on. We register the hook. I don't know why the formatting is such. It seems sell it off. Oh, so we are not doing the end and next property. I didn't surround that with the proper quotations. The proper not sellables but parentheses. So I'm wondering if I'm doing that in both only in one. So let's come here. Okay. So yeah, let's just jump out of this and see which one is incorrect or if they're both incorrect. Okay, they are both incorrect. Or at least step one DDP is. So let's change this and see if main.py is the same. Okay, no, main.py was correct. So now maybe the problem was with DDP and that would make sense if we had the initialized parentheses. Okay, no. Let's see. This is different than the initial value that we got. Okay, so we're still getting the deterministic 0.7072 whatever. That's not changing. But it's not the same as the value we're getting with DDP. So let's, I guess, revert back to the comparison because I didn't get a full view of what we were doing. Compare was selected and do this view. I see what the problem was. I wasn't actually pressing the wrong, right button. So let's close these and see what we can get. I don't know how we can achieve the split screen view for the comparison. So looks like this is the best we've got. And right, we don't have the same cleanup here. We're using this destroyed process group and then cleanup and they respect the versions step. This isn't make a terrible difference in terms of the code itself. And yeah, it seems to be the same. So I'm slightly baffled that we are getting different values since we do have the same seed and everything. Right. For some reason this is green, which makes me think that we're initializing the seed in different places in the two programs. So let's check. We're doing this after we initialize the distributed group in the main.py and then we're doing this here. Oh, wait. Oh, we don't even initialize in the random seed in our code. That must be the reason why. So let's come here. Slightly higher. I believe we're doing it at this stage in the step three code. And now if we run this, maybe it'll be the same. Oh, okay. There you go. So you just solved it and I'm very happy about that. So now, yeah, we've replicated the results across two, two separate processes. And this is the one that we did on our own. Of course, it was pretty trivial to just register the hook. But something that is cool that I want to now demonstrate is a example run, which will compare a few different all-reduce examples. So this time I do remember what it is and process per node. This is the four and then the file itself is srcxamples.py. And this is going to give us a pretty big output. So we're going to have to go through it pretty fine-comed after we finish this. So let's see. Next if we jump into the code, I'll just show you quickly what it looks like. So it's essentially comparing all-reduce algorithms and then also comparing hook versus manual timing and then bucket versus unbucketed. So it's a pretty 200-long Python script, which does this comparison. And let's come and look at what we got in the results. So when it comes to the all-reduce functions, we have the tenser size as 100,000. And all-reduce one didn't perform that well for four milliseconds. The same thing with the manual one, which was pretty similar. All-reduce three for some reason took a very long amount of time. And the ring one, which was essentially the same algorithm as all-reduce three, but by implementing the primitives on a lower level, took only 1.2 milliseconds and the pie torch all-reduce took 1.81 milliseconds. So this is quite compelling. I'm actually surprised by the result. This is more than I was getting initially. But let's try to change the tenser size to right now it's a thousand. Why was I getting 10,000 if it says a thousand? Let me just make sure I'm not overriding it somewhere. Oh, okay, there you go. There's the real tenser size. So what if you make this a million? Let's see what happens in that case. It may take a bit longer. Oh, okay. So now we have a size of 1 million. And the timing is actually much more consistent. So let me just run this one more time to make sure that it wasn't a fluke and see if the results are consistent. Okay, there you go. So for some reason, the ring would scatter gather there was taking 13 seconds in the size, a hundred thousand test run. And I'm assuming that must have been some type of bug. So let's run it once more. And okay, there you go. You can see that now it's taking only three milliseconds, which is still slower than the rest. But in any case, it's not 13. So that must have been some strange error in the run. And then we have a time when it took 13 milliseconds. And then more importantly, when we compare hook versus manual, we'll do timing. When we do manual, it's taking actually 2.64 times longer than with the hook base approaches is impressive. And then when we're using unbucketed time where we're doing it at every single parameter, as opposed to in buckets of 25 megabytes, then it's actually five times slower. So the bucket time is performing much better. And once again, the buckets is when you combine multiple layers together so that you wait for those two layers, for example, to compute their gradients. And then at that point, you perform the all-reduce. Okay, so that was a cool set of examples. Now that we've finished that, kind of after our syllabus, the only thing left is to do a quick performance loss as to see when DDP is worth it and how all it scales. And then that will be the conclusion to our course. So let's talk about some of the trade-offs when we're performing the serial data parallelism. So one thing we're thotings that it does add communication overhead. And understanding when it's worth it is crucial. Overhead is simply how much time you spend communicating versus the total time you spend computing because if you only have one GPU, then you don't spend any time communicating since there's no devices that you need to communicate with. And in effect, the three following scenarios dictate when it's not really worth it to use DDP. For example, if you're using two small-level model, in that case, then the communication time will just be much stronger than the computation time. So it won't really be justified to send all this data across the models with a model is too small. Also if there's just a slow network, this doesn't just apply to DDP. If you're doing any parallelism technique, then slow networks will kill your training speed. And then if the batch size perang is also too small, then, well, the entire intent with DDP is that your batch size is too large to fit on one device so that you're splitting it across devices in order to have the effect of having a very large device, which can compute that large batch size. But in effect, you're actually splitting it across many different devices. And let's talk about some analysis. So if your computation time is much larger than your communication time, which is to say that you're mainly bottleneck on computing, then DDP is really nice because it scales near linearly, which is ideal speed up. And this is just done by adding more GPUs because the more GPUs you add, the more you can compute. And if the communication time is already really small, then adding more GPUs won't affect the computation computation computation to trade off terribly. And then if you are communication bound instead, which means that the Amdolz law analysis indicates that communication is taking the most amount of time, then the scaling for DDP is pretty much plateauing, which means that adding more devices won't really help since the bottleneck is on communication and not computation. So effectively, you're just extending the communication time when you're adding more GPUs. And it's not going to help you in any substantial way. So solutions that could be using grading compression, so that what you're sending is a smaller cost network, also having just a larger batch size per GPU so that you can have less GPUs that communicate less. And then having a faster interconnect interconnect, of course, in Hennie boundary and V-Link something like this will not hurt at all. And then when it comes to memory bound analysis, if you have a batch size that's limited by the GPU memory, then other techniques can be used as well. Instead of just pure data parallelism to have a lower GPU use for the memory per GPU, and this can be done by using model parallelism, which is what we talked about in the pipeline parallelism course, or tensor parallelism, which is when you start the model weights. And then finally, we have fully-shotted data parallelism, which perhaps will make a course on in the future. So these are all types of model parallelism. And in this course, we discussed data parallelism, in particular, distributed data parallelism. So there's some further resources. If you'd like to go in even more detail on distributed data parallelism, otherwise, thank you so much for your attention. And I hope you learned many new things in this course. If there's things which you have an opinion that I need to improve, please comment them down below because I always want to make my videos better. And furthermore, if you have questions on the course content or any DDP-related queries, then do not hesitate to leave a comment down below because I will be surveying the comment section for the next few weeks in order to respond to any questions that there are. Otherwise, thank you so much for watching. I hope you have an amazing day, and I'll see you next time.