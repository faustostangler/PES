---
video_title: "I Was DEFINITELY Using The Wrong VPS Setup"
video_id: E6KVIUJ50y0
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=E6KVIUJ50y0
video_date: 2025-03-07
video_description: |
  Make everything multilingual with Crowdin's AI automated localiztion!
  Check it out: https://l.crowdin.com/dfBYOJR
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  It took me years to figure out that a) not every production deployment needs an over-engineered hyper-scale system, and b) there's a perfectly capable technology that I already have if I just DO IT RIGHT. Check out docker compose, and swarm, for when things get real!
  
  ⌨️ Tech I use: https://kit.co/omerxx/my-battle-station
---
sometimes you have to admit you were wrong while advocating for Devo's best practices distribution microservices and other fancy deployments over the past couple of years working with startups helping them with their system architecture in the cloud I've seen small teams using one server with a few containers running on it making hundreds of thousands of dollars every month no scaling rules no fancy monitoring options just a plain VPS and some raw processes making cash now sure the they called me in because they needed some improvements but nevertheless a basic raw system was doing a pretty good job until things started breaking after over a decade working with containers building hyperscale production environments from scratch and working with developers ranging from high school dropouts over to senior 10 xers who saw everything I noticed that they're all missing something about how they work with multicontainer environments Docker compose is the go-to option for local development with multiple containers it makes things easy by letting you preset environment variables dependencies between running containers it lets you isolate networks but most importantly it lets you start and stop an entire fleet of applications with one command but when things need to step up and move over to production most teams just take things as is and run them on a remote VPS in the cloud this is missing the entire point I'm not advocating you drop compos all together sure it's not the best idea to run it as a long-term production solution but nevertheless it's it's more than capable and even supported officially in the docs okay I think we're going a little bit too fast here let's step thing down a notch and in order to build a real production Beast let's take a look at the compose Basics to show something running in compose I have my famous guestbook app which thanks to Claude is now simplified the code isn't very interesting other than the fact that it's currently speaking to re through local host or a given environment variable running processes naively pops a message about ready is not being available locally so it won't start to this end let's do it the old way Run Red on a local process with which the app can now run smoothly let's even pop the web page it serves and sign the guest book so far so good but that's pretty basic now let's make it interesting by serving reddis instead of a local process as a container and since I have to be different in this instance I'll be using podman to run valky which is like saying I'll be using Docker to run redis but a little bit more open source there's a video above here on podman and another one for valky right here and on the Channel's recent releases okay I'm done being a hipster let's run the application Again by starting the process as is and it still runs this time speaking to reddis or valky to be precise served from a local container but that's cheating a little bit since reddis isn't really running locally it's running in a container that exposes ports which is nice and all work working locally even scaling it up one more replica would create a conflict of ser ports so not ideal let's fix this by building a Docker file or a container file if we keep the hipster theme this is a rather simple container file taking goang as a base layer using it to build and compile the code which is then sent to the from scratch container that holds the binary by the way if you're curious about the process please stop this video and leave a comment below so I can gauge the interest in building great light and fast containers anyway building the image with either podman or Docker shoots The Familiar no red available error good let's give it its redice shall we podman run a detached file key with the ports mapped outside then run the app again and what still no redis what's going on here here's the thing remember our application is pointing at local host in the redish port well running inside a container Local Host is no longer the actual host but the container itself so while red is exposing the right ports they aren't available on the container's local path there are creative ways to fix this of course but the golden standard for multicontainer environments is Docker composed a quick break for a word from today's sponsor ever wondered how companies like Brave Discord and Intel handle Global software localization meet crowding the AI powered localization platform that seamlessly integrates with your development workflow imagine your translations updating automatically with every commit no more manual file handling or coordination nightmares just push your code and crowd in's AI handles the rest supporting over 600 Integrations while maintaining your rapid deployment Pace join the three million users who trust crowding ready to go Global start free at crow.com and let your code speak every language now back to the video now I'm assuming basic level of familiarity with compose but even if you don't have it it's rather straightforward compose is a yaml holding a list of services which are containers and their optional settings like an email ports environment variables and more we have two here valky which is our R cache and app is the cbook web page compose allows for a depends on Entry to make sure the container doesn't fail over not finding reddis until redis is fully ready to work lastly since compose is looking for a Docker file and I'm building a container file I will explicitly tell it to build that and now we can start building Docker compose up minus minus build tells compose to ignore pre-built images if it has something cached and rebuild from scratch once built compose turns to running them and while valky is doing fine our app still doesn't see reddish let's fix this again remember how it looks for local host at Port 6379 compos using the docker local container Network Maps the running Services directly so we can call them by name by the way this is extra cool when running more than one replica of a service since it also serves as a lightweight load balancer okay with the guess book addressing valky directly let's build and run again and there we are the guest book is ready to serve so compose is working but this video is meant to address how most composed users are missing the point and a closer look at the current setup tells us two things one it's not good for development rebuilding and running two containers with every line change is annoying to say the least on the other hand this isn't production ready the hard-coded ports lack of watch doog functionality to restart containers when they crash and other features are simply missing so how do we make compose perfect for both scenarios let's start with devs working locally to adjust the current file in making development of Breeze I'll change the image to the base image used to build the container we'll also add a command to run the process directly and most importantly Mount the current working directory to The Container so that changes I'm working on will be immediately reflected inside the container without having to rebuild it with the current directory mounted to/ app on the container and a proper command to run compos logs now have some go libraries installations and we're good to go loading the web page looks fine but let's make sure things can be changed in real time let's make an HTML change to the web page which is part of the code mounted I'll change the placeholder and without having to touch anything but a page refresh we can now see the new line this makes development of Breeze no restarts no rebuild you can even add a hot reloader like air for instance to detect go changes and restart the process without having to touch it at all right Dev workspace sorted but how does this translate to production there are multiple ways compos can actually be translated to many other tools AWS will offer a direct translation to their orchestrator ECS naturally it can be ported to kubernetes which is an Overkill to say the least in this instance you can use other orchestrators even the one Docker themselves built called Docker swarm which is the big brother of compose so before doing all that let's go with the one naive option I didn't mention and so many teams starting out are actually opting for using the same compose file in production is it great no but there's a way to make it slightly less error prone is it terrible well like I've said seeing more than a few teams making huge bucks with compos on a single machine I had to take my hat off more than once and give it to them so for the teams who are going to production with compose let's do it right let's make compose great again so Docker compose has the hidden feature most teams I've work with tend to miss it can override and merge multiple files why is this important well instead of changing compos to work in production or holding two versions of the same file with tweaked configuration we can create production yaml and only make changes to the relevant areas let's assume valky stays the same for the sake of Simplicity and the image we're going to run is a pre-built built image the host will download the command and working deer aren't necessary as well because they're outlined in the container's image but let's ignore them for now but as for the volumes we can't have that we don't want the code to be overridden by the host mainly because it's not going to be there in the first place compose has a weird way of overriding file configs using the not reset notation and the target reset which in this case is an empty list now we'll run compose with two file Flags the first is the dev compose and the second is production compose making the second layered on top of the first before actually running it compose comes with a config command sending the results to a file looking for the volumes entry comes up with an empty pattern so we basically got rid of those everything seems fine so it's time to push to production changing config to Simply run and composes up with production settings we can even signed the guest book successfully now you may be asking yourself what am I going to have to physically run these containers and watch them what happens when a new version requires an update you have great questions to start the compose has the minus minus no deps flag that allows for replacement of any container in the stack while ignoring its dependent Services keeping them alive running that and Docker compos PS shows the guest app container was started again 14 seconds ago without interfering with valky okay but do I need to physically run that from within the server isn't ssing kind of Beats the purpose you can configure the local Docker host to point at a remote one and then run Docker compose app detached or replace a container version Etc better yet run this from a CI server with access to protect yourself better and employ automation instead of having to run manual commands lastly before swarm compose also comes with a restart always pair allowing for a native Watch Dog restarting containers when they crash keeping the system healthy automatically and adding to that making sure it's even more resilient and can overcome server restarts I tend to add compos to system D the famous Linux service manager you can find on pretty much any flavor of the operating system a short entry of Docker compose app and restart equals always in case of process crashes and you're good to go however when you find yourself looking for a scale roll out options rolling updates alerts and more orchestration features given that you'd like to stick with Docker or at least the current compos setup the next best option is swarm now this deserves one important clarification doer swarm which is now called swarm mode is not to be confused with the container clustering system swarm classic which was stopping updated during covid and is now archived swarm mode or swarm kit is built into Docker comes with lots of nice features and you can read all about them in the documentation and please do comment below if a deeper dive into swarm is of interest but for now let's use the basics using the same compos making it more simple for the example I can now add the entry deploy and replicas underneath it let's make sure the guest book up runs two instances and start swarm to do that we'll run Docker stack and if you want these fancy autoc completions like mine check carace also mentioned on my Nal video above make sure you run Docker swarm in knit to start the virtual swarm host and now Docker stack deploy minus C the yaml file and guest for the application's name and there we have it Docker stack LS now shows the guest book app with two Services running Docker stack BS guest shows a bunch of containers we have two guest containers and one file key as requested for a more clean view we can just go for Docker PS now let's scale it up Docker service scale guest app equals three replicas and swarm takes care of the service now running four containers three for guest book and one for reddis falky with Docker compos in production you may want to give another look at your local tooling while the docker engine is open source other components such as the docker desktop aren't free for commercial use check podman an open- Source alternative with Rich kubernetes features allowing for an upgraded local container experience on the next video right here thank you for watching and i' see you on the next one