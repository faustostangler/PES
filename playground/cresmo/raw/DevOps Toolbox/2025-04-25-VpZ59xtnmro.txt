---
video_title: "Why I Regret Ignoring Docker Swarm for 10 Years"
video_id: VpZ59xtnmro
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=VpZ59xtnmro
video_date: 2025-04-25
video_description: |
  Supercharge your security with Twingate:
  https://www.twingate.com/pricing?utm_source=youtube&utm_medium=referral&utm_campaign=devopstoolbox_q1_2025
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  Docker Compose is great for local use, but when it comes to production, managing scale and at least more than one node distribution, you want something with more muscle. Most people go straight to Kubernetes, which can almost always be an overkill. Meet Docker Swarm, the docker orchestrator we all have already installed, but never actually use.
  
  ⌨️ The keyboard on this video is the Dygma Defy: http://dygma.com/DEVOPSTOOLBOX
  ⚡ Tech I use: https://kit.co/omerxx/my-battle-station
---
we all have a perfectly capable Docker orchestrator on our machines but nobody's using it i've been running infrastructure for over 10 years i still remember one sunny morning asking my mentor back then "What is the best options for containers deployed in the cloud?" He looked at me and said something I'll never forget people tend to develop emotions to technology they're in love with this they hate that but remember there's no best there's better there's worse sure but the most important rule in our line of work is using the right tool for the task at hand so I started exploring back then AWS had an amazing service called Beantock where you deploy your container and AWS takes care of the host but it had a huge flaw just like Docker Compose if you wanted to scale something the only way to do that was to duplicate the server with every single container in it asking anyone for alternatives would usually end up by going directly to Kubernetes to this day this is still the case which leads people to a simple yet biased decision it's either compose or monstrous Kubernetes but what if I told you there's a better way one that's not only incredibly easy to use it lets you scale load balance applications self-heal manage secrets collect logs and so much more it's great for local use home labs but also production systems especially ones that require a few simple nodes and a straightforward deployment but the best thing it's fully compatible with your existing Compose files if you have them so today we're talking about Docker Swarm the underdog that unfortunately everyone seems to ignore there are many ways to start a bunch of machines i'm going with my current favorite Lima VM to self-host them on my machine i made a video about Lima that you can check up here to set up a cluster I'll first need a cluster manager which functions as a worker too but we need a leader for the group to apply interconnectivity I'm going to add the Lima user network so that the VMs can speak to one another directly depending on your host platform you'd want to make sure that's possible before starting out while that's running I'll go ahead and use the same template of a Docker host for my worker which will also run on the same network we'll also create a second worker to make sure that we see real scale in action it's time to start the VMs and connect them to one another swarm make things incredibly easy by allowing its CLI to do most of the work the one parameter we'll need throughout the process of constructing a cluster is the IP address of the leader that's it before hooking things up it's always worth confirming that the network is set up correctly and that the workers can resolve the manager node here within Lima I can address the host with an internally resolvable DNS address with its name which comes up with the correct IP let's use that address and hook things up a swarm cluster starts simply with docker swarm init advertise address and the IP this command returns the initiation of a manager node and a command to join workers which we're going to use let's get a shell on our first worker paste the command with the token and just like that the worker is now a node joined to the cluster magic i'm not sure if you had the dubious pleasure of running CubeADM in the past for setting up Kubernetes from scratch but this experience is so smooth i'm actually having fun seeing it work without any hassle i know I tend to get excited about the nerdiest possible things but this is just sweet so docker info on the manager node shows an active swarm with ids of the cluster the node whether this is a manager and how many nodes are available by default swarm managers functions and workers too which is why we're seeing one manager and two workers after hooking only one node docker node ls shows our node state and other useful informations like engine versions etc so it's time for worker number two getting a shell pasting the command and that's it our cluster just scaled up there we have it one manager three nodes again since the manager is one of them docker Node LS comes up with the cluster and it's time to start using it for real to do that we're going to create a Docker service not to be confused with a Kubernetes service if you've ever used AWS ECS this is kind of the same before we dive in further let me tell you about today's sponsor Twingate the smart way to secure your remote access you know how traditional VPNs are like having a single fortress gate once someone's in they can wander anywhere well Twing flips this model on its head with zero trust security think of it as a modern city where every street requires its own access path and a guard asks for your ID before you move on much safer right but here's what's really cool twing integrates with everything your clusters your backend systems even your Git platform just add it to your compos setup and see how it works everything is protected by zero authentication and you can find yourself in with one finger tap no contact switching needed setup takes minutes not days and work across all of your devices plus you get enterprisegrade security without the enterprisegrade headache whether you're managing a remote team or running a complex swarm deployment Twingate handles the security heavy lifting and you can focus on what really matters building great software ready to level up your remote access security head over to twingate.com for a free trial and see why thousands of teams trust Twingate with their secure access needs in the context of Docker a swarm service refers to a containerized application deployed and managed as a service within a Docker Swarm cluster just by doing that we're enabling automated scaling load balancing and fall tolerance the simplest way to go about creating a service is to run docker service create with a number of replicas and an optional name and a container to run in this instance we'll follow it up with a command to run so we're going to ping docker.com just to see the output starting another shell on the manager node and running service ls shows an ID in the Alpine image starting swarm also waits a while to make sure things are stable before confirming that to the CLI which feels like Kubernetes for children but in a good way no messing about no templates and setups to fight with or health probes just pure work everything I mentioned is absolutely necessary don't get me wrong but for simple use cases home labs even production environments with two applications especially when the cloud is not an option or too expensive as it mostly gets I'm going with Swarm all the way it really doesn't get any simpler than this there are some limitations sure and we'll get to them later but this is the simplest orchestration setup I've ever seen the service now has one of one replicas up so we're basically up and running let's fetch the logs showing a valid ping to docker.com in half a second no less to read the configuration kind of like cubectl describe for those of you who know it service inspect with a pretty flag outputs a human readable service state including everything you need to know you may notice parallelism here which is set to one by default but can also be increased telling Docker exactly how many replicas are handled at a time let's see this in action we're going to scale our hello world app by running Docker service scale changing Hello World to three swarm kicks in and the manager starts orchestrating the request scheduling tasks one by one given our parallelism setting once another is changed to running the third is handled until we see that the service converged i think it's interesting to pause on this word selection here converge suggests two points meet which in the orchestration world it's the desired state and the observed state this is a well-known concept that also Kubernetes shares when you start a service or deploy a resource the system looks at this as a request and different processes start working in iterations to bring that request to life with all the configurations this is slightly different than just installing something it's an ongoing dynamic process that reacts to a system that changes all the time and actually expects failures in the process sometimes it's referred to as a reconciliation loop another term set to emphasize the two parts that need to come together docker service ps now shows a bunch of entries way more than our three instances because it failed a bunch of times over a missing image if you look closely the name column actually marks the failed phantom tasks with a backslash so in total we only have real three running tasks you'll also note and that's the whole point of having a swarm in the first place that the task are spread out across the cluster one scheduled on the manager another one on worker one and a third one on worker two effectively creating redundancy and high availability across many nodes what about load balancing you may rightfully ask well swarm comes with the notion of an internal mesh which is a fancy way of saying it load balances requests to a published port around the swarm the routing mesh enables each node in the swarm to accept connections on published ports for any service running on it regardless of whether the task is actually running on that specific node when it's time to delete things Docker Service RM takes care of removal and garbage collection until things are gone our simple service is gone now let's actually create one that can handle rolling updates we'll roll out a new service three replicas running Valky the open source radius fork there's a video covering that up here if you're curious we're also setting an update delay of 10 seconds which gives us room to breathe and gracefully roll out updates when they're needed in this way the swarm manager hangs 10 seconds between tasks updates of a service instead of the default zero which can be too quick to some applications note that I'm using Valky 8.1 which isn't the latest and that's on purpose so that we can upgrade it

with a new release the progress slowly changes and while it's finishing up we can call again the service inspect command that reveals the status and even the update config with set with the delay failure ratio etc when we want to release a new version and given the context of your swarm this might be done by an automated CI/CD process we use docker service update image and change that to the latest tag slowly but surely swarm goes task by task since we've set parallelism to one and 10 seconds delay until it completes the roll out and even counts down to make sure everything's nice and stable the image is updated with the latest tag and its unique hash that we can reference later if you want to learn further about the change the service inspect also provides a rollout summary under update status telling us where we're at and also how long it took overall so everything we've just shown is when things go right but sometimes things go wrong a server is about to crash or needs an update or replacement or many other factors we all know so like other orchestrators Swarm can drain a node moving tasks away gracefully to other nodes in the system by changing its availability setting nodeless shows the availability status which isn't active at the moment and can be thrown back into the cluster by changing it back to active again remember the network mesh from earlier allowing for load balancing well if you need to make changes or update published ports there's an option for both in the service update command so I can add a published 6379 to our Valky service which can be reachable from anywhere in our swarm but if you see here a port problem you're absolutely right these can conflict with each other using the same port for multiple replicas if they're on the same node for which swarm comes up with a publish mode ingress using this setting gives you the best of both worlds your services are accessible on the standard port in this case 6379 from outside the swarm while internally Docker manages the routing and load balancing armed with Docker Swarm I hope you'd make the right call next time you're thinking about smallcale system or your home lab but none of these work well without having a solid Docker container basics watch Docker for the Impatient next and tick all the boxes on your way to a scalable production deployment thank you for watching and I'll see you on the next