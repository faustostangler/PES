---
video_title: "Lima Is Probably The Best Virtual Machine I've Ever Used"
video_id: 2SGyhd5OI-c
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=2SGyhd5OI-c
video_date: 2025-04-18
video_description: |
  Supercharge your security with Twingate:
  https://www.twingate.com/pricing?utm_source=youtube&utm_medium=referral&utm_campaign=devopstoolbox_q1_2025
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  LimaVM is a fantastic framework that launches templated VMs, automatically mapping local ports, working directory and even home dir to the VM making development a sweet breeze!
  
  ⌨️ The keyboard on this video is the Dygma Defy: http://dygma.com/DEVOPSTOOLBOX
  ⚡ Tech I use: https://kit.co/omerxx/my-battle-station
---
it was already late in the afternoon i could see out the window it was raining in 2 hours it's going to get dark that's London in December i needed to quickly test the customer's application on Linux before the 3 p.m meeting i open Docker Desktop wait for it to start up configure a container deal with the volume mount then the permissions which while rushing I got both wrong then finally I got my environment running the MacBook's fan kicks in as Docker swallows the CPU like a cookie monster as I work the shared file system lags just enough to be annoying and I noticed the battery draining faster than expected there has to be a simpler way to run Linux environments on Mac OS without all this overhead everyone needs VMs for building testing and running software we use them daily even without noticing we find paths around them when we need to build code for different operating systems some of us use costly machines running on AWS or similar platforms others try to do the same with Docker containers running locally which in some cases is just abusive to the computer's resources but what if I told you there's a better way a solution that isn't only faster it's lighter to run maps your ports with a virtual machine automatically mounts your local workspace and is also fully compatible with all types of containers should you choose to run them limo VM started as a project aimed at promoting containerd the famous container runtime used by Kubernetes and other platforms containerd comes with its CLI NerdCl this was mostly addressing non- Linux users trying to run native virtualization without the overhead of Docker and the likes but Lima grew way way beyond that it now supports all operating systems allowing you to spin Linux VMs with minimal fast it automatically handling annoying parts like networking file sharing and port mapping lima has a developer first mindset it doesn't force you into guies or complex configs there's a simple CLI that gets out of your way when all you need is work let me show you how it works before we can play with Lima you'd want to install it mac users can install Lima and Chem the open- source quick emulator for virtualization nyx fans can get Lima directly with Nyx once you've picked your poison Lima start sets you off with your first machine there's always the option to tweak the config on the fly or choose an official preset and we'll get to these later skipping configuration sets you up with the name default and an iuntu geared for docker template for the new virtual host if you pay attention the VM isn't just starting itself it also sets up a TCP port mapping for SSH DNS queries and a dynamic port for quick access it also suggests we run Lima to open the shell lima is a shortand for Lima Ctl with the default machine as a target for example running Lima and then a command to install Neoetch will automatically run it on the default machine we've just set up lima Neofetch will now run it on our default machine and print the result a beautiful bundo running on top of an Apple silicon yes this felt weird saying as much as it is hearing and it got allocated four CPU shares and 4 GB memory by default everything following Lima will keep going to the Lima machine although ran from my local host lima CLL is our gateway for all things Lima machines ls pops the running machines allocated resources and means of access the docs at limmavm.io list all the options and permutations to help you out remember the optional presets lima calls them templates the ones with a golden star are called tier 1 templates which are officially tested by the team you can find many Linux flavors but also different container engines such as Docker and Pondman and yes if you're on your way to orchestration you've got Kubernetes and K3S which is a lighter Kubernetes built for the edge and others before we dive in further let me tell you about today's sponsor Twing the smart way to secure your remote access you know how traditional VPNs are like having a single fortress gate once someone's in they can wander anywhere well Twing flips this model on its head with zero trust security think of it as a modern city where every street requires its own access path and a guard asks for your ID before you move on much safer right but here's what's really cool especially for you developers out there Twing now seamlessly integrates with Kubernetes so instead of bouncing between engineering and IT teams you can configure secure access right in your YAML files and it's all part of the regular engineering flow no contact switching needed setup takes minutes not days and work across all of your devices plus you get enterprisegrade security without the enterprisegrade headache whether you're managing a remote team or running a complex swarm deployment Twing handles the security heavy lifting and you can focus on what really matters building great software ready to level up your remote access security head over to twingate.com for a free trial and see why thousands of teams trust Twingate with their secure access needs to use one of these templates run a lima start template colon/ slash and the templates name here's the official Fedora template that comes with Lima now we've got two running VMs to access either we run Lima CL shell and the VM's name you might notice that the path in blue is familiar that's because Lima mounted my local current path as well as my home directory which in this case are the same it's mounted directly to the VM and sets my path already with the open shell session so my working directory is user/ OMRx just like I had on my local host and my local files are here with me i had to run you name just to make sure this is in fact an Ubuntu I'm using i can access my Fedora instance with the same is mounting the path and attaching a shell where I can yum install libraries and play around i mentioned in the intro how Lima was built to help introduce NerdCl and containerd well running containers on Lima VM using NerdCl is incredibly simple this line would send an Engineix Alpine container mapped to AT80 on the host to the VM note how this command like others is run on the host rather than inside a VM and addresses NerdCl with the Limma notation let's take a look inside the VM and see what's up when we run a container d command nertlps doesn't show anything yet on a split on my local machine which is also the host under Leon VMs NerTl pulls Engineix and ends up with a container hash as a response now we see the container within the VM running with a port mapping of host AT80 and the container's HTTP back to my Mac localhostat80 reveals engineext running a container inside a VM on my machine mapped all the way through in an almost transparent way remember all of this came to life by running a simple VM from my local machine then running the container from the same place and accessing the result from my browser it really doesn't get any simpler than this not with Docker or Podman surely not with mini cube and Kubernetes all these have my utmost respect but for local use I think this is the pick if you value simplicity and things that just work let's shift gears shall we if you're after a Docker container for whatever reason resetting the Docker host to the Lima Docker socket is all you need to enjoy the same process another option is to use the Docker context which is very similar to Kubernetes context if you're familiar with these we can create a new Lima Docker context and map it with the Docker socket under Lima check this out i'll run exactly that then Docker context use Lima-docker now the same engine from earlier run as docker command as if I'm running any container on my Mac but it's now pointing at my Ubuntu Lima VM and there you have it running inside the container available under docker ps and of course through my local browser to remove a VM we first must stop it having Lima cut the socket and the network and everything else mapped then we can delete it altogether but you didn't come here just for containers right well as a longtime happy mini cube user I was very happy to see a Kubernetes template here as Lima is a lightweight option uses containerd which is the Kubernetes modern container runtime and can mimic a real from scratch VM environment without any special presets mini cube is incredible but Lima gives it a fair fight especially when resources are scarce while I have my cube config tooling let's follow the Lima way and set the envir cube config with the output of the command in the docs extracting cube config.l AML with the assistance of Claude here's the simplest pod I could think of using my own container I built and pushed to DockerHub probably 10 years ago me and others have been using ever since as a lightweight placeholder that even responds to health checks only 5 megabytes in size so with cube config set in my new shell it's time to build the pods YAML and apply it into my newly created cluster with Lima honestly compared to mini cube effort-wise setting this up was just as easy maybe even quicker food for thought with the pod running i think we can call this a success it's important to stress that resources allocated by default to Lima VM can be easily tweaked through the YAML or by the command they start with setting CPU memory even the volume type which by the way Lima choose to run virtual FS i hope I'm saying this right it's a shared file system purposefully built at Red Hat for a local file system mounted to VMs with performance in mind okay what if our precious VM is now a critical component that cannot get deleted lima comes with a protect command to make sure that this doesn't happen try to delete a protected machine it'll scare you away until the machine is unprotected back again longtime Docker users know the prune command that acts as a garbage collector and works the same with Lima Ctl if you want to tunnel your way to Lima making your local host join as a guest to the network that's also available although still experimental lastly should you wish to create your own Lima template that's easy head over to the Lima templates path homebrew has them under opt Homebrew share Lima templates and add another YAML the reference is available in the documentation lima CL validates helps with making sure the template is built correctly before it's run to assist as well if you're interested there's a very interesting CubeCon talk from 3 years ago talking about the effort of running a fully open- source Kubernetes solution on Max that featured local host ports propagation they presented running K3S Kubernetes little lighter sibling and other tools around it okay with Lima you're a VM monster now it's time to take this knowledge and leverage it with Podman check this video next to learn about the fastest leanest and best container setup in my opinion for those who work with Kubernetes but also for any user who wants light and secure container setup that comes with an open source GUI that does a ton of work it's also fully compatible with Lima should you choose to run your pods on Lima VMs thank you for watching and I'll see you on the next one