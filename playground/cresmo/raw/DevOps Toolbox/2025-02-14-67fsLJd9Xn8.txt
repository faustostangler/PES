---
video_title: "Do You Actually NEED Kubernetes?"
video_id: 67fsLJd9Xn8
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=67fsLJd9Xn8
video_date: 2025-02-14
video_description: |
  Supercharge your security with Twingate:
  https://www.twingate.com/pricing?utm_source=youtube&utm_medium=referral&utm_campaign=devopstoolbox_q1_2025
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  Kubernetes is absolutely incredible, but it isn't for everyone, should you go for it?
  
  ⌨️ Tech I use: https://kit.co/omerxx/my-battle-station
---
12 years ago I landed my first devop roll I was a single engineer handling all the security and infra operations and what's worse I was an absolute Junior the company who later became a unicorn with a product embedded to this day in every page of Fox News CNN and other platforms was just 10 people at the time and we barely saw any traffic but then one evening our CEO landed the first huge client and we had 24 hours to prepare I was way out of my depth and I knew it I spawn up a bunch of additional servers scaled up every load balancer in the system and froze every scale down rule across the board somehow with a lot of luck and a touch of divine help which can be the only sensible explanation I made it work but that night made one thing Crystal Clear we needed a better way to scale our infrastructure today tools like kubernetes solve exactly this kind of challenge but at what cost here's the thing do you actually need it let's talk about when kubernetes make sense when it's an Overkill how to know the difference and what are the Alternatives that make sense when doesn't but also when it does make sense I'm going to show a little bit of its immense power and why this makes it the undeniable king of container orchestration first things first why does anyone even need a container orchestration system it sounds like a major headache here's a scenario of probably most companies using SAS production deployments before 2013 they were running a large server with their application getting a larger one when things get too crowded doesn't always work so they get a couple more when the loading increases and then you need a load balancer on top which is usually another server or a cluster of them relaying requests and distributing them around the server using round robin or some other method configured by the Admin this allows for more stability and a framework to scale further when needed but it's quite a manual task getting physical machines connecting them to the cluster and configuring them to handle the load also it's extremely hard to nail the size of the machine so that it's efficient with the load in a way that it's not wasteful in one hand but won't choken the first spike in traffic that it sees imagine having a central system to take care of that a brain If you will to manage the cluster of servers create more when needed or removing them when they're not this is kubernetes and while it's far from being the only option for an orchestration system it's by far the leader in its field once kubernetes Provisions these servers to solve our utilization problem from earlier it'll use pods which are usually a single container of an application and group them based on availability on the nodes it has this way it doesn't only use its resources efficiently but can also create distribution so that if our Blue app fails because of its node that dies all of a sudden it has redundancy in another location to serve the app from like we mentioned since it's all automated when servers are not required they can be deprovisioned and removed especially on cloud platforms this becomes a huge cost saving Factor need a load balancer no problem kubernetes creates and integrates with every load balancer in the ex resistance from open source through managed Services by Cloud providers like AWS when a bunch of additional pods are loaded whether due to automatic scale and we see that in action in just a bit or a user's demand the kubernetes scheduler will observe its current capacity place the pods where they have space or create a bunch of additional nodes to host the new demand this way instead of waiting for a new server to be physically connected or rerouted to serve the new need we reduce the time from hours to minutes however production ready solution like that can take weeks or even months depending on the scale complexity and sensitivity of the applications deployed acquiring the skill for new hires or leveling up is not an easy fit it takes a lot of learning if you're interested in tasting the basics and a quick start kubernetes Workshop I've got you covered check the link in the description before we dive in let me tell you about today's sponsor twing gate the smart way to secure your remote access you know how traditional vpns are like having a single Fortress gate once someone's in they can Wonder anywhere well twing gate flips this model on its head with zero trust security think of it as a modern city where every District requires its own access path much safer right but here's what's really cool especially For You developers out there twing a now seamlessly integrates with kubernetes so instead of bouncing between engineering and it teams you can configure secure access right in your yaml files it's all part of the regular engineering flow no contact switching needed setup takes minutes not days and works across all of your devices Plus you get Enterprise grade security without the Enterprise grade headaches whether you're managing a remote team or running complex kubernetes deployments twing gate handles the security heavy lifting and you can focus on what really matters building great software ready to level up your remote access security head over to Twin gate.com for a free trial and see why thousands of teams trust twin gate with their secure access needs many incidents require an expert to dig the root cause out of a mild symptom for example a look back failure of a pod can can be traced back all the way to a misconfiguration in a node scaling rule that has nothing to do with the app the Pod or the deployment or the cluster itself this can take long hours okay enough with fear and scare let's talk about the flip side of things real kubernetes power check this out there are a bunch of methods to run and test local kubernetes clusters my favorite by far is mini cube in the past it was considered a side project but over the years it was adopted by the core team and it comes with a ton of features it's super easy and effective for this magic trick I'm going to need a volunteer kidding a metric server this is a small add-in that you're going to find in 99% of production clusters because it helps with autoscaling let's install an app I'm not going to care too much about what it is but for the sake of explanation there's a deployment here which is a scalable component kubernetes creates to wrap around pods and there's also a service which you can think of as a load balancer on top of the application pod we're going to create many replicas of this pod soon and they're all going to be served by this one end point in the Pod there's a single container from kubernetes IO example registry that listens on HTTP Port 80 additionally pods can be set with specific resources requests and limits requests make sure this pod gets allocated what it has requested and limits does the same and as the name suggests it caps the Pod so that it doesn't interfere with its neighbors let's deploy this code by applying it I'll get the pods with cctl and insert them into a nice table using nal's magic more Nal in the video up here here's also the service named PHP Apache that sits on top of our application with currently only one pod quick tip to debug a cluster and make sure things are fine we tunnel our way through the running service by using port forward naming the service the port on our machine and the remote Port it should be mapped to so 880 on my Local Host should be tunneled to the containers 80 in a split I'll call my favorite utility for HTTP X Ag More X Ag and other terminal API testing tools in the video up here here's the okay response and even the logs above are hitting an incoming request now for the magic powder and check out just how easy what I'm doing right now let's tell kubernetes to automatically scale this application with a given threshold to do that I'm going to use a core component that's called the horizontal pod autoscaler or in short the HPA there's also a vpa in kubernetes for vertical autoscaling up or down but let's not complicate things too much at this point now pay attention Cube C autoscale deployment then the deployment name PHP Apachi and the flags saying the threshold for scaling up or down is 50% and we want to go as high as 10 replicas or as low as one kubernetes will operate in the boundaries we gave it it'll be quick to scale up and very soft on scaling down to prioritize stability cool the rule is deployed and just like that we already have autoscaling applied now tell me this doesn't get you excited the Simplicity and elegancy of what we just did is not something you can easily achieve anywhere else as with any kubernetes object we can always get them I'll get the HPA and there we have it mean Max current and Target which is0 over 50 meaning there's currently zero load on the CPU but we're about to change that let's create a small dos attack I won't bore you with the details and you can find all of them in the example on the official docs but basically this runs a busy box which is a super light Linux container that tries to reach out to our little server every millisecond effectively creating a denial of service attack we can start seeing responses coming in way slower than what we actually asked the server which is 100 times a second meaning there's a huge load on it now getting the HPA with the watch flag should slowly show an increase in measured load and there we are already at full capacity the HPA scale up adding another replica we can also get the pods to see they're both available now in standard production day-to-day operations this is how you tackle load note the measured CPU index is currently at 177% since we have more than one pod and the load is computed per core we can see higher measurements our replicas are already at four let's kill the low generator and wait a little and the lad dropped to 76% across four pods which is not bad at all now if you remember kubernetes is very soft on scaling down so it has a long cool down period and it takes a few good minutes until it decides to lose some running pods after 2 minutes the measured load is zero but it'll take an entire 5 minutes until we scale back down to one replica if you're not impressed by the power of Autos scaling let me show you another killer system that's called Helm Helm is basically a package manager it can run and install almost anything you like with one command kind of like your Brew install or AP get install but for kubernetes let's take reddis as a great example instead of finding the official container and build a deployment around it we can just call Helm we can get a Helm chart that's what you call Helm packages from a good source that has all the best practices applied and since we're after open source project and red is known to have recently become a closed Source sorry it adopts a dual Source available licensing falky is a super popular Fork also backed by AWS and we're going to find a chart for that once installed Helm gives it a name and a bunch of other information Helm LS gives us the status and versions and if we get the pods you can see there are a couple of them falky primary and one replica which means this thing is already built to scale still unsure about the use of kubernetes while I don't want to sell something that complex here's another extremely powerful concept of kubernetes arbac stands for role based access control which by its name you can already guess is a system for roles and permissions that can manage users across the system from which an administrator can control to a very low level of granularity who can do what as a quick example I'm going to create a role named pod reader which is only allowed to get and list pods resources a role binding will binds the role to a user we just made up named Jane Cube C off can I is a funny little command that tells us whether Jane can list pods and yes she can we can then get pods as Jane to see from her eyes with the response Jane cannot list services and trying to list them gets a forbidden answer from the API now just to sprinkle some additional Magic on top kubernetes is also unbelievably popular so that the community and products around it are huge one of the popular tools coming up in recent years is K9s a terminal user interface for clusters it can show any running resource attached to running containers read logs services is even the role we just created for Jane and a list of what she can or cannot do lastly it's important to say that while everything we talked about so far as far as resources go are core objects coming with any kubernetes installation the system is built to be extended by default and encourages you to do so just to show the power I'll create a fake resource with a special kind devops toolbox or in plural devops toolboxes once applied the cluster accepts the new custom resource definition which we can start interacting with I can c C get devops toolboxes but since there are none currently let's build one and deploy it a quick yaml with a name and a madeup spec that runs a container now once the object is applied why nothing real happens is there's no controller which is a convention for an application that takes care of these objects I can now get a toolbox and there we have it this doesn't even scratch the surface I mean look at this map this is the cncf landscape a collection of Open Source or commercial products built around and for cloud native systems most of which are written go and running on kubernetes it's endless like the universe and staying on the same analogy it's expanding rapidly and exponentially he told me you put 60 years of your life in them was it worth it well yes but also no wait wait I know that's an annoying answer let me explain yes kubernetes deserves every little praise it gets the open source project started at Google to manage containers has quite literally took over the world managing the vast majority of container-based production deployments it can manage your infrastructure security cicd Edge functions and more it runs on premise it runs in the cloud it runs on edge locations and if you're not sold on that it's little lightweight sibling k3s does it instead for thousands of Chick-fil-A stations just as a juicy example so that's a huge win isn't it well it's not all Roses and Rainbows having that big of a system becomes a fomo of its own and Engineers want it in their CV engineering leaders thinks it's the wisest choice they can make because well their engineering peers on another company told them so the monster feds itself and everyone and their system are migrating to kubernetes without giving it two thoughts so yes kubernetes is very much worth the hype you can't fight it it's good to know it's good to have in your CV and that's not me saying it's literally part of every average job description these days this makes it factual whether we like it or not but we as a community those of us who think ourselves as critical thinkers need to stop at least every once in a while and ask am I choosing it for the right reasons is it worth the migration cost do we look at other Alternatives and consulted an expert to make sure we're on the right path because as with many engineering decisions once you go down that road you're most likely not coming back I hope you made up your mind whether kubernetes is for you or not regardless of the answer containers are the foundation for all the Technologies mentioned above and recently I started transitioning from Docker as a tool key to run local containers to a new open source option check all about that in the video up here thank you for watching and I'll see you on the next one