---
video_title: "Docker for the impatient"
video_id: lSZDWY80rPw
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=lSZDWY80rPw
video_date: 2025-02-28
video_description: |
  Supercharge your security with Twingate:
  https://www.twingate.com/pricing?utm_source=youtube&utm_medium=referral&utm_campaign=devopstoolbox_q1_2025
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  How to actually get started with Docker and containers, what matters, and how to elevate your local setup!
  
  ⌨️ Tech I use: https://kit.co/omerxx/my-battle-station
---
you're watching this video now meaning there's a server somewhere in YouTube's back end that serves it to your browser or mobile app without disrespecting you or me this server has probably got a few other tasks than us in the old days and by all I mean 10 to 20 years ago companies had huge servers in their basement with lots of Gil foils physically maintaining them and installing the backend applications but over time these started conflicting with each other requiring different versions of local utilities sharing dis space and memory overwriting each other's data the Tech World noticed the problem and tried creating separation at first with something called virtual machines and then and I'm making this long story quite short containers emerged based on a Linux concept that mainly CID means are aware of called namespaces now these allow for a very efficient way of creating isolation and allocating resources to different processes without hurting their neighbors back to you watching this video The backend server is probably a huge machine with many different containers running many different applications and the replicas so that you can enjoy this video while your friends at work are enjoying other content but that's not all containers can do they're great for replicating applications environments eliminating the most hateful sentence by devops Engineers worldwide but it works great on my machine moreover containers can run local utilities without installing anything they can safely connect an entire production environment on your desktop computer with minimal resources making developers life so much easier if only they know how to make the use of that power it makes me feel useful and powerful and that's what we're covering today working with containers for the past 11 years from a junior devops to Cloud architect I'm going to take you from absolute beginner to knowing the essentials of containers and everything you need to know around them you probably think of containers as synonymous with Docker but they really aren't throughout this video I'm going to work with podman which is an OP Source alternative but every single command I run here can be identically replaced with Docker so don't stress install either or both there's no difference as for this session and I made a video covering all about it up here whatever you choose make sure it runs if you go for podman like I did it needs to have a machine so run podman machine start and if all goes well it'll say it started successfully and you can confirm that by running podman machine LS the First Command you'd want to learn for containers is PS it stands for running processes note that these are not identical and I'm only running both to stress that containers run with podman will not reflect on the docker list and vice versa so pick your poison and go with that I listed my reasoning before and in a dedicated video but here's one factor Docker requires a bunch of processes running in the background while podman doesn't it's dim andless and has proved to be lighter on my system to get started let's run our first container podman run in a container name is the basic command for running local containers let's get the famous hello world from podman if you run podman info you'll get a list of important information about the host the machines and other paths amongst we have the configured remote Registries most of the containers CLI defer to dockerhub and this is the configured search registry so that when we search for containers it'll start there let's pull busy box which goes to search it on docker.io with a default tag latest tags conventionally listed after the image's name and path separated by column if they aren't mentioned latest is inferred but it's a good practice to avoid latest tags as these can change most official images have many versions like lighter options versions based on Alpine Linux slim options Etc you'll note a bunch of hashes being pulled these are the container layers without getting into too much detail every container image is built from layers this improves caching storage and improves pulling and pushing times if built correctly imagine having to only pull the new application updates and not its operating system and dependencies if these haven't changed before we dive in let me tell you about today's sponsor twing gate the smart way to secure your remote access you know how traditional vpns are like having a single Fortress gate once someone's in they can wander anywhere well twing gate flips this model on its head with zero trust security think of it as a modern city where every District requires its own access path much safer right but here's what's really cool especially For You developers out there twingate now seamlessly integrates with kubernetes so instead of bouncing between engineering and it teams you can configure secure access right in your yaml files it's all part of of the regular engineering flow no contact switching needed setup takes minutes not days and works across all of your devices plus you get Enterprise grade security without the Enterprise grade headaches whether you're managing a remote team or running complex kubernetes deployments twing gate handles the security heavy lifting and you can focus on what really matters building great software ready to level up your remote access security head over to tate.com for a free trial and see why thousands of teams trust twing gate with their secure access needs about that busy box I mentioned earlier working with containers you're going to hear about it a lot it's an incredibly small container which actually packs many Linux utilities into one binary that runs in a container making it Unix like environment with less than 5 megabytes in size want to know how I can tell the size of the image images subcommand will list existing container images amongst of which busy box with a size out of this world simply running busy box doesn't do much there is no service or process to run so the container runs and shuts down immediately let's tell it to run something by adding a command we'll use the echo utility to print something to the screen by default containers share their STD output stream and there's the output checking for running containers again shows an empty list as expected what if you want to use the container and explore it from within without it exiting immediately one of the ways to achieve that is by running the container with an interactive shell flag and adding sh at the command to run which will create a shell for us within it inside the container we can explore the environment install utilities and make changes let's demonstrate an extreme example of such a change and please kids don't try this at home we'll delete the/ pin directory to eliminate all the binaries on the container none of the utilities work now I mean even clearing the screen is not an option anymore so let's exit the container run it again and there we are with a fresh identical environment started from scratch with everything properly working if you're interested in listing all the containers in the system even those who are running PS minus a will pop a list of all of them adding a minus Q for quiet and filtering status exited will export a list of IDs for all containers stopped which can be extremely useful for cleaning out all garbage dead containers by wrapping them in a subshell and adding the RM command beforehand but recently Docker and podman following added the container prune command that does the same with 10% the hassle so let's clear out the system a bit to run containers that don't keep s shots on dis at the RM flag when running like so here's a public image of a web page you can run right now on your machine now while it's running there's no way for me to access the web page how do we solve that problem well by Port mapping we can tell Docker or podman to create a tunnel by binding a local port to the containers instead of doing it explicitly let's use minus capital P to make the system map a random local port to the one the container exposes I'll also give it an optional name so that it's easier to address it later we've missed one critical flag and that's minus D making sure the container is run detached so that my local shell stays free pman PS shows the running containers but now it indicates a local 5-digit random ports mapped to the container's HTTP Port 80 let's see what it holds to print it in a more readable fashion there's a port command followed by the container's name or ID that lists all available mappings a quick switch to the browser at Local Host and the long Port reveals a web page with hello from Docker index file to stop running a container use the stop command with an ID or a name now before moving on let's cover the important commands so far and again use either Docker or podman for both we had PS to view running containers we had images to view stored images we've used pull to pull images run to start a container in various different ways using the different flags we used stop to stop a running process and even mentioning container prune to clear out space consumed by old dangling containers so we've covered backward but before moving forward I'd like to leave you with one present while most terminal users especially beginner ones follow the CLI and don't get me wrong it's an excellent tool here's a terminal user interface that can be a lifesaver lazy Docker is an open- source project which is by the way sponsored by warp a terminal I covered on the video up here lazy Docker pops a terminal based interface with containers images logs and all other options it was written WR by Jesse duffeld which is slightly more known for his other impressive similar project lazy git and I also got lazy git covered in the video in the link above now you've probably noticed the docker in lazy docker's name if you're using Docker skip 20 seconds forward if you're like me running podman go to the issues scroll back to the beginning of time and the oldest open issue in the project asks about podman support inside you'll find a rather simple line which will also leave in the description that remaps Docker host environment varable to the podman socket path Nell has its own way of exporting variables and once it's set we can run lazy Docker with podman processes but before doing that let's run a container so that we can query something right with a container running let's explore the interface all containers are listed by default whether running or not to filter them hit slash I'll search for running and here's our romantic elgamal huh with its logs to the right it also mentions the image used the volumes and networks moving to the images we can see the config involved layers and for the sake of comparison let's filter the busy box image from earlier and here's our on layered 4 megabytes Linux container with the basics out of the way let's talk about building our own container from the ground here's a simple go HTTP server I'd like to containerize it the widely known option for a containerization file is Docker file let's start by naming it this way and start working on building it every container file starts with a from line setting the base image the Baseline for the environment if you will since I'm using go I'll grab go Ling image that tag after the column is the version of the built image in my case it's a goang version based on Alpine which is a light Linux drro built for containers work there indicates the container path to work with where to copy files and where to start the process from then we use a copy command from the local building machine to the build container it uses a source and a Target either a directory or a file then we add run line to go build my application I'll add an optional expose line to indicate the port my server is working with and lastly an instruction to run the binary built above when the container starts all five lines above are build time instructions while the last one is a runtime instruction right before building let's talk about the file while I did name it Docker file to make things more conventional and less TIY Docker you can name your file a container file and most tooling around it will be able to work with it just like the do with Docker file now build an context path in this instance dot for the local deer starts building with the local files you can see every step forming a layer and Performing the instructions it all ends with a commit which is another container instruction that creates a unique hash for the container much like git running podman images shows the recently built image on top of the list but it doesn't have a name on it and it weighs 339 megabytes which isn't terrible but not extremely light either let's fix both I'll start by building again using the minus t flag for a tag by the way this isn't a must since the image is already built I can actually also run podman tag ID followed by a tag with the new some go server tag we can query it again using the images command the container runs fine with no issues but I can't query it yet remember why if you said Port mapping you are right adding minus p and the container is running PS shows the mapped ports now we can reach out to the API using a new Port which the server responds to successfully now let's deal with its size container files have a special feature allowing them for a chain of steps for example we can use the first container with the goang image as we just did but since goang is a compiled language resulting in a binary there isn't a real necessity for an operating system around the artifact so here's how Pros build images We'll add a from scratch instruction which is a special word reserved for non-operating system containers using the host below for anything they need we'll then copy the product named main from the first container by mentioning its name there are a few ways to do that I like using the as notation and build as a name for the first container then back to the copy line I'll use from Builder and that's it now let's build with the same name but label it as light and see whether it stands for its new name check this out 7 MB instead of 340 quite an improvement but can it run well it runs let's check out the server with the new port and voila win we can now query pull run and build containers now for the Final Touch multicontainer environments while there are many options one of the best is Docker compose like other features it's also available from podman CLI but it's a simple wrapper around compose so you'd want to have it installed locally I'll use a public repo on my GitHub called cbook demo app as an example to start working with compose the conventional way is creating Docker Das compose yaml file I won't bore you with technicalities the main thing to know is that compose holds a list of containers under services in this instance we have two containers named app and reddis while I can use the standard reddis I'll use valky which is an open source fork for reddis backed by AWS Oracle And even Google and has gained immense popularity since reddis announced changes to their license podman compose app will run the compose file note the message above warning that this is using Docker compose externally which means you'd want to have the docker engine running for this two containers were created and we have both the logs printed out I'll open up my Local Host with a port exposed from the composed server and there's the guest book that seems to be saving values to valky successfully let's kill valky container to see what happens by running podman compos stop redis once stopped the UI indicates there's an error we can put it back up using the reverse command compose start redus lastly contrl c will stop the environment and keep the containers hibernated aside compos BS tells us there's no environment started I'll start it back up this time with minus D flag for a detached session to view the logs of a detached session run podman compose logs to destroy the environment compos down will tear everything down and minus V adds some verbosity to the process telling us both containers and network aren't only stopped but also removed before wrapping up compose while great for multicontainer environments is also one great way for running development and debugging containers let me show you how I'll change the compos file from building the image with every run to grabbing the pre-built image for the same service I've pushed to a public registry the trick here is to add a mounted volume from my local directory on my laptop mapped to SLA on the container essentially sharing the same files instead of copying them the Final Touch is to also overwrite the command by adding a command entry to compose telling the container to tail minus F to follow /d/ null now there are a bunch of more elegant ways to make a container hold but that's muscle memory for me now compose up both containers are alive so it's time to learn a new command exec executes a command onto a container I mostly use it to get a local Shell by adding it for an interactive session and following up with sage now I'm in the application container where I can start the process using G run main it looks fine in the browser so let's change it in real time now check this out without out stopping the container I'll open index.html on my machine and update the title refresh the page and what do you know the title is updated in real time okay so you're a container Pro now and you've probably seen me using podman the entire time and speaking about podman versus Docker if you're interested in the why how they defer how they compare with each other I have an entire video covering just that so check this video next thank you for watching and I'll see you on the next one