---
video_title: "Debug With Neovim: A Dev's Edge in the AI Age"
video_id: fvRwG17XsaA
channel_name: "DevOps Toolbox"
channel_id: UCYeiozh-4QwuC1sjgCmB92w
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=fvRwG17XsaA
video_date: 2025-03-21
video_description: |
  Augment Code is the first AI coding assistant built for professional software engineers and large codebases, in other words: Developer AI for real work.
  Start using for free - no CC required: https://www.augmentcode.com/?utm_source=YouTube&utm_medium=@devopstoolbox
  
  ---
  
  ✅ Build a Second Brain With Neovim in Under 90 Minutes: https://learn.omerxx.com/courses/second-brain-neovim
  
  ✅ Zero To KNOWING Kubernetes in Under 90 Minutes:
  https://learn.omerxx.com/courses/k8s-from-scratch
  
  How to actually get started with Docker and containers, what matters, and how to elevate your local setup!
  
  ⌨️ The keyboard on this video is the Dygma Defy: http://dygma.com/DEVOPSTOOLBOX
  ⚡ Tech I use: https://kit.co/omerxx/my-battle-station
  
  ⏱ Timestamps
  00:00 - Basics
  04:10 - Sponsored: Augment Code
  05:40 - Running the deubber
---
a debugger is how you get a view into a system that's too complicated to understand I mean anybody that thinks just read the code and think about it that's an insane statement this was John kmak a legendary programmer and engineer best known for building 3D games engines like doom and Quake but what is he even talking about this is Lex and he has a bug in his app Lex was looking for a while but he can't spot the bug doesn't matter how many times he tries his teammate Carl has a perfect suggestion just print to see where the app goes so Lex adds a print but it doesn't help much so he adds another one this isn't helpful Lex is confused adding log statements recompiling and rerunning it is an incredibly inefficient way of doing it deep down inside Lex knows there's a better way there must be it's amazing what you can do with sometimes much much better tools sometimes you could fix things even before you did one compile cycle You' say well I'm just going to change this right here and yep that did the job so today we're going to talk about debugging a skill that while many users have ignored in the past with the growth in AI generated code is slowly becoming essential for those who want to survive the AI flood but we're not going to just talk about any debugger we'll be using an open source implementation within Nim I'm a lazy viim user there's a video I made about it but you don't have to be envm dap the debug adapter protocol from neovim is available to everyone constantly updating and has improved a lot recently however I am going to use a bunch of plugins for this setup one of them is env. UI to set well the user interface for the debugging process now with lazy Vim these are installed as a lazy extra and everything is done for the user you have the entire list of installed plugins both in the lazy docs and on my DOT files they're grouped under dap and dap core with all that out of the way let's install these Extras by adding dap. core and have lazy take care of the rest and there's are shiny new key mappings for all things de now as for language specific installations lazy VM users are covered by the language packs and non- lazy users can follow the docks I'm using the goang pack here which comes with env. go similarly python devs can find env. Python and you've got other setups like the Java debach adapter and another one for typescript JavaScript and pretty much every other language let's jump straight in shall we the first thing to get familiar with when debugging is break points I'm sure most of you have heard of them or use them in other setups in this instance setting a break point is done with litter DB D for debug and B for break this means the next debug cycle assuming other errors don't pop will stop at this point let's see this in action on a larger screen to make room for everything that's about to come and you've already got a small key Caster on the bottom right hand corner okay let's go ler du toggles thei we've got a bunch of splits here also known as the debugger elements this can be popped to a floating window window moved around the layouts can be reconfigured later for those who like to customize so let's run a debug cycle and see these fill out I'll reset the break point to line 32 where the redish endpoint variable is being tested leader DC for continue shows the debug options we'll use the standard deag the process runs it hits the break point which is now marked with an arrow and check out the surrounding UI elements to the top left is our local variables and available Scopes to the debugger in this instance showing that endpoint is in fact an empty string and it's kindly showing the variable type the next pan is a list of current set breakpoints the next element is threads and St frames and stick till the end to see how threads which are especially hard to debug play nice with the help of this section then there's your watch Expressions which are extremely useful serving as an interactive prompt console for reading values and setting others here an expression can be expanded showing children changing their contents and more I puts you in insert mode and let's look for the rice and point variable that comes up with an empty string this becomes immensely powerful when looking at complicated logic and a lot of variables this complicated logic can come from many places and while it used to be a problem of Enterprises and large teams with AI stepping in hard understanding large code bases and what they do in my opinion becomes one of the most critical skills for engineering going forward before running the process to see the debugger reacting I want to share something that can go hand in in hand with it augment code while traditional AI assistants might suggest generic fixes augment actually understands your entire code base making it perfect for debugging complex systems unlike other tools that focus on writing new code augment excels at helping you navigate and understand existing architecture what makes augment different is its speed and deep context engine it responds almost instantly even on massive code basis and can answer complex questions about your entire system without you needing to tap a colleague on the shoulder imagine having a principal engineer available 24/7 who knows every line of your codebase teams like webflow lemonade and GoFundMe are now trusting augment with their large code bases because it truly understands how their system work not just how to complete a line of code but wait this one gets even better and now it's personal augment is vim first there's a new VM plugin which you've seen me working with and it's an absolute blast while other platforms force you into vs code or Worse the browser the team in augment knows what real devs Need Just One Look at their plugins domain tell you everything you need to know try augment for free no credit card required just visit augment code.com to start debugging smarter not harder now let's

go back to the debugging demonstration let's see how the system reacts in real time adding a change in the reddest end point and another break point on the Red's client creation running another debug process the first breakpoint hits we see the current watched R's endpoint VAR is empty let's hit lader DC again continuing to the next break point since in between the points I've made a change the value is now shown both in line and on the local Spain let's go deeper while here we try to look for the redish client object that comes with a pointer hitting enter to expand list the children of the object which we can expand further and further I hope the idea of having a debugger look into your code is starting to make a lot of sense but watching variables is not even scratching the surface with a debug session we can make it step into a process with liter di we go in into the current Point taking us into the information of the current client in the new client method now bear in mind this isn't even my code it's a reddish dependency brought in yet the debugger goes in as much as we tell it the function now shows every single past argument including its values lader do o will step out back the same stack all the way up to our break point all of these are great but without the ability to manipulate code while it's running a debugging process isn't as powerful as it can be remember what John said sometimes you could fix things even before you did one compile cycle You' say well I'm just going to change this right here and yep that did the job so here we have an app that crashed going through a panic as expected since Ready's endpoint is valky instead of being Local Host and there's the bottom component with the log showing it but what if while debugging we could change the behavior so the session is now active the breakpoint is waiting on line three and before moving forward we explicitly change the value value of the tested variable to Local Host don't forget to save the document before letting the bar proceed and voila pass the Panic statement and onto the next change of the same variable so far so good but what if you'd actually like to set a break point that only stops if something happens to demonstrate that here's a simpler piece of code a for Loop running from 0 to 9 and naive debug flow will stop at the first iteration when I is zero note that it's not yet printed to the log below as the line isn't executed yet it will keep hitting the breakpoint again and again slowly printing throughout now instead of the usual DB we hit l d capital b and a popup asks for a statement we instruct it to only stop if I is seven running another session and what do you know everything's printed to the point where I is now seven and the debugger is waiting for further instructions this is a good point to stop and remind that lazy V users can find the list of D key maps in in a nice table listing every option seen on the menus which are also available to users of witkey plug-in lazy F users are enjoying witch Key by default the internal help docks for dap widgets can help with customization like positioning of the Scopes the sidebar layout even the cursor hover and preview settings last but not least the one area of code I really can't do without proper debugging is multi-threading I've added here a simple go code that creates three workers in three different threads prints the thread index when it starts and a result when they're done there's a weit group below which is goes way of telling the process to wait until the threads are done before finishing the work I've also added three break points the first right after printing the start of work another one if and when the thread is done and the last one which will actually hit first the main threads message about starting the workers I didn't mention it before but the UI like any other pain in lazy Vim is set to be manipulated with the Mouse as well which comes in handy with Wy let's start the process and right off the bat we can see the main thread starting above we already see a weight group set and we can move on to creating our workers by continuing to the next breakpoint right interesting things happening first off the code paint shows the weight groups contents in line as it's already set also note in the threads component there's different threads showing up each with its own stack like the use of fmt print and all the way back to where it was sent from in this instance starting at main.go line 26 another cycle brings us to the creation of the next worker also showing on the log on the bottom it's always nice to represent threads using simple index numbers to show the randomness of their appearance and life cycle jumping forward again three workers active two then zero then one all of them showing as threads on the thread component with their stack for the user to track and follow pushing yet another continuation to see two threads have already completed completed their work and printed it while the third one is just about to do so but we've stopped on the print line before it executes this is a simple representation of the debugger capabilities in the world of multi-threading which you can imagine becomes unbelievably helpful with complex systems especially ones you're not very familiar with maybe because they were written by someone or something else debugging and all is great but if you're still battling installations of plugins and language packs to set up neovim here's your next watch a from scratch guide to getting lazy V up and running in no time thank you for watching and I'll see you on the next one