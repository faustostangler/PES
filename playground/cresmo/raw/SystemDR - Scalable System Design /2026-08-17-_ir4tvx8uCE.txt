---
video_title: "Visual Debugging for Java & C++: Debugging Techniques for Systems Engineers  GDB, LLDB IDE Debuggers"
video_id: _ir4tvx8uCE
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=_ir4tvx8uCE
video_date: 2026-08-17T09:30:16+00:00
video_description: |
  Website Link: systemdrd.com
  Master visual debugging for Java and C++ and learn how systems engineers can diagnose complex software bugs faster using modern debugging tools. In this tutorial, you'll explore GDB, LLDB, Visual Studio Debugger, IntelliJ IDEA Debugger, Eclipse Debugger, breakpoints, watchpoints, stack traces, call stacks, memory debugging, thread debugging, segmentation faults, race conditions, core dumps, C++ debugging, Java debugging, native code debugging, and systems programming with practical examples. Learn how to step through code, inspect variables, trace function calls, analyze memory and threads, and identify difficult runtime failures in large codebases. Whether you're a C++ developer, Java developer, Linux systems engineer, embedded developer, backend engineer, or software engineer, this video provides practical debugging techniques for finding and fixing production-level bugs.
  #C++ #java  #debugging  #gdb  #softwareengineering
---
Who here has attached [music] a debugger to a process running in production on purpose? Take a [music] second and think about that. Most hands stay down. And that's really [music] the gap this session is about. Today, we're covering using visual debuggers for Java and C++ [music] techniques for systems engineers. The goal isn't to teach you

how to click step [music] over. You already know that. The goal is to treat the visual debugger for [music] what it actually is, a client sitting on top of a real wire protocol, [music] talking to a real process. We'll cover the shared architecture behind every debugger you've ever opened, the two protocol [music] families that power Java and C++ debugging, a live scripting example, the hard trade-offs between live and post-mortem debugging, a real production [music] diagnostics pattern, and reverse debugging. the technique that changes what reproducing a bug even means.

[music] Let's get into it. Opening. Frame the two failure modes. Engineers who never look past the IDE [music] and engineers who avoid debuggers entirely and rely on print statements and log reps for everything. Core explanation. Walk through [music] the

four bullets. Emphasize the protocol split. This is the organizing idea for the whole deck. Java debugging rides JDWP. Java Debug wire protocol part of the JPDA Java [music] platform debug architecture stack alongside JVMTI.

C++ debugging on Linux rides the Prace 2 SIS call [music] directly against the OS process. Different failure surfaces, different tooling, different production [music] risk profile. Production reality, a common story. An engineer can reproduce a bug locally in under [music] a minute with the IDE debugger, then spends 3 days failing to reproduce it against the staging cluster because the debugger was never designed to attach through a load balancer, a NAT or a container boundary without deliberate configuration, remote JDWP socket, port forwarding, Prace capabilities inside the container. Audience touch point ask if anyone has hit unable to [music] attach to target VM or prace operation not permitted and what they did next transition to [music] fix that we need the shared mental model every visual debugger is built on estimated time [music] 1.5 to 2 minutes

so let's build that understanding every visual debugger no matter [music] which one you use is four layers wearing a different skin layer one the IDE front content, [music] VS Code, IntelliJ, Eclipse. This layer renders break points, renders the [music] stack, and takes your input. That's it. It owns none of the actual debugging logic. [music] Layer two, the protocol layer. This is

the wire format the [music] front end speaks to get things done. Microsoft's debug adapter protocol, [music] DAP, standardizes this as JSON RPC, which is what decouples modern editors from specific debugger implementations. JDWP is the older [music] Java specific equivalent. And GDB's machine interface is what older native debugging IDE speak. Layer 3, the debugger [music] backend. This is the engine that

actually understands how to control the target. JDI, [music] the Java debug interface for the JVM. GDB or LLDB for native code. Layer [music] four, the target process itself, either the JVM's bite code interpreter or raw native process memory. Here's [music] the key insight, and I want this to stick with you. Stepping into a function is never

one operation. It's the IDE sending a protocol request, the backend translating that into an actual [music] breakpoint, write, an IN3 instruction for native code or a JDWP event request [music] for bite code, the target trapping and suspending and state flowing all the way back up those four layers before you ever see a highlighted line. [music] Every technique we cover today is really just a variation on where in that chain you choose [music] to intervene. And this layering explains something that trips up almost everyone the [music] first time they try remote debugging. You're independently configuring network reachability at two separate points. IDE to backend [music]

and backend to target. And a failure at either one produces a nearly identical can't connect error with a completely different fix. Keep that in mind because it comes up again in a minute. [music] Let's make layers 2 and three concrete because this is where remote debugging setups usually go wrong. On the Java side, the JVM ships a built-in JDWP [music] agent. You enable it with a flag like

agent lib/jdwp transport [music] equals DT socket server equals Y suspend equals N address 505. That suspend equals N setting matters a lot. [music] It means the JVM keeps running normally until a debugger actually connects, which is the setting you want for anything resembling a live service. >> [music] >> Once that socket is open, JDI based clients, your IDE or the JDB command [music] line tool, speak the JDWP wire protocol over it. They register breakpoints as structured event [music] requests and pull back stack frames and variable values as structured packets.

No raw memory addresses involved because the JVM exposes debugging as a managed runtime [music] service. On the C++ side, it's a different world. GDB or LLDB calls prace with the attach flag to become the process's [music] tracer. Then to set a break point, it does something almost blunt. It literally overwrites one bite [music] at the target instruction with the value 0xec, the int3 op code. When the CPU executes

that bite, it raises a sig trap. The colonel stops [music] the process and hands control to the debugger, which reads registers and restores the original bite so it can step past. One more thing [music] worth knowing, hardware watch points, data break points on a specific variable, don't patch memory at all. They load the address into an x86 debug register and let the CPU trap on access. That's why they work even on readonly code pages and cost almost nothing until they [music] actually trigger. And here's a real

constraint worth flagging before you hit it in production. Prace [music] only allows one tracer per process at a time. And on most hardened production hosts, a kernel security setting called Yama's Prace scope blocks [music] non-parent processes from attaching at all. That's a deliberate security control, not a bug. And the right [music] fix is a scoped capability grant for your debugging tooling, not disabling the protection globally across the host.

Now, let's flip this around. [music] Instead of clicking through a debugger, let's script one. GDB embeds a [music] full Python interpreter and exposes its internal object model directly. GDB.breakpoint [music] is a subclassible class and if you override its stop method, you get to run arbitrary logic on every [music] single hit and decide programmatically whether the process should actually halt. What you're

looking at here is a conditional breakpoint on a connection pool destructor path. Every time it's [music] hit, the script evaluates an expression, logs the result, and only actually stops execution when the pool's active [music] count is unexpectedly zero. Everything else, every other hit, is just a silent log line and an automatic continue. Why does this matter? Because this is exactly how you debug a failure that reproduces once every few thousand requests. A UI [music] breakpoint that

stops on every single hit is useless for a bug like that. You'd be sitting there clicking continue for [music] hours. But a scripted conditional breakpoint that only interrupts on the actual bad state [music] turns a multi-hour babysitting session into an unattended overnight run that you just check on in [music] the morning. And LLDB has the exact same capability through its Python [music] scripting bridge if you're on the LLVM tool chain instead. So visual debugger or command line debugger. This isn't

which one is [music] better, it's which one fits your constraints. Right now, the visual IDE based debugger [music] wins on pure ergonomics. Expandable object trees, inline variable [music] values right next to your source code, zero syntax to memorize. That's exactly [music] right. When you're doing local development and you already have a fast connection to the target, the command line debugger, GDB, LLDB, [music] JDB, wins on reach and repeatability. It runs

anywhere a shell runs, including a stripped down container with nothing but the binary inside it. And because it's scriptable, you can [music] encode a triage procedure once using the Python API we just looked at and run it identically every [music] single time the bug shows up again instead of rederiving the same click sequence from memory. A lot of teams end up hybrid. Point a DAP [music] speaking IDE at GDB or LLDB's machine interface over an SSH tunnel. You get IDE ergonomics with CLI [music] reach at the cost of one more protocol layer to reason about when something breaks. Here's a line I want

you to remember. The scariest words in an incident channel are, [music] "Let me just attach a debugger real quick. Live attach gives you the only view that shows a bug actually happening in real time." But it has real costs. On the JVM, most debugger operations need the runtime to reach a safe point, a moment where every thread state is consistent enough to inspect, [music] which means a single debugger operation can pause the entire JVM, not just [music] the thread you care about. That

pause is invisible in your debugger UI, but very visible as [music] a latency spike to every service calling in. On the native side, a prace [music] attached process runs measurably slower, even between explicit breakpoint hits because every signal round [music] trips through the tracer. Post-mortem analysis sidesteps all of that. A core [music] dump is the operating system's own crashtime memory snapshot configured with Ulimit, typically triggered by a signal like sigapp. A Java heap dump captures the entire object graph at a single point in time. Both can be pulled off [music] a

production host with essentially zero risk and analyzed later offline at your own pace using tools like GDB [music] against the core file, Eclipse for Java heap dumps or windigg for Windows [music] mini dumps. The standard operating pattern at mature engineering organizations never live attached to a production process during a customer impacting incident [music] unless post-mortem tooling has already failed to explain what happened. Here's what that principle looks like in practice. The best production debugging tool is [music] often the one you never had to attach at all. Java Flight Recorder, JFR, ships inside the JDK itself, not as a [music] bolt-on agent. It's a

continuously running low overhead event recorder, commonly cited at [music] around 1 to 2% steadystate overhead. Instead of attaching a debugger to a live JVM [music] and risking a safe pointwide pause, teams just leave JFR recording by default [music] and pull a time windowed recording after something goes wrong or trigger a snapshot right at the moment of an anomaly like [music] a GC pause or an allocation spike. Async Profiler, a widely used open source companion, layers sampling based CPU and allocation [music] profiling on top of that, using Linux's Perf events and JVMTI hooks [music] without the safe point bias that plagued older sampling profilers, which could only sample at [music] safe points and systematically underrepresented everything happening between them. The pattern this teaches goes further than the last slide's [music] live versus postmortem trade-off. For hot path production services, the mature answer is often neither. You instrument continuously at

near zero cost and you only reach for a debugger once the always on data has already narrowed the search enough to [music] justify the risk. And the same shape shows up on the native side. Production C++ services increasingly rely on always on sampling tools like Perf [music] or Ebpfbased tooling for the 95% case and save GDB or core dump analysis for what's left over. Last technique and this one [music] changes the shape of the whole problem. What if you never had to say it doesn't reproduce [music] again? RR is Mozilla's open-source

record and replay debugger for Linux. It uses [music] Prace Plus hardware performance counters to record a process's execution completely deterministically. Every SIS call [music] result, every signal, every thread interle down to the exact instruction. Once you have that recording, you can replay it as many times as you want under GDB, [music] including reverse continue and reverse step, which actually run the real recorded execution backward to the exact instruction that [music] first corrupted a value. Perosco, a commercial cloud debugger built by RR's original authors, takes this even further. It indexes the

[music] entire recording and lets you just query where did this value last change instead of manually [music] stepping backward yourself. Why does this separate practitioners from beginners? Because it inverts the entire debugging loop. Traditional debugging is reproduce, [music] guess a break point, step forward, repeat until you get lucky, and land on the cause. Reverse debugging is reproduce once and then work backward from the symptom [music] directly to the root cause. No rerunning, no lucky

breakpoint placement, no heisen bugs that vanish the moment you attach a debugger. The cost is real. recording overhead and Linuxon support. [music] Though ARM 64 coverage is growing, so this fits CI triggered failures and hardto- reproduced race conditions far better [music] than routine day-to-day debugging. The natural home for this is your CI pipeline. When a flaky or hard

to reproduce test fails, you record it under RR right there in the environment where it failed and hand that recording to whoever investigates. They replay the exact failing execution locally [music] instead of trying to coax a race condition into happening again on their own machine, which for a genuinely racy bug might [music] simply never happen again by chance. Let's pull this together into five things to actually carry back to your team. One, know your [music] protocol, not just your IDE. JDWP and Prace [music] fail in different ways. Diagnosing a connection failure

means knowing which layer you're actually debugging in. Two, [music] default to suspend equals N and postmortem first. Never let a live attach be [music] your first move against anything serving production traffic. Three, [music] script the repeatable, click the exploratory. One-off local bugs, use the IDE.

Recurring or rare production bugs, [music] script GDB or LLDB so the triage runs unattended. Four, [music] instrument continuously before you debug reactively. JFR and Async profiler style always on data narrows an investigation before you ever need to attach anything at all. And five, [music] reach for reverse debugging on true Heisenbugs. If a bug won't reproduce reliably, record it once under RR and stop trying to get lucky with breakpoint placement. Notice these [music] aren't

five independent tips. They're one coherent way of thinking about debugging as a discipline with real costs, [music] not just a feature bolted onto your IDE. A few pointers if you want to go deeper. You don't need to memorize any of this right now. You just need to know it exists so you can find it again at 2 in the morning. for protocols, the JDWP and

JPDA [music] specification on Oracle's doc site, the debug adapter protocol spec from Microsoft, and [music] the prace man page for day-to-day tools, GDB [music] and its Python API, LLDB, Eclipse MAT for heap dump analysis, and Visual VM, both of which worth noting [music] are usually already available in your JDK environment, even if your teams never open them. and for the frontier rpro.org, Pernosco, [music] and Async Profiler on GitHub. Thank you. I'd love to open this up for questions. Here's the question

I'll leave you with, and I genuinely [music] want to hear your answers. Which of your team's hardest to reproduce bugs from the last [music] quarter would a recorded R replay have solved in minutes instead of [music] days? Let's talk about it.