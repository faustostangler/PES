---
video_title: "Uncertainty Quantification in AI:Building Reliable High-Stakes Machine Learning Models Deep Learning"
video_id: E0bdUJwIR1E
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=E0bdUJwIR1E
video_date: 2026-08-05T09:30:15+00:00
video_description: |
  Website Link: systemdrd.com
  Learn how Uncertainty Quantification (UQ) improves the reliability, safety, and trustworthiness of high-stakes AI models used in healthcare, finance, autonomous systems, cybersecurity, and scientific computing. In this tutorial, you'll explore the fundamentals of aleatoric uncertainty, epistemic uncertainty, Bayesian Deep Learning, Monte Carlo Dropout, model calibration, confidence estimation, probabilistic machine learning, AI risk assessment, explainable AI (XAI), trustworthy AI, model reliability, deep learning, Python, TensorFlow, PyTorch, and machine learning evaluation. Through practical examples, you'll discover how to measure prediction confidence, detect out-of-distribution data, reduce AI risk, and build production-ready AI systems that make more reliable decisions under uncertainty. Whether you're a machine learning engineer, AI researcher, data scientist, or Python developer, this video provides essential techniques for deploying trustworthy and robust AI models in real-world applications.Learn how Uncertainty Quantification (UQ) improves the reliability, safety, and trustworthiness of high-stakes AI models used in healthcare, finance, autonomous systems, cybersecurity, and scientific computing. In this tutorial, you'll explore the fundamentals of aleatoric uncertainty, epistemic uncertainty, Bayesian Deep Learning, Monte Carlo Dropout, model calibration, confidence estimation, probabilistic machine learning, AI risk assessment, explainable AI (XAI), trustworthy AI, model reliability, deep learning, Python, TensorFlow, PyTorch, and machine learning evaluation. Through practical examples, you'll discover how to measure prediction confidence, detect out-of-distribution data, reduce AI risk, and build production-ready AI systems that make more reliable decisions under uncertainty. Whether you're a machine learning engineer, AI researcher, data scientist, or Python developer, this video provides essential techniques for deploying trustworthy and robust AI models in real-world applications.
  #artificialintelligence  #machinelearning  #deeplearning  #python  #trustworthyai
---
Welcome [music] everyone. Today, we're talking about uncertainty quantification in high-stakes AI models, and I want to open with a promise. By the end of the [music] session, you'll be able to explain the difference between the two fundamental types of uncertainty a model can have, and you'll know which quantification method to reach for given your actual production constraints, not just the textbook answer. Here's the provocation I want to sit with for a second. [music] A model can be 99% accurate on average and still be the most dangerous component in [music] your system because average accuracy tells you nothing about whether the model knows when it's in that unlucky 1%. In medicine, [music]

autonomous driving, lending, and criminal justice, systems don't usually fail on the easy cases. They fail on the edge cases the model was never trained to recognize as edge cases. Uncertainty quantification is the discipline of making a model's confidence trustworthy, not just its point prediction. We'll walk through a mental model for the two kinds of uncertainty, three production-grade [music] methods for estimating them, how teams actually monitor this in production, a real-named case study, and where the frontier [music] is heading with generative models. Let's start with why point predictions alone are structurally unsafe [music] in these domains. So, why is this a problem

worth solving? The core failure mode is simple. A point prediction and a confidence-sounding score [music] tell you nothing about whether the model has ever seen a case like this one before. Modern neural networks are also [music] empirically, systematically overconfident. That's not speculation, it's a well-documented finding [music] from the calibration literature. Softmax

outputs get routinely misread as calibrated [music] probabilities when they simply aren't. Think about what high stakes [music] really means across a few domains. In clinical diagnosis, a missed low-confidence [music] tumor read isn't a statistic, it's a life. In autonomous driving, a rare object misclassification at highway speed leaves no time [music] to recover. In credit and lending, regulators require lenders to justify and bound their automated denials. And in criminal

justice, risk [music] scores influence bail and sentencing decisions. So, overconfidence doesn't just create errors. It compounds existing bias. I want to ground this in something real. In 2018, a widely investigated autonomous vehicle fatality [music] was traced by the NTSB back to a perception system that repeatedly reclassified the same object across categories, vehicle, bicycle, unknown, [music] right before the collision, without that uncertainty ever escalating to a safe fallback behavior.

That's epistemic uncertainty going unhandled, not at the model level, but at the system's level. So, the question we need to ask isn't is the model accurate? It's does the model know when it's likely [music] to be wrong? And does the system actually act on that signal? To answer that, we need a precise vocabulary [music] for uncertainty.

Let's build that next. Here's the single mental model the rest of this talk hangs on. Not all uncertainty is the same, [music] and conflating the two types leads you to the wrong engineering facts. Aleatoric [music] uncertainty is irreducible. It lives in the data. It's

the noise baked into the process being measured. [music] A coin flip, sensor jitter, a genuinely ambiguous X-ray, these stay uncertain no matter how much data you add. It's caused by measurement noise, label ambiguity, and class overlap, and you reduce it with better sensors or cleaner labels, not more [music] data. Epistemic uncertainty is reducible. It lives in the model. It's the model's

ignorance [music] about regions of input space it has rarely or never seen. And this is exactly the uncertainty that shrinks as you collect more relevant data. It's caused by sparse training coverage and distribution [music] shift, and you reduce it through more data, active learning, or better architecture. Total [music] predictive uncertainty is the sum of both, and the engineering consequence here is direct. If your monitoring can't tell these two apart, you'll [music] waste budget collecting more data to fix what's actually sensor noise, or you'll ship [music] a model assuming irreducible noise when the real problem is a training data gap you could have closed. In production, this

distinction lets you route [music] irreducible ambiguity to a fixed human review budget while routing epistemic [music] gaps into a continuous active learning loop. With this vocabulary in hand, let's look at the three [music] production-grade families of methods engineers actually use to estimate this. There are three production-relevant ways to estimate epistemic uncertainty [music] at inference time. First, Bayesian neural networks. Instead of treating [music] every weight as a fixed number, you treat it as a probability distribution. Exact posterior [music]

inference is intractable for modern architectures. So, in practice, teams use variational approximations or the cheaper trick of Monte Carlo dropout, repurposing dropout at inference time as an approximate [music] posterior sample. You run several stochastic forward passes and look at the spread of the outputs. Second, deep ensembles. This sidesteps the [music] intractable posterior problem entirely. Train several

independently initialized models and treat the disagreement between their predictions as the epistemic [music] signal. Empirically, this is often better calibrated than the Bayesian approximations at the cost of running that many models at both training and inference time. Third, [music] conformal prediction. And this one is qualitatively different because it doesn't touch model internals at all. Given any trained model and a held-out calibration set, conformal methods produce a prediction set with a provable finite sample coverage guarantee.

>> [music] >> Something like the true label is in this set at least 90% of the time without any assumptions about the data's distribution. That's why conformal [music] prediction has become the default retrofit choice for teams that already have a production model and can't afford to retrain it as an ensemble. Most teams [music] don't pick just one of these in isolation. A common pattern is a small ensemble for real-time epistemic scoring with a conformal calibration layer on top that converts that raw uncertainty into a statistically guaranteed threshold a compliance team can actually sign off on. Let's [music] see conformal prediction implemented end-to-end since it's the lowest cost retrofit onto a model you [music] already have running often.

Here's >> split conformal prediction in real shippable [music] code. Let's walk it quickly. Calibrate takes a held out calibration set, data never seen during training, and computes a non-conformity score per example, 1 minus the predicted probability of the true class. So, [music] confident correct scores near zero and confident wrong scores near one. It then takes [music] a specific finite sample corrected quantile of those scores, the exact correction that gives this method its distribution-free coverage guarantee [music] from the conformal prediction theory developed by Vov, Germann, and Shafer and popularized for ML practitioners by Angelopoulos and Bates.

Predict set [music] then applies that single learned threshold at inference time including every class whose score falls under it. Here's what that buys you. >> [music] >> The underlying model is completely untouched. This works identically for a logistic regression, a gradient boosted [music] tree, or a fine-tuned transformer as long as it exposes predict proba. Three things worth remembering. Alpha

[music] controls the guarantee. 0.10 targets 90% coverage by construction. Calibration is the whole trick. Q hat is a single quantile computed once with zero retraining ever.

>> [music] >> And the output is a set, not a label. Its size is the uncertainty signal. One warning. The most common mistake is calibrating and evaluating on overlapping data which silently breaks the guarantee. Your calibration set must be strictly [music] held out. And this

guarantee is marginal, not conditional. It holds on average, not for every subgroup, [music] which matters for fairness audits. Now, how do these three methods compare when you have to choose one for production? >> [music] >> Let's translate the three methods into a decision framework for your next design review. Inference [music] cost. Bayesian

networks need multiple stochastic forward passes at serve time, commonly 10 to 50, [music] a real latency tax. Deep ensembles scale linearly with ensemble size and dominate GPU memory. Conformal prediction adds essentially zero inference cost. [music] It's a post-hoc lookup on outputs you're already computing. Retraining is often the deciding factor.

A team with an already validated production model frequently can't justify retraining it as a Bayesian network or an ensemble, which makes conformal prediction the only retrofit compatible option. Statistical guarantee [music] is conformal prediction's genuine differentiator. The only one of the three with a provable finite sample coverage [music] bound, regardless of data distribution. That's the guarantee your compliance and legal teams can actually act on. And complexity tracks directly with how much of the pipeline each method touches, high, medium, low.

A pattern repeated across [music] large tech and healthcare companies, start with conformal prediction as the fast, low-risk retrofit, and only invest in ensembles or Bayesian architectures later if the conformal [music] sets turn out too wide to be actionable. A guarantee on paper isn't the same as a well-behaved [music] system in production, though. Let's look at how these are calibrated and monitored once they're live. Calibration [music] drifts. You have to monitor it like any other production metric. Expected calibration error works

like this. Bucket predictions by confidence, [music] then compare each bucket's average confidence to its actual accuracy. A well-calibrated model's buckets sit right on the diagonal. Bars [music] below it mean the model is systematically overconfident, exactly the dangerous [music] direction for high stakes use. A reasonable target, ECE below 0.03 [music] for

automated decisions, escalating to human review above 0.05. Both Bayesian methods and ensembles can drift out of calibration as live traffic moves from the training distribution, and conformal predictions guarantee is only as fresh as your last calibration refresh. It's not permanent. It's conditioned on the calibration set staying representative. [music]

So, operationally, recalibrate on a schedule since drift silently invalidates your curves. Track coverage by [music] segment, not just in aggregate. Marginal coverage can look perfect while a specific demographic is [music] badly undercovered. Set an explicit abstention threshold, so wide prediction sets route to a human reviewer instead of an automatic decision, and alert on set [music] size inflation. A sudden jump is often the earliest signal of distribution shift, showing up before accuracy even drops with no ground truth required. Let's

ground this in [music] a real named production deployment. Google Health's deep learning system for grading diabetic retinopathy from retinal photographs is one of the most thoroughly published examples [music] of confidence gated automation in medical AI, validated and deployed in real screening programs in Thailand and India. Here's the design choice that matters for us. The model doesn't just [music] output a severity grade. It outputs a grade plus a confidence signal, and only images that clear a confidence bar get graded [music] automatically. Images that don't clear

that bar because of poor photo quality, borderline severity, or an unusual presentation get routed automatically [music] to a human ophthalmologist rather than forced through automated grading. This converts an abstract [music] model uncertainty number into a concrete operational decision, trading off three things at once, screening throughput, how many patients get same-day automated results, specialist workload, how many images need expert review, and patient safety, avoiding [music] false reassurance on a genuinely ambiguous image. Interestingly, [music] deployment studies in real-screening camps found that image quality, not just diagnostic ambiguity, was a major driver of low-confidence [music] cases. That finding led the team to add automated image quality gating upstream of the diagnostic model itself. It's a great example of how production uncertainty work [music] often surfaces upstream data quality problems the modeling team never originally scoped. This case study

used well-established methods on a discriminative [music] model. The frontier now is applying that same discipline to generative and foundation models. >> The classical uncertainty toolkit [music] was built for models with a fixed output space, a class label, a bounded number. That assumption breaks down for open-ended text generation, and that's where the field is moving now. First, [music] selective prediction and abstention. The system is trained or

wrapped to say, [music] "I don't know." rather than hallucinate a confident, fluent, wrong answer. Critical for high-stakes generative [music] use like clinical summarization or legal drafting, where fluent hallucination is far more dangerous than an honest admission of insufficient information. Second, conformal prediction for black-box [music] language models. Recent research adapts that same coverage guarantee to LLMs, [music] scoring candidate outputs, and building calibrated prediction sets, even through an API you don't control [music] the weights of. Same core mechanism as our

earlier code, applied to a harder output space. Third, semantic uncertainty. Token-level probability is a poor proxy for meaning. Two generations can look lexically different, but mean the [music] same thing. So, instead, sample multiple generations and cluster them by whether they entail the same meaning. Tight

clustering means the model reliably knows the answer. Wide dispersion means it [music] doesn't, regardless of phrasing. Teams building high-stakes LLM assistants increasingly use a two-tier [music] pattern. Cheap checks as a first-pass filter, escalating only flagged cases to expensive semantic clustering or conformal passes. The same cost-tiered pattern we saw in classical [music] uncertainty quantification.

Let's distill everything into principles to take with you. >> [music] >> Five heuristics to take with you. One, separate aleatoric [music] from epistemic before you choose a fix. If the uncertainty is irreducible noise, more data won't help. Diagnose before you spend engineering budget.

Two, >> [music] >> prefer conformal prediction as your default retrofit. It's the only method with a distribution-free guarantee that doesn't require retraining an already validated model. Three, treat calibration as a live service level [music] objective, not a launch checkbox. Monitor expected calibration error and coverage [music] by segment continuously. Drift silently invalidates guarantees made at ship time.

Four, remember that marginal coverage can hide subgroup failure. >> [music] >> A guarantee that holds on average can still fail badly for a specific population. Audit conditional coverage, especially in regulated [music] domains. And five, the one that ties everything together. Uncertainty [music] must trigger a system action, not just a number. A

confidence score nobody reads downstream is zero. Wire it to abstention, >> [music] >> escalation, or human review threshold. If I had to pick the single highest leverage thing anyone in this room could do this quarter, it's auditing [music] whether your existing confidence score is actually consumed anywhere. Because in practice, it's astonishingly common to find it's computed, logged, and never acted on. For anyone who wants to go deeper, here's where to start.

>> [music] >> A few places to go next. On papers, Angelopoulos and Bates' [music] Gentle Introduction to Conformal Prediction is the best starting point for engineers. Written for a machine learning audience [music] with working code. Lakshminarayanan, Pritzel, and Blundell's Deep Ensembles paper, and Gal and Ghahramani's Monte Carlo Dropout paper are the [music] two foundational references for the epistemic methods we covered. And Guo, Pleiss, [music] Sun, and Weinberger's On Calibration of Modern Neural Networks is the short, readable empirical result [music] that motivated our entire calibration monitoring discussion. Worth assigning

as a pre-read before [music] anyone ships a confidence score into a product. On tooling, MAPIE is the most [music] actively maintained open-source conformal prediction library and it integrates directly with scikit-learn pipelines. It's the fastest path from today's code slide [music] to a real prototype. TensorFlow Probability is the right starting point if you're committing to full Bayesian modeling. And Uncertainty [music] Toolbox is useful specifically for the reliability diagram and calibration tooling we saw on the monitoring slide. My honest

recommendation, start with MAPIE on a model you already have running this [music] week rather than reading all four papers first. The fastest way to build intuition for calibration is to compute it on real, already deployed outputs. Let's close. Let's bring [music] this full circle. A model that knows what it doesn't know is a fundamentally different kind of system. One you can actually trust in

production. The path we walked [music] today separate aleatoric from epistemic uncertainty. Pick a method matched to whether you can retrain, Bayesian networks, ensembles, [music] or need a fast retrofit with a formal guarantee, conformal prediction. Monitor calibration continuously, not just at launch. And most importantly, make sure the resulting number actually drives a downstream action.

I'd love to open it up for questions. If it's helpful [music] to start us off, how would you retrofit conformal prediction onto a model you already have in production? How do you explain a coverage [music] guarantee to a non-technical stakeholder or a regulator? Or how does any of this change for multi-label or regression settings? I'm also happy to share the conformal prediction [music] code from this deck as a working notebook afterward. People

retain a lot more from a method they've run themselves. [music] Thank you.