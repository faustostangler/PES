---
video_title: "Text Summarization Explained: Abstractive vs Extractive Summarization with NLP & Transformers"
video_id: Zu7yEQF4k88
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=Zu7yEQF4k88
video_date: 2026-09-02
video_description: |
  Website Link: www.systemdrd.com
  Learn how Text Summarization works in Natural Language Processing (NLP) and discover the key differences between Abstractive and Extractive Summarization techniques. In this video, you'll explore how AI and NLP models summarize long documents, articles, and text using Natural Language Processing, Large Language Models (LLMs), Transformers, BERT, T5, GPT, Hugging Face, Python, NLP algorithms, text mining, information extraction, semantic understanding, attention mechanisms, and generative AI. We compare extractive summarization, which selects important sentences from existing text, with abstractive summarization, which generates new concise sentences while preserving the original meaning. You'll also learn about text summarization algorithms, NLP pipelines, model evaluation, ROUGE scores, and practical applications in AI, content processing, search, and document automation. Perfect for AI engineers, machine learning engineers, NLP developers, data scientists, and Python programmers looking to understand modern text summarization and build intelligent NLP applications.
  #nlp  #textsummarization #machinelearning  #generativeai  #python
---
Welcome everyone or afternoon depending on where you're joining from. Today we're going deep on a problem every one of you has run into whether you knew it or not. [music] Text summarization. Specifically, we're going to unpack the two dominant families of summarization techniques, extractive [music] and abstractive. And by the end of this session, you'll understand not just how each one works under the hood, but when [music] to reach for one over the other in a real production system.

This isn't a surface level tour. We're [music] going to look at the actual algorithms, walk through production quality code, and talk about the failure modes [music] that trip teams up when they ship these systems for real. Let's get into it. So, why does this matter? Think about any production system that [music] ingests documents, search engines, customer support tools, legal review platforms, [music] research triage pipelines. They all eventually

hit the same wall. There is simply more text [music] than any human can read. Summarization is the compression layer that sits between raw text and a [music] decision maker's attention. And critically, the algorithm you choose determines whether that compression is [music] faithful, fluent, or fast. And you usually can't maximize all three at once. Here's the core engineering

question we're answering [music] today. Do you select the most important sentences that already exist in [music] the document, or do you generate brand new sentences that say the same thing more efficiently? Those [music] are two fundamentally different bets. On the right side of your screen, you can see the two paths side [music] by side. Extractive methods copy and rank existing sentences verbatim from the source. Abstractive methods generate

[music] new phrasing that paraphrases the source's meaning, much closer to how a human would summarize. And just so you [music] know the reference points, CNN Daily Mail and Exom are the two data sets you'll see cited constantly when comparing these families headto-head. Keep those names in your back pocket. Let's build the mental model that's [music] going to anchor everything else we talk about today. Same input document, two completely different [music] processing pipelines. Look at

the top lane first, the extractive path [music] in blue. The document comes in and the very first thing that happens is scoring every [music] sentence for importance. typically using something like TFIDF waiting or graph-based ranking. Then the [music] system selects the top end sentences, applying some diversity awareness so you don't end up with five sentences [music] all saying the same thing. Finally, it reassembles those selected sentences, usually restoring their original [music] order from the source document into the final summary. Notice what's never happening

here. [music] At no point does the system write a single new word. Every word in the output already existed in the input. Now [music] the bottom lane, the abstractive path in violet. The document gets encoded into contextual [music] embeddings. Essentially a rich

numerical representation of meaning. Then a [music] decoder generates the summary step by step, attending back to the source document at [music] each step to decide what to say next. It's actively generating new tokens, making novel word choices the source document may never have used. The output on top is a verbatim summary extracted, [music] not written. The output on the bottom is generated pros, paraphrased, and potentially expressed in ways [music] the original author never used. That's

the fundamental split. Everything else we discussed today is a consequence of this one architectural decision. Let's zoom in [music] to the extractive path first because it's the older, simpler, and honestly still the most widely deployed approach in production today. [music] The pipeline runs in five steps. Step one, segment the document into individual sentences. [music]

Normalizing whites space and encoding along the way. Sounds trivial, but [music] sentence boundary detection on messy real world text is its own small research problem. Step two, vectorize each sentence. [music] Historically, this meant TF IDF term frequency inverse document frequency. [music] But modern systems increasingly use sentence transformer embeddings [music] like the Bert sum family of models which capture semantic similarity far better than raw word overlap. Step three, build

a similarity [music] graph. You compute cosine similarity between every pair of sentences [music] and that becomes a weighted graph where sentences are nodes and similarity scores are edge weights. Step four is where it gets elegant. [music] You run page rank. Yes, the same algorithm Google used to rank web pages [music] directly on that similarity graph. This

is the core idea behind text rank [music] and lex rank. Sentences that are highly similar to many other sentences in the document [music] end up with high centrality scores and centrality becomes your proxy for importance. Look at the diagram [music] on the right. See that larger filledin node in the middle? That's a sentence with high centrality.

[music] It's similar to several other sentences, which signals it's likely covering a central theme of the document. Step five, [music] select your top K highest scoring sentences, applying a redundancy penalty, often [music] called maximal marginal relevance or MMR, so you're not just picking five near duplicate sentences. Now, [music] let's flip to the abstractive side and actually look at production code because the difference between a toy notebook implementation and something you'd actually ship [music] matters a lot here. What you're looking at is a small Python class called abstractive [music] summarizer built around hugging faces transformers library using [music] Bart, specifically Facebook/bart/large CNN, a model fine-tuned specifically for summarization. Walk through the structure [music] with me. We define a summary result data

class first. That's good practice. It gives callers a [music] typed structured return value instead of a loose dictionary. It carries the summary text, which model produced it, and whether the input had to be truncated. The summarizer class itself does [music] three things worth calling out, and you'll see them numbered on the right side of the slide. Number one, [music]

lazy model loading. Notice the pipeline is only constructed the first time summarize is called and then cached on selfcore [music] pipe. These transformer models can be several gigabytes. You do not want to reload that from disk on every single request. Number two, bounded generation. We pass max length

and min length explicitly [music] to constrain how long the decoder is allowed to run. And we set do sample to [music] false, which gives us deterministic, greedy, or beam search output. That matters [music] enormously in production. If your summarization endpoint isn't deterministic, your tests become flaky and your outputs become impossible to reproduce for debugging. And number three, this is the one engineers skip and later regret. Fail

loud, not [music] silent. Look at the except block. We're not just swallowing the exception. We log it with full context using logger.exception and then [music] we rease it as a typed runtime error with the original exception chained via from exe. That means callers

never silently receive a malformed or empty summary. They get a clear traceable failure they can handle or alert on. [music] This is a small function but it embodies three habits. caching expensive resources, constraining generation explicitly, and failing loudly that separate a demo from something you trust in [music] front of real users. Okay, we've seen how both methods work. Now, let's talk [music]

about how they actually behave once you put them into production because this table is the one I'd screenshot if you only remember one slide from today. Start with fluency. Extractive [music] summaries can read as stitched fragments because that's literally what they are. Sentences lifted from different parts of a document and glued together. Abstractive summaries [music] read as natural humanlike pros because they were generated as a coherent unit. Now, flip

that coin over. [music] Factual risk. Extractive is low risk here because it's reusing the sourc's exact [music] wording. It literally cannot say something the document didn't say. Abstractive carries meaningfully higher risk because the model can hallucinate.

[music] It can generate plausible sounding claims that were never in the source. Compute cost tells a similar story in reverse [music] of what you'd expect from the fluency line. Extractive is cheap, often CPU only since TF and graph ranking [music] don't need a GPU. Abstractive requires GPU decoding because you're running full transformer inference token by token. [music] training data. Extractive methods can

run unsupervised or with only light supervision. [music] Abstractive methods generally need large labeled document summary pairs to fine-tune on. Interpretability and latency follow the same [music] pattern. Extractive is traceable back to an exact source sentence and runs in singledigit milliseconds. [music] Abstractive is a black box internally and can take anywhere from hundreds of milliseconds to several seconds. None of this means

one approach is better. >> [music] >> It means they're tools with sharply different risk profiles and the right choice depends entirely on your use case. Let's talk about how you'd actually measure whether either of these systems is working [music] and where each one tends to break. On standard metrics, Rouge 1 and Rouge 2 measure engram [music] overlap with a reference summary and they're the default metric for extractive systems since extractive output is by definition made of source engrams. [music] Rouge L looks at longest common subsequence rewarding sentence level structural overlap. BERT [music] score

is the more modern choice. It's embedding based so it captures paraphrase quality [music] far better which matters a lot once you're evaluating abstractive output that may express the same idea in completely different [music] words. And increasingly critical factual consistency metrics [music] like sumaka or QAGS. These specifically check whether the claims in the generated summary are actually entailed by the source document. Once your model can invent text, [music] Rouge alone can't tell you if it invented something false. You need a

metric built for that. Now, [music] the failure modes, and I want you to remember these because they're the ones that show up in your bug report 6 months after [music] launch. Extractive systems suffer from redundancy. Top ranked sentences often restate each other, which is exactly why that MMR diversity penalty from slide 4 matters. They also suffer from dangling references. A

selected sentence's pronoun can lose its anticedant once you rip it out of its surrounding context. [music] Abstractive systems have their own two classic failures. Hallucination, where the [music] decoder generates plausible but unsupported facts, and repetition loops, where beam search degenerates into repeating the same phrase over and over without a coverage penalty to stop it. So the decision rule at the bottom of this slide, pick [music] extractive when auditability outweighs fluency. Think legal, medical, compliance contexts. Pick abstractive when

readability and compression ratio matter more. Chat assistance, digests, [music] meeting notes. Let's ground all of this in real published systems. On the left, pointer generator networks from C. Leu, and Manning's 2017 [music] paper, memorably titled Get to the Point. This

is a hybrid design, and [music] I'd argue it's one of the most influential ideas in this space. The model has [music] a copy mechanism that can extract key phrases directly from the source, behaving extractively, [music] while still retaining a generator network that can produce novel connecting words when [music] needed. The result is meaningfully reduced hallucination compared to pure abstractive decoding because the model has an [music] explicit safe option of just copying when it's unsure. It was benchmarked on the CNN Daily Mail data set we [music] mentioned earlier. on the right, Pegasus from Ciang and colleagues at Google in [music] 2020. What's clever

here isn't the architecture, it's the pre-training objective. Pegasus pre-trains by masking [music] out entire gap sentences from a document and asking the model to regenerate them. That's a self-supervised [music] objective built specifically for summarization rather than borrowed from general purpose language modeling. [music] and it produced strong reported rouge gains over prior abstractive baselines, particularly on the XOM [music] data set. Two very different design philosophies, both still [music] directly relevant to how production summarization systems are built today.

So where is this space [music] heading? Let's talk about the pattern that's actually winning in production right now. Extract [music] then abstract. Here's the tension it's solving. instruction tuned [music] large language models made pure abstractive summarization accessible to basically anyone [music] with zero fine-tuning required. That's a huge win. But they

inherited the hallucination [music] problem and at a much larger scale because now you might be summarizing an entire codebase, [music] a legal contract or a long customer conversation, not just a short news article. The pattern practitioners are converging on is straightforward [music] in concept. Run a lightweight extractive past fur to retrieve the [music] most salient verifiable sentences using exactly the graph ranking techniques we covered earlier [music] and then hand only that reduced grounded context to the generator for rewriting. You get the fluency of abstractive generation constrained [music] by the safety of extractive retrieval. Three trends are worth tracking here. First, retrieval

grounded [music] generation. constraining the decoder to sentences a retriever already scored as relevant, [music] which directly shrinks the hallucination surface. Second, zero and fewot [music] prompting. General purpose LLM now match fine-tuned baselines on many summarization [music] benchmarks without any task specific training at all. And third, and I'd argue this is the most [music] important shift, faithfulness first evaluation. teams are

moving their primary quality metric away from rouge [music] and toward factual consistency scoring before anything ships to production. The takeaway isn't that abstractive [music] has won. It's that the best systems today are hybrids that borrow the strengths of both families. [music] Let's bring it together with five heruristics you can actually apply on Monday morning. One, [music] match risk tolerance to method.

high stakes text, legal, medical, compliance favors extractive [music] or an abstractive system with very tight constraints. Two, [music] hybrid pipelines usually win. Extract then abstract reduces [music] hallucination while keeping the fluency users actually expect from a summary. Three, rouge alone is not enough. pair it with a [music] factual consistency metric. Sumok QAGS or similar before you

[music] trust abstractive output in a production setting. Four, [music] extractive is cheap to run at scale. No GPU, no fine-tuning [music] required. It's a strong default when you're processing high volume on a limited budget. And [music] five, LLMs shift the trade-off. They don't remove

it. Zeroot fluency from modern language models [music] is genuinely excellent, but grounding that output in truth and verifying it before it reaches a [music] user is still squarely the engineer's job. No model architecture makes that responsibility [music] go away. Those five ideas are the ones I'd want you walking out of this room with. So [music] to close where we started, extractive and abstractive summarization aren't competing techniques [music] so much as they're two tools with very different guarantees. Extractive [music] gives you

safety and traceability. Abstractive gives you fluency and compression. And increasingly, [music] the systems worth shipping combine both. I want to [music] leave you with the same question we opened with because I think it's the right lens for evaluating any summarization system you build or buy. For this specific [music] use case, is it more dangerous to say something wrong or to say something unclear? Your

answer [music] to that question should drive your architecture, not the other way around. That's what I've bought for you today. I'd love to open it