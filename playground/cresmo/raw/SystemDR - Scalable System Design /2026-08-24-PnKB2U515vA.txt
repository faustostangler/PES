---
video_title: "Big Data Roadmap 2025: Spark vs Flink, Real-Time Data Processing & the Future of Big Data"
video_id: PnKB2U515vA
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=PnKB2U515vA
video_date: 2026-08-24
video_description: |
  Website Link: systemdrd.com
  Explore the evolution of Apache Spark and Apache Flink and discover the technologies shaping the future of big data engineering. This video provides an advanced Big Data roadmap for 2025, covering Apache Spark, Apache Flink, real-time data processing, stream processing, batch processing, distributed computing, data engineering, big data analytics, Kafka, data pipelines, cloud computing, lakehouse architecture, machine learning, and scalable data processing. Learn how Spark and Flink compare, where each framework fits into modern data architectures, and why real-time analytics, event-driven systems, and cloud-native data platforms are becoming increasingly important. Whether you're a data engineer, big data developer, ML engineer, or preparing for a data engineering career, this roadmap will help you understand the skills and technologies needed to build modern, production-scale data systems.
  #bigdata  #apachespark  #apacheflink #dataengineering  #realtimeanalytics
---
Welcome everyone. Today, we're doing a systems level tour of where the [music] two dominant big data engines, Apache Spark and Apache Flink, are actually headed [music] in 2025. This isn't a beginner comparison, and it isn't [music] a which one is better debate. It's an architectural first look at how two engines with very different histories, Spark's [music] batch-first data frame heritage and Flink's streaming-first data flow heritage, are converging on nearly identical capabilities, continuous processing, disaggregated state, and a shared lakehouse foundation. [music] I'm assuming you're comfortable with distributed execution, DAGs, and at least one of these two engines already.

>> [music] >> Here's the arc we'll follow. We'll start with one shared mental model, move into the concrete architecture and code differences, talk through production tradeoffs [music] and failure modes, walk through a real case study, look at where the frontier is heading, and close with the principles you should actually walk away with. Before we dive in, quick gut check. How many of you are running Spark, Flink, or both in production [music] today? Keep that in mind as we go, because most teams don't choose an engine in a vacuum. They inherit one from a [music]

previous hire or a vendor platform, and then spend years working around its limits [music] instead of understanding when the other engine might actually fit better. That's the gap we're closing today. >> [music] >> Let's start with why the old batch versus streaming split doesn't hold up anymore. So, why is batch versus streaming no longer the right way to frame an engine choice in 2025? Three things changed.

First, [music] both engines now expose unified APIs. Spark's data frame and SQL layer, and Flink's table API, both treat a bounded table and an unbounded stream as the same [music] logical abstraction. That means the streaming versus batch question has moved out of the API [music] layer entirely and down into the execution layer, which is where the real engineering differences now live. Second, lakehouse formats, Iceberg, [music] Delta Lake, Paimon, are pulling both engines toward the same storage substrate. Increasingly, the real architectural decision isn't [music] which table format to use, it's which execution engine you run against a shared set of tables.

Third, latency expectations have compressed hard. Fraud scoring, [music] ad bidding, operational ML feature pipelines, these workloads now assume sub-second freshness as [music] table stakes, not a stretch goal. A few years ago, teams could get away with a nightly batch job feeding a warehouse. Today, [music] product and risk teams routinely ask, "Why can't this be live?" And increasingly, the honest answer is, "It can be. We just

haven't re-architected for it yet." [music] And that pressure is precisely what's driving both engines' 2025 releases, Spark 4.0 and Flink 2.0, [music] which reorganize their internals specifically to hit that sub-second bar. The rest of this talk explains how.

To understand where both engines are going, [music] we need one shared mental model of how streaming architecture itself has evolved. Let's look at that now. >> [music] >> This is the one mental model I want you to hold on to for the rest of the talk, three stages of architectural evolution. Stage one, [music] lambda architecture. Teams ran a batch layer, correct but slow, and a speed layer, fast but approximate, in parallel, then reconciled the two downstream. That

doubled the code base and was a constant source of subtle correctness bugs whenever the two layers [music] drifted out of sync. Stage two, Kafka architecture. This replaced that split with a single append-only log, [music] Kafka or Pulsar, that both real-time consumers and batch re-processing jobs read from. One source of truth. But you typically still have two [music] separate compute layers sitting on top of that log.

Stage three, and [music] this is where we are now, the unified streaming lakehouse. Spark 4.0 [music] and Flink 2.0 both read and write the same Iceberg or Paimon tables. The log itself becomes queryable table state, and the choice of engine becomes a one-time decision, not an architectural fork.

Notice the drivers underneath the arrows. [music] Operational complexity is what pushed teams off lambda, and the arrival of real streaming sequel plus mature lakehouse [music] format is what's pushing the whole industry into stage three right now. If your organization [music] is more than about five years old, you probably still have lambda era scars somewhere in your stack. A batch reconciliation job nobody wants to touch because [music] it fixes the streaming numbers every night. Stage three isn't just an academic upgrade. It's the thing that finally

lets you delete that job. With that model in place, let's look at how Spark 4.0 [music] and Flink 2.0 concretely implement stage three under the hood. This is our core architecture slide.

[music] Two stacks, four layers each, same underlying goal. Starting with Spark 4.0. Spark Connect [music] is now the default client mode. It splits the driver process from the application process over GRPC. So, Spark

is usable from thin clients and notebooks without pinning a long-lived JVM [music] session. Layer two is the vectorized native execution engine, whole stage [music] code generation over columnar batches, and that's what lets Spark compete on raw per core throughput. Layer three, [music] adaptive query execution, replans joins and shuffle partitioning mid-query using live [music] statistics instead of static estimates, which matters a lot on skewed production data. And layer four, structured streaming, remains micro-batch by default, but the experimental continuous processing mode is Spark's attempt to close the latency gap with Flink. Now, Flink 2.0. [music]

Layer one, and honestly the single biggest 2.0 architectural change, disaggregated state. The state back end moves [music] off local disk onto remote storage, decoupling how much state a job can hold from how many task slots [music] it has. Layer two, async non-blocking checkpointing, removes the barrier alignments stalls [music] that used to turn checkpoints into a three-putt cliff under skew. Layer three, the unified runtime, treats [music] bounded batch jobs as a special finite case of the same unbounded data flow model. No separate batch engine

bolted [music] on. And layer four, materialized tables, is Flink's declarative bridge straight into the lakehouse. One SQL statement can define [music] a self-refreshing table instead of a manually orchestrated job. Here's the production reality worth remembering. Disaggregated state is the change that [music] unlocks Flink jobs with terabytes of keyed state that previously required painstakingly tuned local RocksDB volumes. Teams can now

scale and compute and state independently for the first time. Let's make all of this concrete [music] with actual code. >> Same logical pipeline, two engines. A one-minute tumbling window aggregation over a click [music] stream. On the left, PySpark. We read a stream

from Kafka, apply [music] a 30-second watermark to bound how late an event can arrive and still be counted, group into a one-minute tumbling window, and [music] write out with a one-second processing time trigger. That trigger interval is the important part. [music] It's what pushes Spark toward near continuous behavior, rather than its traditional multi-second [music] micro-batch default. On the right, the equivalent in Flink SQL. A create table backed by Kafka, a tumble table value function over that same one-minute window, and [music] this is the key line, a refresh mode of continuous in the width clause. That's

what makes this a materialized table. The result stays live and self-refreshing, rather than being a one-shot query result. >> [music] >> The takeaway at the bottom of the slide is the one to remember. Functionally, these are the same aggregation. The engine choice only changes what happens [music] underneath. The checkpoint

protocol, the state back end, not the semantics [music] an analyst or downstream consumer sees. One production caveat, >> [music] >> don't assume that one second Spark trigger gives you Flink-like latency. A one second microbatch still carries [music] scheduling and shuffle overhead per batch that a true event at a time Flink pipeline doesn't pay. If you need consistent sub 200 millisecond latency, [music] Spark's continuous mode won't fully close that gap yet. Now, let's talk about how these differences actually show up in production, latency, throughput, and operational cost. [music] This table is

the cheat sheet slide, the one people screenshot. Quickly through the rows, and remember, these are directional, not absolute. [music] Latency, Spark's continuous mode narrows the gap, but Flink still wins on tail latency for true event at a time processing. >> [music] >> Throughput, both engines push very high sustained volume, >> [music] >> but Spark's strength shows up more in large batch and mixed workloads. Flink's in sustained event-driven load.

State back end, this is the most consequential row. >> [music] >> Flink's RocksDB and disaggregated state were purpose-built for large, long-lived keyed state in a way Spark's shuffle-oriented model wasn't. Checkpoint model, Flink's async incremental checkpoints carry lower overhead under skew than Spark's batch-oriented commit protocol. Ecosystem, >> [music] >> Spark's breadth, SQL, MLlib, notebooks, BI connectors, is [music] still unmatched. Flink's depth on stateful event-driven applications is unmatched.

And learning curve, teams from a SQL or data frame background ramp on Spark faster, [music] while Flink asks you to reason explicitly about watermarks, state PTL, and back pressure. That learning curve row is the one that gets underestimated in planning. Teams budget for the Flink cluster setup, but not for the [music] weeks it takes engineers to get comfortable with event time semantics and back pressure. And that's often the [music] larger real cost. Understanding tradeoffs in the abstract is one thing. Let's look at how they

actually fail in production and how to decide between the two. Three failure modes worth knowing cold. [music] Back pressure cascades. A slow sink, a database, an external [music] API can't keep up, and that slowness propagates upstream through the entire pipeline. Fix it with [music] buffer de-bloating and rate limiting at the source, not just by scaling the sink.

>> [music] >> Checkpoint timeouts. A small number of hot keys accumulate disproportionate [music] state, blowing your checkpoint SLA. Disaggregated state and incremental snapshots address this directly by removing the stop the world barrier [music] alignment problem. Data and key skew is really the root cause behind both of the above. A handful of hot keys, [music] a viral post, a whale trading account, overload a single task. [music] Standard

mitigation, local pre-aggregation before the shuffle, plus salted partitioning to spread that hot key artificially. On the decision [music] side, choose Flink when sub-second SLAs, large keyed state, or complex event time semantics dominate. Choose Spark when the workload mixes batch, ML, and BI, >> [music] >> and one to five second freshness is acceptable. And increasingly, run both against a shared iceberg or Paimon [music] lakehouse, so each engine owns the stage it's best at. One more note. Skew bites hardest

because it's invisible in development and staging. A pipeline that passes every load test can fall over the first time a real hot key shows up. So per key load monitoring should be non-negotiable for any keyed [music] streaming job. Let's ground all of this in a a named production example. Two real publicly documented stories, one [music] per engine, Uber and Flink.

Uber's engineering blog has long documented [music] one of the industry's largest Flink footprints, powering surge pricing, ETA estimation, and fraud detection. >> [music] >> Workloads that need per driver and per trip keyed state, strict event time correctness, and an always-on [music] operational SLA. That's exactly the profile Flink's disaggregated state and async checkpointing were built [music] for. On the other side, lakehouse platforms and Spark. Large lakehouse [music] deployments run mixed batch ETL, BI queries, and ML feature engineering against the same Delta or Iceberg tables, leaning on Spark's vectorized [music] engine and adaptive query execution. The value here isn't raw

latency. It's that one engine spans SQL, data frames, and ML lib, [music] so a single team can own the whole pipeline from ingestion through model training without handing off between specialized systems. [music] Notice neither of these organizations picked their engine off a spec sheet. Uber's choice reflects a company built around real-time operational decisions from day one. Lakehouse-first companies [music] optimized for cross-functional velocity across data engineering, analytics, and ML. The lesson? Pick

based on your [music] actual workload mix and team structure, not a generic benchmark. Zooming out from these two examples, let's look at where both ecosystems are headed next. This is [music] the beyond the roadmap slide. The trend that matters more than either engine's individual feature [music] list. The real story of 2025 isn't Spark versus Flink. It's both engines converging on a

shared streaming lakehouse substrate, [music] with feature stores and vector indexes becoming first-class consumers of that stream, >> [music] >> right alongside BI dashboards. Three things driving that. Streaming table formats, Apache Paimon and streaming-oriented branches of Iceberg, >> [music] >> turn the lakehouse itself into a change log. So, a A isn't just a snapshot anymore, It's a queryable stream of changes. Materialized and declarative tables, Flink's materialized tables, Spark's declarative pipeline APIs, [music] push orchestration that used to live in Airflow DAGs directly into the engine itself. And AI-native feature pipelines

are the newest consumer of this substrate. [music] Online feature stores and vector indexes now subscribe to the same unified stream [music] that feeds your dashboards. So, the freshness guarantees we discussed all talk long directly determine how current your models' features are [music] at inference time. One pattern worth watching. Teams building feature stores [music] today are increasingly skipping a dedicated serving layer entirely and reading straight from materialized lakehouse tables. That collapses what used to be a

three-system pipeline into one [music] at the cost of needing the engine to hit tighter latency and consistency guarantees than pure [music] analytics ever demanded. Let's distill everything into the principles you should actually walk away with. >> Five decision-making heuristics, not [music] a recap. One. Pick the engine by latency budget, not by habit. Sub-second and heavy keyed

state [music] points to Flink. Mixed batch, BI, and ML with one to five-second freshness points to Spark. Two. [music] Let the lakehouse be the contract between systems. Standardize on Iceberg or Paimon, and either engine can read or write the same tables without a migration project when requirements change.

>> [music] >> Three. Design for disaggregated state as a first-class [music] capacity planning dimension independent from compute. This is the architectural shift that most changes [music] how you size a Flink cluster in 2025. Four. Treat checkpointing as a reliability [music] feature you actively tune, not a default you leave alone.

Async incremental checkpoints are the difference [music] between a transient blip and a full SLA breach during a skew event. Five. plan for streaming to feed AI [music] systems from day one. Feature stores and vector indexes are now first-class [music] stream consumers alongside dashboards. So, the freshness decisions you make early affect [music] model quality downstream.

If there's one heuristic to over-index on, it's the first. The single most common architecture mistake in [music] this space is choosing an engine based on team familiarity rather than the workloads' actual latency and state requirements, [music] and paying for that mismatch in operational pain for years afterward. For anyone who wants [music] to go deeper, here are a few resources to continue with. Quickly, the official Spark and Flink documentation [music] is the fastest way to verify exact current behavior of Spark Connect, structured streaming [music] triggers, disaggregated state configuration, and materialized tables, since these subsystems are evolving [music] release to release. The Iceberg and Paimon specifications are worth reading [music] directly if you're designing that shared lakehouse contract we talked about. And the Uber

Engineering blog post on Flink at scale is a great next read if you want the operational detail behind the case study, state sizing, checkpoint tuning, [music] incident patterns at real production scale. One caution, documentation for both engines' newest features moves faster than most third-party tutorials can keep up with. Treat blog posts [music] and conference talks as directional, and always cross-check specifics against the current official docs before making an architecture decision. That wraps the deep dive. Let's open it up for questions.

Thank you all. Quick recap [music] in one breath. We went from why the batch-stream split is collapsing through the concrete architecture and [music] code differences between Spark 4.0 and Flink 2.0 into production tradeoffs in a real [music] case study, and out to where the whole ecosystem is heading with streaming lakehouses and AI-native pipelines.

>> [music] >> I'd love to take questions on any part of this. And if it helps get the conversation going, the question [music] I get most often after a talk like this is which one should I start with if I'm green-lighting a new pipeline today? [music] The honest answer, default to whichever engine your team already runs. Unless the new workloads latency or state requirements [music] clearly cross the thresholds we walked through on the trade-off slides. Is anyone here actively evaluating a migration or building a new pipeline right now? I'd love to hear specifics.

Happy to dig into it together. Thanks again. [music] My contact information is on the screen if anything comes up after today.