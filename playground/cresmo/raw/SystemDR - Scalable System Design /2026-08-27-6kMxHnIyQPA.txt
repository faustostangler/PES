---
video_title: "Data Catalog & Data Discovery Explained | Build a Robust Data Catalog for Data Engineers"
video_id: 6kMxHnIyQPA
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=6kMxHnIyQPA
video_date: 2026-08-27
video_description: |
  Website Link: www.systemdrd.com
  Learn how to design a robust Data Catalog and Data Discovery system for Data Engineers and build a scalable foundation for metadata management, data governance, data lineage, data quality, and self-service analytics. This video explains the architecture and key components behind modern data catalogs, including metadata ingestion, automated metadata discovery, dataset search, schema discovery, business metadata, data ownership, lineage, data classification, data quality, and intelligent data discovery across data lakes, data warehouses, and enterprise data platforms. Whether you're a Data Engineer, Analytics Engineer, Data Architect, Data Scientist, or Data Platform Engineer, this guide will help you understand how to build a production-ready data cataloging and discovery platform that makes enterprise data easier to find, understand, trust, and use. Relevant topics include Data Catalog, Data Discovery, Metadata Management, Data Engineering, Data Governance, Data Lineage, Data Quality, Data Lake, Data Warehouse, Data Mesh, Data Platform, Enterprise Data Catalog, Metadata Catalog, and Self-Service Analytics. These terms closely match the language used in relevant YouTube content and current data-catalog documentation.
  #dataengineering  #datacatalog #datadiscovery  #datagovernance  #metadata
---
Welcome [music] everyone. Today, we're tackling one of the least glamorous, most expensive [music] problems in data engineering, making data findable and trustworthy at scale. A data catalog [music] isn't a nice-to-have wiki of table names. It's the metadata control plane for your whole data [music] platform. It answers what data exists, can I trust it, who owns it, and what [music] breaks if I change it. As

organizations accumulate thousands of tables, streams, and dashboards across multiple warehouses [music] and lakes, manual tribal knowledge stops scaling somewhere between the 500th and [music] 2000th data set. Today, we'll design that system properly, the metadata [music] model, the ingestion architecture, the storage tradeoffs, and where the field is heading with AI-native [music] discovery. So, why does this become a real engineering problem and [music] not just a documentation nice-to-have? Widely cited industry surveys consistently find that data practitioners somewhere between 40% and 60% of their working time just locating, vetting, and understanding data sets >> [music] >> before any actual analysis even begins. That number comes from four symptoms [music] that compound each other. First,

data set sprawl. Warehouses, lakes, streams, [music] and BI extracts multiply faster than any team can document them by hand. Second, tribal knowledge. [music] The one engineer who understood a table's caveats eventually changes teams, >> [music] >> and that context walks out the door with them. Third, duplicate pipelines. Teams

rebuild the same aggregation because it was faster [music] than finding or trusting the one that already existed. And fourth, broken [music] metric trust. Two dashboards disagree, and nobody can trace which source or transformation is actually [music] authoritative. I've seen platform teams run internal data set amnesty weeks [music] just to find and deprecate hundreds of undocumented tables. That's always the telltale [music] sign a catalog was needed a year earlier. Before we get

into architecture, we need one shared mental model. >> [music] >> A catalog is a metadata graph, not a list. Every data asset carries [music] three layers of metadata. Technical metadata is what a system can extract automatically. Schema, column types, partitioning, storage [music] format.

Operational metadata comes from actually running the pipelines. Freshness, lineage, data quality results, and usage statistics pulled from warehouse [music] logs. And business metadata is the human layer. Ownership, glossary terms, sensitivity tags, and SLA commitments. None of these three layers [music] is the catalog by itself. The catalog's

real job is connecting entities across all three into a graph. This table produced by this pipeline, [music] owned by this team, feeding this dashboard, containing this classified column. That graph is what powers real discovery and real impact analysis. What breaks if I change this column is a graph [music] traversal question, not a lookup. Teams that build a catalog as a flat [music] table of descriptions hit a wall fast. They can search names, but they

can't answer what depends on this, [music] because that requires relationships, not rows. Here's the reference architecture that [music] actually builds and serves that graph. On the left are data sources. Warehouses, lakes, [music] streaming topics, BI semantic layers. Two collection paths feed the pipeline. Pull

connectors [music] crawl schema catalogs on a schedule. Push listeners consume standardized lineage events, [music] most commonly using the open lineage format, emitted directly by orchestrators like Airflow, Spark, [music] or DBT at runtime. Both paths converge into an extraction and normalization layer, which diffs [music] schemas against the last known state, profiles basic statistics, and resolves deciding that [music] prod.orders and the orders table referenced in a DBT model are actually the same [music] node. Normalized metadata lands in two stores, a graph database holding data sets, pipelines, [music] and people as connected nodes, and a search index, increasingly a hybrid of full text and [music] vector embeddings, optimized for fast ranked discovery.

A metadata API layer exposes GraphQL [music] for graph traversal and REST for simpler CRUD and webhooks. And everything about [music] that, the catalog UI, IDE plugins, Slack bots, governance workflows, [music] consumes that same API, so there's exactly one source of truth. The graph store and the [music] search index are usually two separate systems kept in sync by the extraction layer. Trying to force [music] one database to do both jobs well well is a common early design mistake that teams end up re-architecting later. >> [music] >> Let's look at a simplified but realistic slice of that pull-based harvester in code.

This co-routine harvests [music] one table's metadata. It acquires a connection from an async pool, queries the schema catalog [music] and an approximate row count, builds a normalized metadata asset keyed [music] by a stable URN, a unique resource name, like URN:catalog:postgres:prod.orders, [music] and upserts it into the catalog through the metadata API from the last slide. Three details matter [music] here for production correctness. First, the retry decorator with exponential backoff. At scale, a

harvester touching tens of thousands of tables will hit connection [music] resets and timeouts constantly, and without backoff, a blip turns into a thundering [music] herd retry storm against the source database. Second, everything runs through pooled non-blocking connections, because [music] a synchronous harvester crawling one table at a time simply cannot keep pace with a warehouse adding hundreds of tables a week. And third, the most important detail for correctness, the upsert is idempotent. URNs [music] are deterministic, so rerunning the same harvest on a schedule updates the existing graph node instead of creating a duplicate. Teams that skip idempotent URNs [music] learn the hard way when a scheduler retry duplicates half the catalog overnight. URN design is worth getting

right before the first production run, not after. [music] With ingestion covered, let's talk about the tradeoffs teams actually argue about in production. >> [music] >> The first big decision, how does metadata actually get into the catalog? Pull-based crawling scans source systems on a schedule, [music] reading schema catalogs directly. It requires zero cooperation from [music] the teams that own the pipelines, which makes it the easy default. Its weakness

is staleness. Anything that changes between scans is invisible until the next run. [music] And at high data set counts, the scan itself becomes expensive. Push-based ingestion [music] flips this. Orchestrators emit structured lineage events, commonly using open lineage standard, [music] the moment a job runs.

That gives you exact runtime-accurate lineage instead of inferred lineage, and it scales with event volume rather than [music] a fixed scan window. The cost is instrumentation. Every framework in your stack needs integration, and historical backfill [music] is hard. The common production pattern is hybrid. Push-based lineage for anything orchestrated where instrumentation is a one-time cost, and scheduled pull crawling as a safety net [music] that catches manually created and unmonitored sources. The second [music] major

decision, where does the graph actually live? A native [music] graph database like Neo4j or JanusGraph models the catalog's actual shape directly, [music] so multi-hop questions like what dashboards break if I drop this column? >> [music] >> Stay fast. That's exactly the query pattern graph engines are built for. >> [music] >> The cost is operational. Fewer engineers

have run a graph database in production, >> [music] >> and very large graphs need real tuning attention. The alternative, relational plus search, stores entities in Postgres-style [music] tables with a search index like Elasticsearch layered on top. Teams already know how to operate this stack reliably, and simple tabular reporting is trivial. [music] But, relationship-heavy queries require either recursive joins that get slow past a few hops, or [music] maintaining a separate graph layer anyway, which means keeping two systems consistent. Several well-known [music] open-source catalogs, including LinkedIn's DataHub, made this trade-off explicit by pairing a graph-shaped metadata model with a [music] search index, rather than picking one store to do both jobs, which is exactly the architecture from slide four. Let's ground all of this in one

[music] well-documented, real-world build. LinkedIn built DataHub to address the exact [music] fragmentation problem we described earlier, at a scale of thousands of data sets, [music] pipelines, and dashboards across many teams. The design choices map directly onto what we've covered. It's push-first, with metadata change [music] events emitted by producing systems, closely mirroring the push-based pattern from [music] a couple slides ago. Its metadata model is graph-shaped. Data sets, pipelines,

dashboards, and people [music] as connected nodes, not flat rows, matching our mental model from slide three. And it serves discovery through a search and graph hybrid. [music] So, ranked full-text search and relationship-aware lineage queries both stay fast. LinkedIn open-sourced DataHub in 2020, >> [music] >> and it's since grown into one of the more widely adopted open-source catalogs. The detail worth remembering isn't [music] the specific tech stack, it's the sequencing. DataHub treated metadata

[music] as a streaming, event-driven problem from the start, rather than bolting push-based ingestion [music] onto a batch crawler years later, which is a much harder retrofit. One more idea [music] before we wrap up. Where this discipline is actively moving right now. >> [music] >> Three shifts are converging. First, semantic search. Instead of ranking

[music] results by keyword overlap with a table name, catalogs increasingly compute [music] embeddings over descriptions and sample values. So, a search for customer churn risk can surface a well-named but undocumented table because its [music] content is semantically close. Second, auto-drafted documentation. [music] Large language models given schema, sample rows, and query history can draft [music] a first-pass description for a data steward to review and approve. Turning documentation from a blank page [music] task into an edit task. And

third, hybrid lineage inference. Static analysis [music] of SQL and DBT models can trace column-level lineage without waiting for a job to run. [music] And combining that with runtime lineage events gives column-level high-fidelity impact analysis. The most important framing here, the catalog isn't [music] just a UI for humans anymore. It's increasingly the tool an AI agent calls before writing a query to check what a table [music] actually means and whether it's safe to use. The catalog is becoming the

retrieval [music] layer for AI agents, not just a directory that humans browse. Let's distill everything into five principles [music] you can apply on Monday. One, model metadata as a graph, [music] not a flat table. Relationships are the product, not an afterthought. Two, default to push-based event-driven ingestion [music] wherever you can instrument it. And

treat scheduled poll crawling as a safety [music] net, not your primary mechanism. Three, treat documentation as a tracked workflow [music] with ownership and freshness SLAs baked into the catalog itself. A wiki page nobody owns [music] rots within a quarter. Four, invest in search relevance as seriously as coverage. [music] A catalog that returns 20 irrelevant results for every real one trains people to stop searching [music] and go back to Slack.

And five, design the catalog's API [music] assuming AI agents, not just humans, will be its primary caller going forward. Teams that get the graph model and ingestion [music] right first and polish search and AI integration second tend to avoid the expensive rebuilds that come [music] from bolting a graph model onto a catalog that started as a flat table. If you want to go deeper [music] than a single talk can cover, here's where to start. For hands-on evaluation, DataHub and Open Metadata are the [music] two most actively maintained open source catalogs today. DataHub leans push first and graph model, [music] matching the case study we just covered.

While Open Metadata unifies [music] schema, lineage, and data quality checks in a single model. Amundsen from [music] Lyft was an early and influential entrant focused specifically on discovery UX. Apache Atlas is the standard choice inside [music] Hadoop-centric ecosystems. On the standard side, Open Lineage is worth adopting regardless [music] of which catalog you choose, since it decouples your lineage event format from any single vendor. And for the underlying [music] discipline, the DAMA-DMBOK is the standard reference for data governance and metadata management practice broadly.

Most teams don't build a catalog [music] from scratch anymore. The realistic path is adopting one of these platforms and investing [music] engineering time in the connectors specific to your own stack. That's [music] the full arc, from the problem through architecture and tradeoffs to where discovery [music] is heading. To recap, dataset sprawl and lost trust create the discovery problem. The fix is [music] modeling metadata as a graph across technical, operational, and business layers. [music]

That graph gets populated through a push and pull ingestion architecture [music] and served through a search and graph hybrid store. Production teams make deliberate trade-offs on ingestion [music] model and storage engine rather than defaulting to one. And the frontier is extending [music] all of this to serve AI agents, not just human browsers. If you're starting from zero, the highest leverage first [music] step is almost never picking a catalog product. It's agreeing on a URL and entity naming scheme across [music] your team since every connector and every downstream integration depends on getting that right once.

Thank you. [music] I'd love to hear what the biggest metadata pain point is on your own platform. Let's open it up for questions. [music]