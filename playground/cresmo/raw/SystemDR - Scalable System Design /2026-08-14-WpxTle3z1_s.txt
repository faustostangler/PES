---
video_title: "High-Dimensional Data in Machine Learning: Model Selection, Feature Selection, Dimensionality"
video_id: WpxTle3z1_s
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=WpxTle3z1_s
video_date: 20260814
video_description: |
  Website Link: systemdrd.com
  High-dimensional datasets can make machine learning model selection much harder by increasing overfitting, computational cost, variance, and the risk of poor generalization. In this video, you'll learn how the curse of dimensionality affects model performance and how feature selection, dimensionality reduction, regularization, cross-validation, PCA, model evaluation, hyperparameter tuning, and feature engineering can help build better ML models. We explore practical approaches for selecting relevant predictors, reducing unnecessary features, improving model accuracy, and handling high-dimensional data with Python, Scikit-learn, machine learning, data science, and AI. Whether you're a data scientist, ML engineer, AI developer, or preparing for machine learning interviews, this tutorial will help you understand and solve high-dimensional modeling challenges.
  #machinelearning  #datascience  #python  #featureselection  #ai
---
Welcome to module four of the System DR advanced systems and ML education track, the [music] impact of high-dimensional problems on model selection and performance. Here's the question we're going to answer over the next 20 minutes. [music] What actually happens to your models when the number of features starts to rival or even exceed [music] the number of samples you have? It turns out the answer isn't just things get harder. It's that entire assumptions [music] baked into cross-validation, distance metrics, and even the classical bias-variance tradeoff quietly stop holding. By the end of this session,

you'll know how to recognize when that's happening, how to regularize [music] and validate correctly when it does, and where the cutting edge of research is actually challenging [music] what we thought we knew about overfitting. This is an advanced track session [music] aimed at ML engineers and data scientists who are already comfortable with regularized [music] linear models and cross-validation and want to understand exactly where those tools break down and what to do instead. Let's start with a number that tends to stop people in [music] their tracks. In 100 dimensions, 63% of a hypercube's volume [music] sits within the outermost 1% shell. Let that sink in for a second.

Almost 2/3 [music] of the space is crammed into a razor-thin layer near the surface. Almost nothing is left in what we'd intuitively think of as the middle. That single geometric fact is the root of the curse of dimensionality [music] and it cascades into three very practical problems for anyone building models. First, distances [music] stop discriminating. As the number of features grows, the ratio between your nearest neighbor's distance and your farthest neighbor's [music] distance converges toward one. K-nearest

neighbors, clustering, and density estimation [music] all quietly lose their signal. Second, cross-validation gets [music] optimistic. When your feature count approaches or exceeds your sample count, a single train test split can overfit the validation set itself. Naive cross-validation doesn't fail loudly. It just silently [music] inflates your reported performance.

And third, model selection becomes combinatorial. Exhaustively searching [music] subsets of your features is a two to the P problem. Past a few dozen predictors, [music] that's simply not feasible, which is exactly why we lean on regularization instead of brute force search. Keep these [music] three problems in mind. Everything else in this module is solving one or more of them.

>> This chart makes that curse of dimensionality concrete instead of abstract. What you're looking at is the percentage of a unit hypercube's volume [music] that falls within its outer 1% shell, plotted against the number of dimensions. [music] At one dimension, that's a trivial 1%. But look what happens [music] as dimensionality climbs. 5% at five dimensions, 18% at 20, [music] 63% at 100, and 87% by 200 dimensions.

Here's the intuition for why this happens. To stay in the core of the space, a point has to stay close to the center on every single axis [music] simultaneously. The probability of doing that across all of those axes at once shrinks geometrically as you add more of them. And this is exactly [music] the same underlying mechanism that causes nearest neighbor and farthest neighbor distances to converge. Relative distance contrast simply [music] collapses as dimensionality grows.

What this means in practice, >> [music] >> points in high dimensions live near the surface, not the interior. The local neighborhoods that K nearest neighbors, Parzen density estimators, and clustering algorithms [music] depend on become sparse and nearly empty. The takeaway is straightforward. Distance-based [music] methods need dimensionality reduction or a learned metric before you can trust them past roughly 15 to 20 [music] raw features. Now for the part that tends to surprise even experienced practitioners, >> [music] >> what actually happens to test error as we keep increasing model capacity past the point where the model can perfectly fit the training data. That's the

interpolation threshold marked in red, and it's actually the worst point on the entire curve >> [music] >> with test error spiking to its highest value. Keep going past it into the heavily over parameterized regime, and test [music] error falls again. That second decline is what Belkin and colleagues named double descent in 2019, >> [music] >> and it directly challenges the assumption that more capacity always eventually means more overfitting. Here's why this matters for model selection specifically. >> [music] >> If you're doing early stopping or searching over model capacity based on the old assumption of a single [music] U curve, you can easily stop your search right at the worst possible point, the interpolation threshold. Instead of

either pulling back to the classical [music] optimum or pushing all the way through to that second benign overfitting descent. We'll come back to exactly [music] when that second descent is trustworthy in a few minutes. Let's get concrete with a production quality implementation. This is a nested cross validation function written [music] for exactly the scenario we've been describing where your feature count approaches your sample count. >> [music] >> Notice the structure. There are two

loops here, not one. The inner loop, that's inside the Elastic Net CV model itself, >> [music] >> tunes alpha and L1 ratio. The outer loop, built with scikit-learn's cross_val_score, scores the tuned pipeline [music] only on folds that the inner loop never touched during tuning. That separation is the whole point. Why does this separation matter so much?

If you use a single cross [music] validation loop that both tunes your hyper parameters and reports your final score, you're reusing the same folds twice. In a high-dimensional setting, that can inflate your reported [music] R squared by 15 to 30%. That's not a rounding error. That's [music] the difference between a model you should ship and one you shouldn't. Second, >> [music] >> notice that we're using elastic net here, not plain lasso. We're sweeping L1

ratio across a range from 0.1 up to 1.0. That lets correlated [music] feature groups, which are extremely common in genomics data and in embedding spaces, survive together in the model, instead of [music] lasso arbitrarily picking just one representative feature group and zeroing out the rest. And finally, look at that try except block wrapped around the cross-validation call. This isn't

decorative. High-dimensional pipelines fail [music] in genuinely confusing ways. Singular matrices, NaN propagation from sparse columns, and re-raising the error with the actual shape of your input array turns a cryptic solver stack trace into something an on-call engineer can actually act on it 2:00 in the morning. [music] >> Not every dimensionality reduction technique serves the same purpose, and picking the wrong one for the job is one of the most [music] common production mistakes I see. On the left, we have PCA, principal component analysis. It's linear, [music]

it's fast, about 1.2 seconds on 20,000 features and 800 samples using a randomized SVD, [music] and it's fully deterministic. Its components are linear combinations of your original features, so the loadings are genuinely inspectable. [music] This makes PCA the right default when you're preprocessing before a linear or tree-based model, and especially when your features are noisy and [music] correlated. On the right, we have the non-linear methods, UMAP and t-SNE.

These preserve local [music] neighborhood structure far better than PCA can, which is exactly why they're so good for visualization and cluster discovery. But [music] they come with real costs. They're slower, roughly 8 to 15 seconds on that same data. They're stochastic, so [music] we need to fix a random state for reproducibility. And critically, their axes carry no direct meaning at all.

That last point is the one that trips [music] teams up. A common anti-pattern is feeding UMAP output directly into a production classifier as [music] if it were just another feature transform without re-validating on every retrain. UMAP's transform step has no [music] guarantee of reproducing the same geometry run to run. Use it for exploratory analysis and cluster QA, >> [music] >> be very careful before you wire it silently into a production model. Once you've decided how many features to keep, [music] you still have to decide how to constrain the coefficients on whatever remains. This table compares

your three main options. L1 or Lasso [music] drives coefficients exactly to zero, which gives you sparse, easy-to-explain models. But when several features [music] are correlated, Lasso picks one of them somewhat arbitrarily and zeros out the rest, which means your selected feature set can be unstable [music] across retrains. Use it when your true signal is genuinely sparse and feature selection itself is the goal. L2 [music] or Ridge never zeros anything. It

shrinks all coefficients towards zero, but keeps them [music] all in the model. It distributes weight evenly across correlated features, which is more stable, though less interpretable. Use it when multicollinearity dominates [music] and you believe most of your features carry at least some real signal. Elastic Net blends both, using that L1 [music] ratio parameter we saw in the code a moment ago. It selects correlated feature groups together rather than arbitrarily picking one. This is [music]

why it's the safer default, particularly in genomics and embedding-heavy pipelines [music] where correlated blocks of predictors are the norm, not the exception. The decision rule I leave [music] you with, start with elastic net. Sweep L1 ratio from 0.1 to 1.0, and let cross-validation find where on that spectrum your data actually lives.

>> [music] >> It degrades gracefully to either lasso or ridge at the extremes, so you're rarely committing to the [music] wrong prior up front. Let's ground all of this in a real publicly documented data set. [music] The Cancer Genome Atlas pan-cancer classification problem. Look at these numbers. 20,531 [music] gene expression features. Roughly 800

tumor samples per cohort. [music] That's a 25 times ratio of features to samples. About as extreme a P greater than N scenario as you'll encounter in production machine learning. >> [music] >> And yet, with the right pipeline, this problem is absolutely solvable. 94.6%

[music] held out accuracy using nothing more exotic than elastic net regression and nested cross-validation. [music] Here's how the pipeline actually handles a 25 to 1 [music] feature to sample ratio in three stages. Step one, a variance filter drops the bottom 40% of genes by expression variance before any modeling happens, cutting the feature count down to around 12,000 with negligible loss of real signal. Step two, elastic net with nested cross-validation. Exactly the pattern from our code slide, >> [music] >> where the outer five-fold loop estimates generalization, and the inner five-fold loop tunes alpha and L1 ratio, avoiding the leakage we [music] discussed earlier.

And step three, stability selection. The pipeline reruns on 200 bootstrap resamples, [music] and only genes selected in more than 60% of those runs make it into the final reported biomarker panel. That third step is worth pausing on. It's a useful reminder that a single [music] high accuracy number on its own doesn't tell you whether your feature selection is trustworthy. Stability [music] across resamples is what earns that trust.

We opened this module by treating more parameters than data as a warning [music] sign. Let's complicate that a little because this is genuinely where current [music] research is pushing. Belkin and colleagues 2019 double descent finding, the curve we looked at on slide four, >> [music] >> and Bartlett and colleagues 2020 formalization of what they call benign overfitting, >> [music] >> together show that certain over parameterized models, including some over parameterized [music] linear regressions, can generalize well even when they perfectly interpolate noisy training data. [music] The key technical condition, according to Bartlett's work, has to do with how variance spreads across the [music] many near zero eigenvalue directions of your feature covariance matrix, not simply how many parameters your model has. I want to be very clear about what this is not. [music]

This is not a green light to abandon regularization. It's an active area of research with narrow, well characterized conditions under which [music] it holds. Benign overfitting is a property of specific architectures and specific noise structures. It does not generalize to arbitrary high dimensional linear models without some implicit bias toward low norm solutions [music] baked into how they're fit. So, here's the practical heuristic to take away. If you must operate near or

past the point where your feature count equals [music] your sample count, prefer model families that carry implicit regularization. Deep networks [music] trained with stochastic gradient descent or minimum norm solvers over raw unregularized linear fits. And regardless of which regime you believe you're operating in, validate with the nested cross validation [music] strategy from slide five, every time. Let's consolidate everything [music] into five heuristics you can carry directly into your next high dimensional project. One, reduce or regularize before you trust distance. [music]

K nearest neighbors, clustering, and kernel methods need PCA, UMAP, or a learned embedding once your feature count exceeds roughly [music] 15 to 20 raw features. Two, always nest your cross-validation [music] once your feature count approaches your sample count. A single CV loop that both tunes and scores reuses the same folds, >> [music] >> and that can inflate your reported performance by 15% to 30%. Three, default to elastic net. Overcommitting [music] early to pure lasso or ridge. Sweeping L1 ratio

degrades gracefully [music] to either extreme, so you're rarely locked into the wrong prior. Four, over-parameterization is not automatically overfitting. [music] Past the interpretation threshold, test error can fall again, but only for model families that carry implicit regularization, [music] and only under specific conditions on how noise is structured. And five, report [music] feature selection stability, not just accuracy. A single high accuracy run can hide a feature set that's genuinely unstable [music] across retrains. Bootstrap or

stability selection frequency is what actually earns stakeholder [music] trust. Run through these five before you ship any model where feature count starts to rival sample count, and you'll catch the leakage, instability, and false confidence [music] issues this module has covered before they ever reach a production dashboard. A few places to go deeper. The two papers referenced [music] throughout this module, Belkin, Hsu, Ma, and Mondal's 2019 PNAS paper on reconciling modern machine learning with the [music] bias-variance tradeoff, and Bartlett, Long, Lugosi, and Sigler's 2020 [music] PNAS paper on benign overfitting in linear regression are the primary sources behind everything we covered on the double descent slide [music] and the frontier insight slide. For implementation, scikit-learn's own documentation on cross-validation is the best reference [music] for the nested CV pattern from slide five. It's the single

most commonly misimplemented pattern in this whole module, so it's worth bookmarking. The UMAP learn documentation covers the tool referenced on flight six. >> [music] >> And if you want to explore the actual data set behind our case study, the TCGA PanCancer Atlas is publicly available through the Genomic Data Commons. [music] All of this, along with the course itself, is linked at the course home address shown at the top of the slide. That's your fastest [music] way back into the full module. That brings us to

the end of the core content for this module. Thank you for working through high-dimensional [music] model selection with me. I want to leave you with an open question, deliberately. If the [music] number of features in your systems keeps growing, whether that's from feature engineering, from ingesting inherently high-dimensional data like genomics or embeddings, or just from scaling an existing pipeline past its original design [music] assumptions, what's the next thing that breaks? If you've got your own P greater [music] than N scenario sitting on your desk right now, bring it. We can walk through

the reduction, regularization, and validation strategy together, live, using exactly [music] the patterns we covered today. And when you're ready to continue, you'll find module five waiting [music] at the course link on screen now. Thanks again for your time.