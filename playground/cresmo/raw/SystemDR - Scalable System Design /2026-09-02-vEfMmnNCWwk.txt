---
video_title: "Abstractive vs Extractive Summarization"
video_id: vEfMmnNCWwk
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=vEfMmnNCWwk
video_date: 2026-09-02
video_description: |
  Website Link: www.systemdrd.com
  What's the difference between Abstractive and Extractive Text Summarization? Learn how NLP and AI models summarize articles, documents, and large amounts of text using Transformers, LLMs, GPT, BERT, T5, Python, and Hugging Face. Discover how extractive methods select key sentences while abstractive models generate new summaries that preserve the original meaning.
---
Let's [music] build the mental model that's going to anchor everything else we talk about today. Same input document, [music] two completely different processing pipelines. Look at the top lane first, [music] the extractive path in blue. The document comes in and the very first thing that happens is scoring every sentence [music] for importance, typically using something like TF-IDF weighting or graph-based ranking. Then the system selects the top end sentences, applying some diversity awareness so you don't end up with five sentences [music] all saying the same thing.

Finally, it reassembles those selected sentences, [music] usually restoring their original order from the source document, into the final summary. Notice [music] what's never happening here. At no point does the system write a single new word. Every word in the output already existed in the input. Now at the bottom lane, the abstractive [music] path in violet. The document

gets encoded into contextual embeddings, essentially a rich numerical representation of meaning. >> [music] >> Then a decoder generates the summary step-by-step, attending back to the source document [music] at each step to decide what to say next. It's actively generating new tokens, making [music] novel word choices the source document may never have used. The output on top is a verbatim summary, extracted, [music] not written. The output on the bottom is generated prose, paraphrased, and potentially expressed [music] in ways the original author never used.

That's the fundamental split. Everything else we discuss today [music] is a consequence of this one architectural decision.