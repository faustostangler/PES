---
video_title: "Data Quality & Data Validation in the Modern Data Stack | Data Engineering, dbt & Data Observability"
video_id: R_7-uiP68p0
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=R_7-uiP68p0
video_date: 2026-08-31
video_description: |
  Website Link: www.systemdrd.com
  Learn how to build reliable, production-ready data quality and validation frameworks for the modern data stack. This video explores essential data quality, data validation, data observability, data engineering, data pipelines, data testing, data governance, data integrity, data profiling, schema validation, data contracts, dbt tests, Great Expectations, automated data quality checks, pipeline monitoring, and data reliability practices. Discover how modern data teams detect missing values, duplicate records, schema changes, freshness issues, anomalies, and broken upstream dependencies before they reach analytics dashboards, machine learning models, or business-critical systems. Whether you're a Data Engineer, Analytics Engineer, Data Scientist, MLOps Engineer, or AI Engineer, this tutorial will help you design scalable validation workflows and improve trust in data across the modern data stack.
  #dataengineering  #dataquality  #datavalidation  #dataobservability #moderndatastack
---
Welcome [music] everyone. Today, we're digging into something a lot of AI and search teams treat as one topic [music] when it's really two: data mining, and specifically the difference between information retrieval [music] and information extraction. If you've ever built a search feature, a chatbot, or a retrieval [music] augmented generation pipeline, you've made a choice, maybe without realizing it, about which of these two paradigms [music] you're actually building. Get that choice right, and your system behaves predictably. Get it wrong, [music] and you end up with something that looks fine in a demo and falls apart the moment someone asks a precise factual question.

So, here's the plan. [music] We'll trace where both fields came from, look at how each is actually architected in production, >> [music] >> talk through where each one breaks, walk through a system you've all used, Google Search, as a live example [music] of the two working together, and finish with where large language models are taking all of this next. [music] Let's get into it. Let's start with why this still matters. Every [music] modern search box, every chatbot, every rag pipeline is silently [music] making a choice.

Surface relevant documents or surface structured [music] facts. Those are not the same job. Get that choice wrong, >> [music] >> and errors compound downstream in very specific ways. A system that's architecturally built for retrieval, but gets [music] asked a precise factual question, will hedge. It'll surface a passage that's plausibly related without ever actually committing to an answer.

Meanwhile, a [music] system built purely for extraction will miss the long tail of questions nobody [music] anticipated when the schema was designed, because extraction only knows how to answer what it was built to recognize. [music] These two disciplines also have very different birth dates. Information retrieval traces back to the 1960s [music] with Boolean retrieval and systems like SMART formalizing how we rank documents against [music] a query. Information extraction, on the other hand, wasn't formalized as a task until the DARPA and NIST Message [music] Understanding Conferences, the MUC series, ran from 1987 through 1998, defining what it actually means to fill [music] in a structured template from raw text. And that's the core tension you'll see repeated throughout this deck. IR

optimizes [music] for coverage, surfacing anything plausibly relevant. IE optimizes for precision of structure, committing to a specific factual claim. Production systems generally need [music] both. The mistake is deploying them by default instead of by design. So, let's fix one shared mental model [music] before we go any further, because everything else in this talk builds on it.

Information retrieval answers [music] one question: Which documents are relevant? You give it a corpus and a query, and it hands you back a ranked list of [music] documents. It's reasoning at the level of the whole document. It never actually has to understand what's inside a page, only estimate how likely that page is to satisfy the query. >> [music] >> That's why its evaluation metrics are things like precision at K, recall, and NDCG, all about ranking quality, >> [music] >> not correctness of any single fact.

Information extraction answers a completely [music] different question: What facts does this text actually state? You give it unstructured text, and it hands you back structured tuples, a graph of entities, spans, and relations. >> [music] >> It's reasoning at a much finer grain, individual entities and the relationships between them, which is exactly why it's more computationally and label intensive [music] than IR. Its metrics reflect that, too. Slot F1, entity F1, exact match, [music] all about whether the structure it committed to is actually correct.

Neither one is harder in some abstract [music] sense. They're solving genuinely different problems. If you ever catch yourself comparing accuracy numbers across an IR system [music] and an IE system, that's usually a sign the comparison itself doesn't make sense. Let's make this [music] concrete, starting with how an information retrieval system is actually built. There are five stages here, and almost [music] every production search system you've used follows the same shape.

First, ingestion. A crawler or connector [music] pulls raw documents into the system. Second, analysis. That raw text gets tokenized, [music] normalized, stemmed, or lemmatized into a consistent stream. Third, and this is the load-bearing wall of the whole system, the [music] inverted index. Every term in the

vocabulary maps to a sorted list of document IDs that contain it. That's what turns a search over [music] billions of documents into a lookup that only scales with the number of terms in your query, instead of the size of the entire [music] corpus. Fourth, the query processor parses and expands the user's query. >> [music] >> Synonym expansion, spelling correction, that kind of thing. And fifth, the ranker scores and sorts the candidates.

>> [music] >> Historically, TF-IDF, then BM25, which is still an industry default today, and increasingly layered with learned ranking models trained on [music] real click-through data. One thing worth calling out, modern systems don't run this sparse pipeline in isolation anymore. >> [music] >> They typically pair it with a dense vector index, embeddings plus approximate nearest neighbor search, and blend the two signals together. [music] That's what people mean by hybrid search, and it's quickly become the default, rather [music] than the exception. Now, let's look at the mirror image, how an information extraction pipeline is built, and then walk [music] through what a production implementation actually looks like in code. Extraction

runs in four stages. [music] Segmentation splits the raw text into sentences and spans, and this step matters more than people expect, [music] because a missplit sentence corrupts everything downstream of it. Second, named entity recognition, [music] tagging spans with types like person, organization, date, or money. Historically, this was done with CRFs or BiLSTM-CRF [music] models. Today, it's typically a fine-tuned transformer or increasingly [music] an LLM used zero-shot.

Third, relation extraction, taking pairs of entities within some distance [music] of each other in the text and classifying the relationship between them, something like acquired or employed [music] by. This is the stage where precision tends to drop off the fastest. And fourth, entity linking, resolving a mention like Apple to one specific canonical entity in a knowledge base, disambiguating it from [music] every other thing that might share that name. Now, look at the code on the right. This is a production pattern, not a toy example. Notice [music] a few things.

It's async because NER and relation extraction are both expensive model calls you don't want blocking your event loop. >> [music] >> It only accepts a relation if the model's confidence clears a threshold, here 75%, before it's allowed to become a structured fact. And on a timeout, [music] it doesn't return a partial or best-guess result. It fails closed, returning nothing at all. That's a deliberate choice. A system consuming

this output downstream [music] has no way to know a triple is low confidence unless you tell it. So, it's safer to withhold the fact [music] entirely than to hand over something that looks equally trustworthy, but might be wrong. That last point is worth sitting >> [music] >> with because it points at the single biggest operational risk in any IE pipeline, error [music] propagation. One mistake at the NER stage doesn't just cause one error. It silently corrupts [music] every relation and every triple built on top of it.

So, now that we've seen how both are built, let's talk about how they actually break once they hit [music] real traffic and real data. On the retrieval side, the failure modes are mostly about coverage and gaming. Index staleness [music] is the most common one operationally. Your ranker will score results with complete [music] confidence, even when it's scoring against a corpus that's quietly gone out of date. [music] There's an inherent precision recall trade-off. Widen your net to catch more

edge cases and you dilute the quality of your top results. [music] Cold start ranking is a real problem, too, because learned ranking models need click data that a brand new [music] corpus simply doesn't have yet. And sparse retrieval is still vulnerable to adversarial content. SEO-style [music] term stuffing can game a purely term matching relevance score. On the extraction [music] side, the failure modes are mostly about correctness and cost. We already talked

about error propagation. [music] A pipeline is only as strong as its weakest stage. Schema brittleness is IE's most distinctive failure. [music] If your relation extractor only knows a fixed set of relationship types, it literally cannot express [music] a fact that falls outside that schema. Whereas an IR system can always return the closest matching thing, even for a weird [music] query.

Supervision is expensive. Labeling spans and relations costs far more than collecting relevance judgments for search. [music] And long-tail entity recall tends to be systematically weak because rare entity types are almost by definition underrepresented [music] in training data. Given all of that, how do you actually decide which one to reach for? Let's turn this into [music] a framework you can apply directly.

Reach for information retrieval [music] when your users want documents, not facts, when the corpus is large, heterogeneous, and low structure. When you need [music] sub-100 millisecond latency at query time, or when relevance is genuinely fuzzy >> [music] >> and a little subjective. Reach for information extraction when a downstream system needs [music] typed, queryable data. When you're building or feeding a knowledge graph, when facts need to be auditable back to a specific source span, or when your domain has a stable, [music] well-defined schema you can commit to. And use both when [music] you're building retrieval augmented generation.

Retrieve candidates first, then extract from them. When search results should show structured summaries alongside [music] ranked links, when your product needs both discovery and precise question answering, or when you're trying to scale human review by having a machine [music] pre-extract candidates for a person to verify. One important note [music] here, these aren't mutually exclusive within a single product. Apply this framework per component, [music] not per product. The most common mistake I see is teams building the both architecture from day one, when the actual need on day [music] one is pure retrieval. And that extra extraction

stage just adds latency, cost, and a new [music] failure surface with no benefit yet. Let's ground all of this in a system every one of you has personally used in the last day, Google Search. When you search Google, you're often getting both outputs at once. On the left, you get the ranked results page. That's pure [music] information retrieval. It's descended directly from

classical IR, an inverted index over the crawled web, [music] term matching signals that trace back to TF-IDF and BM25, link graph-based authority signals like PageRank, all now layered with learned ranking. The output is 10 blue links ordered by estimated relevance. [music] Critically, it's not making any claim about what any specific page actually says, [music] only that it's probably worth reading. On the right, for entity-style queries, you get the knowledge [music] panel. That's information extraction.

Structured facts, a birthdate, a founder, a headquarters, a population, are extracted from web pages, from explicit schema.org markup that site owners add [music] specifically to help this kind of extraction, and from other structured sources. Those facts get entity [music] resolved into Google's knowledge graph. The output is a typed panel that can answer something like, "When was this company founded?" directly, [music] and it's sourced back to specific pages.

These two systems have different freshness cycles, different QA [music] processes, and very different failure signatures. A wrong ranked result is a minor annoyance. >> [music] >> A wrong fact asserted in a knowledge panel is a credibility incident. That's the production pattern worth copying. Don't [music] force one model to do both jobs. Run two systems with two different

guarantees, and compose their outputs. So, where is all of this heading? The honest insight is that large language models [music] are dissolving the clean boundary we've been drawing all talk. Here's the key insight. Retrieval-augmented [music] generation is structurally an IR stage feeding an IE-flavored generation stage. Dense

retrieval [music] finds a handful of candidate passages, and then the model performs what's effectively in-context [music] extraction and synthesis in a single forward pass over that narrowed context. Three things [music] are driving this. First, dense retrieval is reshaping IR itself. Embeddings plus approximate [music] nearest neighbor structures like HNSW now sit alongside traditional sparse indexes, and [music] hybrid retrieval combining both signals is the new default rather than dense replacing sparse outright. Second, LLMs [music] can perform zero-shot extraction, named entity recognition and relation extraction with no task-specific training at all, which trades [music] what used to be months of schema engineering and labeled data collection for a prompt design and evaluation problem instead.

And third, [music] structure generation closes the loop. JSON mode and function calling interfaces constrain an LLM's output into a typed schema, [music] which turns extraction into a first-class generation target rather than a separate bolted-on model. But, here's the honest caveat, and it matters. This shifts the dominant failure mode. [music] Classical IE tends to under-recall on rare entities. It

misses facts it was never trained [music] to recognize. LLM-based extraction, instead, tends to confidently hallucinate. The fix isn't some vague notion of trust the model less. [music] It's the same structural discipline classical IE always needed. Schema validation, confidence thresholds, and grounding [music] every claim back to a source span.

>> Let's bring this all together into five heuristics [music] you can actually carry into your next design review. One. IR answers which documents, [music] and IE answers what facts. Never conflate the two when you're scoping a system. That confusion is the root cause of most our search [music] just feels bad complaints.

Two. Errors compound multiplicatively [music] through an IE pipeline. A mistake at the NER stage corrupts every relation built on top of it, [music] which means QA needs to happen per stage, not just end-to-end. Three. [music] Sparse and dense retrieval are complementary, not competing. Hybrid

search, running both together, is now the production default, [music] not an either-or decision. Four. LLM extraction [music] trades schema engineering cost for hallucination risk. That's a trade, not a strict upgrade. >> [music] >> Validate structure and ground every claim back to its source span.

And five. Modern rag systems and knowledge graph pipelines [music] are compositions of IR and IE, not replacements for either one. Design and evaluate each stage [music] on its own metric, not as one opaque black box. >> If you want to go deeper than we had time for today, here's where I'd point you. [music] For the foundations of information retrieval, Modern Information Retrieval by Baeza-Yates and Ribeiro-Neto is still the standard [music] graduate reference.

Indexing structures, ranking models, and evaluation methodology, all in one [music] place. For the origins of information extraction, go straight to the [music] primary source, the Message Understanding Conference Proceedings, MUC-1 [music] through MUC-7. Reading even one of their original scoring guidelines [music] will tell you more about why slot filling metrics look the way they do than any secondary summary will. Jurafsky and Martin's Speech [music] and Language Processing is the best single text for connecting both of these fields to modern neural methods. And on the practical side, spaCy, [music] Elasticsearch or OpenSearch, and Hugging Face Transformers are the open-source tools you'd actually reach for to prototype everything we covered today.

So, to recap [music] in one breath, we traced information retrieval and information extraction from their 1960s and MUC-era origins through how each is actually [music] architected, where each breaks in production, a live example in Google Search, and where the [music] LLM era is taking both of them next. I'd love to open it up for questions on any stage of either pipeline >> [music] >> or on how you'd apply that decision framework from earlier to whatever you're building right now. Thank you.