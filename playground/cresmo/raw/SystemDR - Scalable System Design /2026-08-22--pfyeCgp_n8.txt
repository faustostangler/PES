---
video_title: "CI/CD for Data Pipelines: Airflow vs Prefect vs Dagster | Data Engineering & Workflow Orchestration"
video_id: -pfyeCgp_n8
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "Science & Technology"
url: https://www.youtube.com/watch?v=-pfyeCgp_n8
video_date: 2026-08-22
video_description: |
  Website Link: systemdrd.com
  Automate CI/CD for data pipelines using modern workflow orchestration platforms like Apache Airflow, Prefect, and Dagster. In this tutorial, you'll learn how to build reliable data engineering workflows with Data Pipeline Automation, CI/CD, Workflow Orchestration, Apache Airflow, Prefect, Dagster, ETL Pipelines, ELT, Data Engineering, MLOps, DataOps, Python, Docker, GitHub Actions, Pipeline Testing, Deployment Automation, Scheduling, Monitoring, and Production Data Pipelines. We'll explore how orchestration platforms improve pipeline reliability, testing, deployment, observability, and scalability while comparing Airflow vs Prefect vs Dagster. Whether you're a data engineer, ML engineer, DevOps professional, or Python developer, this video will help you build automated, scalable, and production-ready data pipelines.
  #dataengineering  #apacheairflow  #datapipelines  #cicd #mlops
---
Welcome. Today we're talking about automating [music] CI/CD for data pipelines. Specifically, how to bring real [music] software delivery discipline to orchestration platforms like Apache Airflow, Prefect, and Dagster. This isn't [music] a tutorial on any one of these tools. It's a look at the layer that sits on top of all three. [music] How do you test,

build, and deploy pipeline code safely, the same way you'd ship any other piece [music] of production software? Let's start with why this problem is harder than it looks. Here's the core [music] asymmetry that makes this topic worth your next 20 minutes. A broken web deploy fails [music] loudly. You get a 500 error, a failed health check, an automatic roll back, and [music] an on call engineer paged within seconds. A

broken data pipeline [music] usually does none of that. It keeps running. The task shows green. The DAG shows success. [music] And underneath it's quietly writing wrong numbers into a table that three dashboards and a finance report depend on. Nobody notices until the

[music] numbers stop making sense. And by then, the bad data has been propagating for days. That's the [music] picture on the right of this slide. A software bug fails fast. Exception thrown, [music] request errors, someone paged in seconds. A data pipeline [music] bug

fails silently. The task reports success, but the values are wrong and they cascade downstream for days before anyone [music] catches it. This is a scenario almost every data team eventually lives through. An analyst notices a KPI dashboard looks off on a Monday morning and [music] 2 days of investigation later, the root cause turns out to be a join key that was silently changed [music] by an unreed pipeline edit the previous Thursday. The fix takes 10 minutes. Finding [music] it

takes 2 days. CI/CD is how we collapse that gap, catching the problem before it ever touches production data, not after. So what does shipping even mean [music] when the artifact isn't a web service, but a pipeline? This is the single mental model I want you to leave with today. [music] Traditional software CI/CD ships one thing, code. [music] A

change gets built, unit tested, packaged into a deployable artifact, and it [music] starts serving traffic. Success is binary and immediate. The service is either up and healthy [music] or it isn't. Data pipeline CI/CD ships three things at once. Code, yes, but also the shape of the DAG or asset [music] graph and the schema of the data it reads and writes. All three have to be validated

[music] together before anything reaches auler. Testing has to happen against data [music] that actually looks like production, not just synthetic fixtures. Because most pipeline bugs [music] are data shaped bugs. Nulls, type drift, unexpected cardality that a plain unit test won't catch. And deployment doesn't target a [music] fleet of servers. It

targets an orchestrator. Rollout has to be backfill aware. Because if a broken version ran for 6 hours before anyone rolled [music] it back, the fix isn't just deploy the old code. It's deploy the old code and reprocess [music] the window that ran wrong. Here's the line I want you to remember. A pipeline deploy

that succeeds can still be wrong. [music] Correctness depends on the shape of the data, not just the shape of the code. Keep that in mind [music] because everything else in this talk is really just tooling built to make that distinction visible before it costs you anything. Let's make [music] this concrete with an actual reference architecture. One that works whether you're running Airflow Prefect [music] or Dagster underneath. It starts like

any CI/CD pipeline with a Git repository holding your DAGs, [music] assets or flows. A change lands there and a CI runner, GitHub actions, GitLab CI, whatever your team already uses, takes over. It lints the code, runs unit tests against [music] individual task or asset logic, and critically it performs a graph import [music] check. It actually imports the module and confirms the DAG or asset graph parses without raising an exception. That one check catches the single most common class of production [music] pipeline failure. A broken import that

silently disables an entire files worth of tasks, [music] sometimes without any visible error in the scheduler UI at all. A passing build produces a [music] versioned immutable artifact, typically a Docker image, and pushes it to a registry. Then CD [music] takes over. For Airflow, that might mean updating a GitCync target or rolling out a new Helm release. [music] For Prefect, it's running a deploy command against a workpool. For Dagster, it's updating a

code location, often through [music] an ephemeral branch deployment first. We'll come back to that shortly. From there, the orchestrator schedules and executes [music] tasks against your data warehouse. And underneath all of it runs a continuous loop. Observability and alerting. Freshness checks, row count

checks, [music] schema conformance checks, all wired to trigger an automatic roll back the moment something regresses rather than waiting for a human to notice a stale dashboard 3 days [music] later. I want to call out that import check step specifically because it's cheap, often under a second per D, and it catches a [music] disproportionate share of real incidents. A single missing import or a typo variable reference can take down [music] an entire file of tasks and most orchestrators won't raise a loud alarm about it. [music] They'll just quietly stop scheduling that deck. Let's zoom into that CI stage and look [music] at an actual test. This example is Dagster

specific, but the pattern generalizes to any of these platforms. This function creates a fully [music] ephemeral Dagster instance. no shared database, no shared run history, which means it can run safely [music] in parallel across many concurrent CI jobs without any of them interfering with each other. Then it attempts to materialize a deliberately small [music] representative slice of the asset graph, raw orders and daily revenue rather than the entire production [music] graph that keeps the test fast, seconds rather than the minutes a full backfill would take while still exercising the real dependency chain end [music] to end. Notice there are two failure modes handled explicitly and differently.

[music] A Dagster invariant violation error means the graph itself is malformed. A broken dependency, a duplicate asset key, and it's caught before [music] any computation even runs. A result.uess check afterward catches a runtime failure inside one [music] of the actual computations. Both paths print a properly formatted error annotation and return [music] false. And

that boolean return is what turns into a nonzero process exit code. the actual mechanism that fails [music] the CI job and blocks the merge. I want to flag a common mistake here. Teams often write [music] CI checks that only confirm the graph imports without reason. Necessary, but not [music] sufficient. This test

goes one step further and actually runs a small materialization, [music] which catches an entire class of bugs that only show up at execution time, a resource that isn't configured correctly for the test environment, [music] or dependency that's declared but never actually produces what the next asset expects. Now, let's compare how this actually plays out across the three major platforms [music] because the CI and deployment story looks meaningfully different depending on which one you're running. Airflow uses an imperative taskbased [music] model. You explicitly wire operators together into a DAG. [music] Its CI story typically centers on exactly the kind of test we just looked at. Import every DAG module. Scan

for [music] import errors. Deployment usually means either a git sync sidecar pulling files from a repo on a schedule or baking DAGs into a versioned Docker image deployed via Helm. Airflow tends to be the right fit for organizations with real [music] platform engineering investment and thousands of tasks that need to run reliably at scale. Prefect takes a more pythonic dynamic approach. Flows and tasks [music] are just decorated functions which makes local testing easy since you can often just call the flow directly inside [music] a test. Deployment is a first class

command. Prefect deploy registering against a workpool with flexible storage for pulling code from S3 or GitHub. Prefect tends to win with smaller platform teams that want to move fast and have highly dynamic parameterized [music] workflows. Dagster is the most declarative of the three. You define softwaredefined assets with explicit inputs and outputs [music] and the framework builds a full lineage graph automatically from that. CI leans

on local development runs plus asset materialization tests like the one we just walked through and deployment [music] centers on code locations often through ephemeral branch deployments. Daxter tends to win when data quality and lineage visibility are first order requirements. [music] For example, teams under real compliance or data contract obligations. Whichever [music] tool you're running, the actual deployment moment is the highest risk step in this [music] whole process. So, let's talk about how to make it safe. The flow has

five [music] stages. A pull request opens with a proposed change. Rather than deploying [music] straight into a shared staging environment, the safest pattern spins up an ephemeral preview deployment scoped just to that branch. Isolated infrastructure that can [music] safely read from production-like data without any risk of writing to real production tables. A human reviewer plus automated [music] checks like the smoke test we saw earlier gate the merge.

After merging, [music] rather than immediately resuming every scheduled run at full scale, you run a canary. Let exactly one [music] production scheduled run execute on the new version and inspect its output before resuming the full schedule. Only after that canary [music] passes does the change get full promotion. And if anything goes wrong at any point, the roll [music] back path has to be explicit. get revert redeploy the last known good version and this part is specific to data pipelines [music] reprocess the time window that ran under the broken version simply redeploying [music] old code doesn't fix data that's already been written incorrectly I want to call out the failure mode in the box at the [music] bottom because it's the hardest one to catch schema drift a column rename or a type change frequently doesn't break anything syntactically it just silently corrupts data until the next backfill [music] runs weeks later the fix is treating data contracts as an explicit required CI gate, not something a reviewer is [music] expected to catch by eye in a diff. This pattern isn't

theoretical. Let's ground it [music] in something real. Dagster Plus, the managed offering from Dagster Labs, productizes [music] exactly this preview deployment workflow as a native feature. Every push to a branch with an open pull request [music] automatically spins up a fresh, fully isolated deployment of that branch's code, separate from production. Because it's isolated infrastructure [music] rather than a shared staging slot, multiple engineers can each have their own preview running at [music] the same time without stepping on each other. A real pain point with

traditional shared staging [music] environments. Teams can point these previews at production scale data, [music] watch real materializations happen in the UI, and catch problems interactively before merge. [music] And when the pull request closes, the preview tears itself down automatically. No lingering infrastructure cost. This discipline isn't unique to assetbased tools either. Airflow native [music]

teams get a lighterw weight version of the same safety net through DAG integrity testing baked into CI, importing every module, asserting [music] zero import errors, a practice that's been part of Apache Airflow's own contributor guidance for years. The deeper lesson generalizes [music] across all three platforms. The safest deployment is one where you've already watched the change run successfully [music] against something that looks like production before it ever actually touches production. Let's talk about where this space is heading because I [music] think there's a real architectural shift underway. Not just a checklist of best practices. An Airflow

task in the classic model is [music] fundamentally ephemeral. It's a step that ran and once it's done the orchestrator doesn't retain a durable notion of say this [music] table as a first class object with its own identity and history. Daxter's [music] softwaredefined asset model flips that. The asset, a table, a file, a model, is the first class object [music] and the code that produces it is secondary. And that identity is exactly the hook that lets tooling do things an imperative [music] task graph structurally can't.

Diff the lineage graph between two versions of code and say precisely which [music] downstream tables are affected by a change. Attach a schema directly to the asset definition and validate it in CI. generate a genuinely useful preview [music] environment because the tool knows which specific assets changed, not just which files changed. [music] You can see this playing out in three concrete trends. Data contracts becoming CI gates instead of Slack alerts after the fact. Lineage [music] aware diffing

replacing manual code review guesswork. And quality checks that block a merge instead [music] of triggering a 2 a.m. page after the bad data has already shipped. To be clear, [music] this doesn't mean imperative DAG frameworks are going away. Airflow remains

extremely well suited to complex operational scheduling, but even Airflow native shops are increasingly layering assetaware features on top of task decks [music] specifically to borrow this identity model. Let's pull this all together into [music] five heristics you can actually take back to your team. One, test the graph import, not just the task [music] logic. A broken DAG or asset graph import is the single most common cause of production pipeline [music] outages. and it's the cheapest thing you can catch. Seconds in CI every

single time. Two, [music] treat schema as a CI gate. Validate data contracts before code ever reaches theuler because a successful looking [music] deploy can still be silently writing wrong data. Three, prefer ephemeral per branch previews over a single shared staging environment. [music] branch deployments or an equivalent pattern. Beat a staging pipeline that

[music] everyone fights over and that drifts out of sync the moment more than one person needs it. Four, deploy with backfill awareness for a pipeline. Roll back means revert plus reprocess, not just revert. Plan that path before you [music] need it, not during an incident. And five, match the orchestrator to your team's actual topology. Airflow [music]

for ops mature scale. Prefect for dynamic workflows. Dagster for lineage first contract-driven organizations. If you only do one of these starting [music] Monday, make it the first one. It's an hour of work and it prevents the single most common cause of outage. For

anyone who [music] wants to go deeper after this, here's where I'd start. Each orchestrator's own documentation is genuinely the best source for deployment specifics since these APIs [music] move fast. Airflow's testing guide, Prefix deployment docs, and Dagster's [music] CI/CD and branch deployment docs. On the tooling side, Piestbased [music] DAG validators, the Dagster CLI for scripting branch deployments, and ready-made [music] GitHub actions templates for each platform. And if you want to go broader, look at the Dora state of DevOps research for deployment frequency benchmarking [music] and at data contract patterns from ecosystems like DBT and Pantic, which pair naturally with the schema [music] as CI gate principle we just covered.

Start with whichever orchestrator matches your current stack, [music] not all three at once. So to close, CI/CD for data pipelines [music] isn't a lesser version of software CI/CD. It's the same discipline applied to a harder correctness problem where the artifact [music] is code, schema, and data shape all at once and where failures are silent instead of loud. Ship data pipelines like software safely, [music] repeatably, and observably. I'd love to take questions on [music] any of the three platforms, the architecture, or any of the specific patterns we covered today. Thank you. [music]