---
video_title: "Design Twitter's Timeline: The Senior+ System Design Walkthrough #systemdesign #twitter #interview"
video_id: px_Y5Zu38U0
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=px_Y5Zu38U0
video_date: 2026-08-27
video_description: |
  📖 Full Walkthrough: https://systemdr.systemdrd.com/p/design-twitters-timeline-the-senior
  🌐 Visit our website: https://systemdrd.com/
   
  Twelve different FAANG system design questions — Instagram's feed, TikTok's For You, Reddit's front page, LinkedIn's home page — all share the same backbone as "design Twitter's timeline." In this walkthrough we build the whole thing from scratch: clarifying questions, back-of-envelope estimates, the data model, and the single trade-off that decides the round — fanout-on-write vs. fanout-on-read — plus how production systems actually rank a feed with a two-stage ML pipeline.
    
  If this helped you think through the celebrity-fanout problem or the ranking pipeline differently, drop a comment with the question you want walked through next, and subscribe for more senior-level system design breakdowns.
   
  #systemdesign #softwareengineering #faangprep #twitterarchitecture #techinterview #distributedsystems #backendengineering #softwarearchitecture #codinginterview #machinelearning
---
Twitter's home timeline is the single most drilled [music] system design question in the FAA NG interview bank because 12 other questions from Instagram's feed to TikTok's for you page share [music] its exact backbone. Today we're walking the senior plus version end-to-end so that if you internalize this one, you can walk into 11 other interviews you haven't specifically [music] drilled. The version most interviewers actually ask is not the 4-hour design Twitter monster. >> [music] >> It's narrower. Design the home timeline, design a newsfeed, design the part of a product that shows posts from people you follow.

All of these collapse to the same probe, so don't over scope in the first 5 [music] minutes. Notice the phrasing carefully. Posts can be original tweets, retweets, or replies, and by default replies live on the conversation [music] thread, not the home timeline. That's a detail most candidates get wrong under pressure. >> [music] >> The reason this generalizes so well is structural. Instagram's feed, Facebook's

[music] newsfeed, LinkedIn's homepage, Reddit's front page. All four are direct equivalents. Many users, many posts, a feed that [music] has to be fresh and ranked, and a write-to-read ratio that decides the whole architecture. TikTok's for you feed is close but has one real difference. It's not a follow [music] graph feed. It's a recommendation feed,

so candidate generation changes even though the serving architecture rhymes. Naming that distinction out loud is itself a [music] signal that you're not just pattern matching. Before you draw a single box, six clarifying [music] questions separate a mid-level answer from a senior one, but three of them do most of the work. First, ranked or chronological? Old Twitter was reverse chronological, trivial, [music] just sort by timestamp. Modern feeds are

ranked, which drops in a scoring system, a feature pipeline, and almost certainly a model. If the interviewer doesn't specify, default to ranked [music] because it's the harder and more realistic version, and you can always simplify down from there. Second, what's the DAU, the average follow [music] count, and this is the one people skip, the maximum follow and follower count. That maximum is the decisive number. It's the celebrity [music] problem hiding in plain sight, and it's what breaks the naive fan out design later.

Third, the read to write ratio. It's always lopsided in a social feed, but a 100 to 1 system looks [music] architecturally different from a 1,000 to 1 system because a heavier skew justifies doing more expensive work on the right path. The other three, freshness target, [music] what else rides in the feed besides original posts, and the load SLO matter, too, but they're refinements. State a number for each. Something like a 5-second [music] P99 freshness target and a P50 under 200 milliseconds for the API. [music]

Interviewers remember candidates who commit to numbers instead of hand-waving. Let's put working numbers on this because everything downstream, cache [music] sizing, sharding strategy, even the fan out threshold, falls out of these estimates. Assume 500 million daily active users, [music] each opening the app about five times a day. That's 2 and 1/2 billion feed reads daily, roughly 30,000 reads per second on [music] average, spiking to around 200,000 at peak. On the right side, each user [music] posts about a fifth of a time per day, 100 million posts daily, 1,200 writes per second average, [music] 10,000 at peak. That read to write

ratio, on the order of 25 to 1 at the request level, [music] is exactly the kind of number you'd state out loud in step one. The follow graph itself [music] is enormous but cheap. Hundreds of billions of edges at roughly 30 bytes each, so 3 to 5 terabytes total. The posts table, [music] 5 years of retention at a kilobyte per post, lands around 180 terabytes. But the layer that actually distinguishes a senior answer is the timeline [music] cache. 500

million users times 1,000 cached posts times 200 bytes per entry is about 100 terabytes, bigger than your hot transactional store, and it has to live somewhere built [music] for it, typically Redis or Memcached. Naming this as its own system, separate from the posts database, is exactly the kind of detail that tells an interviewer you've [music] actually built something like this. Four stores, and the mistake candidates make is putting all four in in database. In production, [music] they're four different systems, each shaped by its access pattern. The posts table is the source of truth, sharded by author_id, and it uses snowflake IDs, Twitter's own invention, [music] for distributed, roughly time-sortable ID generation without a central counter.

The follow graph is deliberately not in your OLTP store. [music] At scale, it lives in something graph-aware, or at minimum, a sharded relational table with a composite key on follower [music] and followee, with a secondary index the other direction. And here's the asymmetry that matters. Who do I follow is fast and small, because most users follow under 500 accounts. Who follows me? Can return over 100 million rows for

a celebrity. [music] That single query pattern is what breaks naive designs later. The user_timeline_cache is the hot read path, a Redis sorted set keyed by user ID, holding roughly [music] the thousand most recent post IDs, updated by ZADD on write and read with a simple ZREVRANGE. [music] No database hit, no graph traversal. And posts_by_author_cache exists [music] purely to support fanout on read for the handful of accounts too large to push to. We'll get to exactly

why in a moment. This diagram [music] is the whole system in one picture, so let's walk it left to right, right path first. An author submits a post through the [music] API routing fabric, which authenticates and throttles. That right lands in the sharded post store, keyed by snowflake ID, and immediately publishes a post [music] created event onto a Kafka pipeline. That queue is what decouples the [music] write spike from everything downstream.

From there, the social graph directory looks up the author's followers and evaluates [music] celebrity status against the decision block. Is follower count at or above roughly 1 million? If no, push the post into the standard timelines [music] precompute cluster for every follower. That's the Redis ZSET fanout [music] on right path. If yes, stop the fanout entirely and isolate the account's posts into a dedicated hot creator timeline buffer instead, [music] so one viral celebrity account never creates a right storm that starves everyone else's rights. Now, the read

path, on the right, a timeline consumer requests their feed, [music] and the aggregation engine's first stage assembles candidates, merging the reader's own precomputed timeline with any hot [music] creators they follow injected at read time. Stage two runs the two-stage ranker we'll cover next. And notice the critical fallback matrix [music] sitting right there in the diagram. If the ML ranker times out past 50 milliseconds, the system safely drops to reverse chronological rather than [music] failing the request. Underneath both paths, a global post federation cache [music] and a real-time feature store keep the hot path off the database entirely. This is where the interview

round actually gets decided, [music] so let's be precise about the tradeoff rather than just naming both sides. Fan out on right, [music] the push model, does the work at post time. The moment someone posts, you immediately write that post ID into every follower's [music] timeline cache. The read path becomes trivially fast and predictably fast because the timeline is already sitting there computed. The [music] cost is right amplification. A user with a

thousand followers triggers a thousand cache writes for one post. A celebrity with a hundred million followers triggers a hundred million writes. A single tweet becomes a right storm that can crowd [music] out every other right in the system. And you're doing that work even for followers who haven't opened the app in six months. >> [music] >> Fan out on read, the pull model, flips it. Posting is nearly free, an O of one

write to your own table, >> [music] >> and nothing happens for inactive followers. But the read path pays for it. A user following 500 accounts means up to 500 lookups [music] or one expensive fan-in query, and your latency now depends on how many people that specific user follows, [music] which is unpredictable. Production systems don't pick one. They do both.

Most users get fan out on right by default. Accounts above an empirically tuned [music] follower threshold, call it 1 million as a starting hypothesis, get fan out on read instead. When [music] a reader follows both regular accounts and celebrities, you assemble at read time. Pull the precomputed timeline. [music] Pull each followed celebrity's recent posts. Merge by timestamp, then rank.

And if an interviewer pushes on where that threshold number comes from, the honest answer [music] is that it's an operational tuning decision. You'd instrument right amplification per account and adjust. And a dynamic version that only fans out when both the writer and the reader have been recently active [music] is the answer that reads a staff plus. Once candidates are assembled, roughly 200 posts between the precomputed [music] timeline and any celebrity pulls, the job is to pick the best 20 and order them. That's ranking, and modern feeds [music] don't skip it. The

score itself is a weighted sum. Recency, how much you historically engage with this specific author, >> [music] >> a predicted engagement term for whether you specifically will like or reply to this exact post, topical interest, social proof from people similar to you, >> [music] >> and a negative dampening term for content you've already seen or that's been flagged low quality. Those weights come from a model trained on engagement [music] data. As the system design candidate, you don't need to own that model. You need to know it exists and how it's served. And how it's served is

the two-stage part. A lightweight scorer runs on every single candidate in well under a millisecond. Simple, hand-tuned Then a heavier real ML model rescores only the top candidates [music] that survived stage one, taking around 10 milliseconds. Both stages pull precomputed features from a feature store, Feast or an in-house [music] equivalent, with sub-10 millisecond P99 lookups, covering things like an author's recent viral velocity or a user's historical topic affinities. [music] Most candidates skip mentioning a feature store entirely. Naming it is a

real signal. And critically, what happens [music] when the perfect path fails? If the heavy ranker doesn't return within 50 milliseconds, you fall back to the lightweight score. If [music] that somehow also fails, you fall back further to pure reverse chronological. The feed degrades gracefully.

>> [music] >> It never goes down. And saying that explicitly is exactly the kind of answer interviewers are listening for. So, [music] what actually separates the levels? At L5 senior, the bar is delivering the framework cleanly. Both fan out approaches named with the hybrid, ranking [music] mentioned with real features attached to it rather than hand-waved, snowflake IDs and sharding called out, and [music] a detail most candidates miss, the author's own post has to show up in their own timeline immediately, not [music] after fanout latency, which means the post service writes to the author's own timeline cache synchronously before the async fanout even starts. [music]

At L6, staff, the operational story gets real. You're expected to engage with the threshold tuning [music] question directly, including the dynamic variant. Ranking becomes explicitly two-stage with a feature store attached. Multi-region shows up >> [music] >> with an honest admission that a user in the EU following someone in the US will see their posts after real replication lag, seconds, not instant, and you say that out loud instead of pretending otherwise. Inactive users get pruned from the timeline cache [music] after 30 days to save that 100 TB footprint, and rebuild via a one-time fanout on read when they return. And deletions and

edits are their own fanout problem in the opposite direction, with a deleted post ID set acting as a backstop [music] filter at read time. At L7 and beyond, the same shape extends outward. >> [music] >> Non-follow candidate generation for recommendation-style feeds, the org boundaries between who owns posts, timeline, and the ranker, multi-year [music] capacity planning, and treating the model retraining pipeline itself as a system worth designing. You don't need all of that for a senior loop, but knowing where the ceiling [music] is tells the interviewer you know exactly where you're choosing to stop. That's the full walk-through. The framework,

the estimates, the fanout tradeoff, [music] and the ranking pipeline that turns a raw follow graph feed into a ranked one. For the complete write-up, with the follow-up probes, and the full [music] list of common mistakes that failed around, head to systemdrd.com, and you can dive deeper on the Substack link in the description. [music] See you in the next one.