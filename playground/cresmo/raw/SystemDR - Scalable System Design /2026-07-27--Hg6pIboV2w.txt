---
video_title: "Mastering Time Series Cross-Validation:Best Practices for Data Splitting in Machine Learning Python"
video_id: -Hg6pIboV2w
channel_name: "SystemDR - Scalable System Design "
channel_id: UCi1BkzqiwN5wChnvt20uBWQ
channel_category: "uncategorized"
url: https://www.youtube.com/watch?v=-Hg6pIboV2w
video_date: 20260727
video_description: |
  Website Link: systemdrd.com
  Master Time Series Cross-Validation and learn the best practices for splitting chronological data to build accurate, production-ready machine learning models. In this tutorial, you'll discover why traditional random train-test splits fail for time-series forecasting and how to correctly use TimeSeriesSplit, rolling window validation, expanding window validation, walk-forward validation, backtesting, feature engineering, forecasting, model evaluation, hyperparameter tuning, and cross-validation techniques in Python and Scikit-learn. We also cover time series forecasting, machine learning, data leakage prevention, model validation, ARIMA, XGBoost, forecasting pipelines, predictive analytics, and AI model evaluation using practical coding examples. Whether you're a data scientist, machine learning engineer, quantitative analyst, AI developer, or Python programmer, this video will help you evaluate forecasting models correctly, eliminate data leakage, and improve prediction accuracy on real-world time-series datasets.
  #machinelearning  #timeseries  #python  #scikitlearn  #datascience
---
Welcome. Today, we're talking about [music] mastering cross validation. Specifically, the best practices for splitting data in time series analysis. >> [music] >> Here's the promise of the next 15 to 20 minutes. By the end of this session, [music] you'll know how to partition historical data into train and test sets without [music] silently leaking the future into the past. That sounds like a

solved [music] problem. Scikit-learn ships a splitter. Everyone's used k-fold before. What's the issue? The issue is that most production forecasting and trading [music] models that worked great in back testing and then failed in production, failed right [music] here at the validation step, not in the model architecture.

This isn't one trick. >> [music] >> It's a family of splitting strategies. Expanding window, sliding window, purged k-fold with embargo, >> [music] >> and choosing the right one depends on your data's stationarity and auto correlation structure. Let's start with [music] exactly how standard cross validation breaks on time ordered data. Classic k-fold cross validation makes [music] one core assumption, that your rows are exchangeable. That shuffling them

changes nothing about what the model can learn. Time series data [music] violates that assumption directly. Each observation is correlated with its neighbors, and a shuffled fold routinely ends up [music] training on data that was collected after the point it's being tested on. Here's the key [music] insight I want you to walk away with from this slide. A shuffled k-fold split can silently inflate your reported validation accuracy compared to a true >> [music] >> out of time test. Why? Because folds are

trained on rows that occur chronologically >> [music] >> after the ones they're scored on. The model isn't forecasting anything. It's interpolating. [music] Look at the comparison on screen. In the shuffled fold, row order is scrambled.

[music] 1 4 2 7 3 9 5 8 6 10. And the test points, 7 and 8, >> [music] >> are surrounded by training points that come both before and after them in time. That's leakage. >> [music] >> In the chronological fold, rows 1 through 7 train, and rows 8 through 10 test. Order is preserved end to end. No

peeking [music] into the future. That's the standard we're going to hold every strategy in the stack to. If your validation accuracy [music] looks unusually high for how hard the forecasting task actually is, the [music] first thing to check is whether your split respects time order. So, what [music] does a mental model for doing this correctly actually look like? Let's build it.

Every valid time series split [music] obeys one rule, and I want you to hold on to this single sentence for the rest of the talk. At the moment [music] a prediction is made, the model may only have seen data from before that moment. [music] Walk-forward validation, also called rolling origin evaluation, puts that rule into practice. You move a training window forward [music] through history, and at each step you test on the slice immediately ahead of it. >> [music] >> Look at the diagram. Fold one trains on

the first three units of data. Then there's a short gap. We call [music] this the embargo. And then it tests on the next unit. Fold two expands the training window to five units. Same gap, tests on the next

unit. Fold three expands [music] again to seven units. Why the gap? Because most real [music] pipelines compute lagged or rolling features. A 7-day average, an exponentially weighted moving average.

Without a gap between train and test, the test window's [music] earliest rows can indirectly encode information leaking backward from the end of the training window, >> [music] >> just through shared feature computation. The embargo blocks that. This one [music] structure, train embargo test, generalizes into everything we're about to cover. [music] Expanding window, which is what's shown here. Sliding window, where the training [music] block is fixed size and slides forward instead of growing, and a purged K-fold, >> [music] >> where the test block can sit anywhere in the sample with embargo on both sides.

All three are variations on the same goal. This is also, by the way, exactly how forecasting competitions [music] and shipped libraries like profits simulate deployment. It's the closest offline [music] proxy we have to what would have happened if we shipped this model at each historical point in time. Now, let's compare the [music] three concrete strategies that actually treat it. This is the slide to come back to when someone on your team asks, [music] "Which splitter do I actually use?"

Three real strategies, stacked so you can compare them directly. >> [music] >> First, expanding window. Training data accumulates fold over fold and never shrinks. This is a good default when you believe older data is still relevant, [music] and you want the maximum possible training signal at every step. Second, sliding [music] or rolling window. The training window is a fixed

size, and it slides forward, discarding the oldest data each fold. >> [music] >> This is the right choice when the underlying process is non-stationary. Think a demand [music] forecasting model, where behavior from 3 years ago may actively hurt a model trained on current patterns. Third, purged K-fold with [music] embargo. This one's a bit different structurally. It was developed for

finance by Marcos Lopez de Prado. Instead of the test block always sitting at the end, it can sit [music] anywhere in the sample. Training uses every remaining row, but rows whose label windows overlap the test window [music] get purged, removed. And there's an embargo warning on both sides of the test block, not just one. >> [music] >> All three strategies share that embargo warning. And here's a rule of thumb you

can take straight into a code review. Your embargo should be at least as large as your longest [music] feature lookback plus your label horizon. In practice, expanding window is the safe default [music] for slow-changing processes. Like macroeconomics or accounting. Sliding window [music] is standard for fast-moving domains. Ad click-through

rate models, for example. And purged [music] embargoed is close to mandatory in quantitative finance. Rolling windows are built from forward-looking return [music] models. And let's see the purged embargoed version as actual production models. This [music] is the minimum production correct implementation, not a toy example. The whole leakage [music] fix

lives in exactly one line, and I'll point to it. This is a Python data [music] class called purged window split. It takes three parameters: the number of [music] splits, the test size, and critically an explicit embargo. Making that embargo a first-class code review parameter, rather than an implicit [music] assumption buried somewhere in a notebook, is the whole point. The split [music] method anchors the last test window at the end of the sample and walks backward. So, folds are

evenly spaced walk-forward windows [music] across the most recent data. Now, look at line 23, highlighted on screen. Train end equals [music] max of zero and test start minus embargo. That's it. That's [music] the entire leakage fix. It shrinks the training

boundary by the embargo amount before slicing, which guarantees that no training row falls within embargo [music] samples of the test window. One more thing worth calling out. Scikit-learn's built-in time series [music] split doesn't support an embargo parameter out of the box. Teams doing rigorous time series validation, [music] especially in finance, almost always end up writing a small custom splitter just like this one, rather than relying on a stock library class. Given these strategies, [music] how do you actually choose between them in production? Let's talk tradeoffs.

Think of this as a practical [music] decision tree. Expanding window is the right default when you have good reason to believe the process is roughly [music] stationary. The statistical relationships that held in month one still hold [music] in month 24. It's data efficient, which matters most when you're working in a low volume [music] domain. The trade-off? Training cost

grows every fold. And [music] there's a real risk that stale patterns from early history end up diluting your most recent, most relevant signal. Sliding [music] window trades data volume for recency. It explicitly discards old data on [music] the theory that it no longer represents current behavior. This matters enormously anywhere you expect regime change, pandemic era retail [music] demand, interest rate driven credit models, any market with structural breaks. [music]

Its trade-off is the mirror image. Too short a window and you starve the model of enough signal to learn from. Here's [music] a pattern worth stealing. These two approaches aren't mutually exclusive. Some production systems [music] run both and use the gap between the fold scores as a built-in stationarity [music] diagnostic. If

sliding window performance is meaningfully better than expanding window, that's evidence the process has drifted. A common workflow: Use sliding window during model development for fast iteration and a fixed compute budget. [music] Then validate your finalist with expanding window before shipping. Just to make [music] sure the model isn't overfit to a narrow, recent slice of data. Strategy [music] choice solves one class of problems, but there's a second class of failure modes that shows [music] up no matter which splitter you pick.

These four failure modes survive even after you've picked the right splitter because [music] they live in the feature pipeline, not in the splitter itself. Feature leakage [music] is the most common and honestly the hardest to catch. A rolling 30-day average computed once over the full data set, >> [music] >> then split afterward, still encodes information from inside the test [music] window into training rows near the boundary. The fix? Compute rolling [music] features per fold, using only the data available at that fold's cut-off, not the whole data [music] set up front.

Overlapping show up specifically with forward-looking targets, things like [music] the return over the next 5 days. Two adjacent rows can share label information [music] even when their feature time steps differ. This is exactly the failure mode [music] that purging is designed to remove. Non-stationary is really a data problem, not a validation code problem. But good CV [music] design surfaces it.

If your fold scores consistently degrade over time, [music] that's a signal to go investigate a regime change, not to keep tuning hyper parameters. And finally, an embargo that's too short [music] gives you a false sense of security. It looks like you did the right thing, but if it's shorter than your longest feature lookback, leakage is still happening, [music] just less of it. Lopez de Prado's research in finance found that most published profitable trading [music] strategies fail out of sample for exactly this reason. The validation code passed [music] for you, but the feature pipeline upstream of it Let's ground [music] all of this in two concrete named production systems.

Two examples, one open source [music] forecasting library almost everyone here has used, and one institutional practice from finance [music] showing the exact same principle at very different scales. First, Meta's Profit. [music] It's built-in cross-validation function is a direct shipped implementation [music] of walk-forward validation. You specify an initial training period, a horizon, and a spacing between cutoffs, and it [music] runs the full rolling origin loop automatically. A companion function then reports error metrics per forecast horizon, not one blended number. That

matters because [music] a model can be excellent at one-day-ahead forecasts and much worse at 30-day-ahead, and a single average metric would [music] hide that completely. Second, purged K-fold in quantitative finance. Lopez de [music] Prado's method from his book Advances in Financial Machine Learning is standard practice at quantitative [music] funds for validating models whose labels are built from forward-looking return windows. That's the overlapping label failure mode from the previous slide, just operating at institutional scale with real [music] capital on the line. The takeaway from both, this isn't an academic nuance. It's load-bearing

infrastructure inside production systems [music] that real people depend on for real decisions. So, where is [music] this technique heading next? Let's look at the frontier. This is the idea that [music] separates people who have read about time series cross-validation from people who've had to defend [music] a backtest in front of a skeptical risk committee. Combinatorial purged cross-validation, CPCV, [music] extends purged K-folds by generating many different train and test path combinations from the same purged embargoed [music] blocks.

Instead of running one backtest, you get a whole distribution [music] of out-of-sample performance estimates. That distribution is what makes it possible [music] to estimate something called the probability of backtest overfitting, the likelihood that [music] a strategy's apparent edge is really just an artifact of trying many configurations against a single historical path rather than a real [music] generalizable signal. Here's why this matters. A single backtest is one draw from a distribution. [music] CPCV is the difference between reporting a point estimate and reporting a confidence interval.

If your team has [music] tried many model configurations against the same limited historical window, the more configurations you've tried, the higher the chance one of [music] them looks good purely by luck. And this technique is a direct way to quantify that risk. Let's compress everything we've covered into the heuristics you should actually [music] walk away with. Five heuristics. Let's go through them.

One, >> [music] >> never shuffle time ordered data. Order is information. A random shuffle destroys the one structural [music] fact you actually have. Two, size the embargo programmatically. Embargo [music] should be greater than or equal to your maximum feature look back plus your label horizon. [music]

Never a handpicked constant. Match your strategy to stationarity. Expanding window for stable processes. >> [music] >> Sliding window when the process drifts. Four, report a distribution of fold scores, not just an average. Fold score variance

[music] is a signal about regime stability. Don't throw it away. Five, validate the whole pipeline, not just the split. Leakage most often hides in feature engineering computed [music] before the split ever runs. If you take one action item out of this talk, make it this. Turn these five

[music] points into an actual pull request checklist item. Something like time series split reviewed, shuffle free, [music] embargo sized to look back plus horizon, feature pipeline computed per fold. That single [music] process change is probably the highest leverage thing a team can do here. A few resources [music] if you want to go deeper. Quickly, four places to go deeper.

>> [music] >> Advances in Financial Machine Learning by Marcos Lopez de Prado is the definitive source for purged [music] K-fold, embargo sizing, and CPCV. Scikit-learn's time series split documentation is a fine starting point for expanding window validation. Just [music] remember, it doesn't support embargo natively, so treat it as a base to extend, the way we did in the code slide. Prophet's cross validation [music] function is the fastest way to see rolling origin evaluation working end to end without writing a [music] custom splitter first. And Forecasting Principles and Practice by Hyndman and Athanasopoulos [music] has the clearest independent treatment of rolling origin evaluation outside finance [music] specific literature. And

it's free online. That's the full arc, from why standard k-fold breaks [music] on time ordered data to how to actually defend a backtest. The one-line summary, correct [music] time series validation isn't a single function call. It's a discipline that spans the splitter, the embargo [music] sizing, and the feature pipeline upstream of both. Validate [music] like the future depends on it, because in production, it does.