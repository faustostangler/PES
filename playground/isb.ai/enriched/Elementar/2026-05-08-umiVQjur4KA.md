Tudo resolvido!

1. O erro de lock no Git (`.git/index.lock`) foi corrigido e o arquivo removido.
2. O arquivo `.gitignore` foi atualizado para ignorar arquivos temporários (`*.tmp`) e o arquivo de cookies `.yt_dlp_cookies.txt` foi removido do rastreamento por segurança.
3. Todas as alterações foram salvas em commits locais (`8beb0fe` e `f28d1e5`).

Sua branch local `main` está atualizada e pronta. Quando desejar enviar os commits para o repositório remoto no GitHub, basta rodar no seu terminal interativo:

```bash
git push origin main
```